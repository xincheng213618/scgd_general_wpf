file(GLOB_RECURSE srcs CONFIGURE_DEPENDS src/*.cpp include/*.h)
add_library(sfr STATIC ${srcs})
find_package(OpenCV REQUIRED)
find_package(Eigen3 REQUIRED)
target_include_directories(sfr PUBLIC include)
target_include_directories(sfr PRIVATE ${OpenCV_INCLUDE_DIRS})
target_link_libraries(sfr PUBLIC ${OpenCV_LIBS})
target_link_libraries(sfr PUBLIC Eigen3::Eigen)