# 光谱仪幅值标定核心实现

> 给 Codex 的迁移说明。目标项目有光谱仪，但没有 ColorVision 的光谱仪服务；直接接目标项目现有 SDK。

## 大体逻辑

首次生成幅值文件时，使用同一台光谱仪、相同积分时间和平均次数，依次采集暗场和标准灯明场：

```csharp
dark = spectrometer.CaptureRaw(integrationTime, average, shutterClosed: true);
light = spectrometer.CaptureRaw(integrationTime, average, shutterClosed: false);

// 使用相同厂家 SDK 时等价调用：
// CM_Emission_CreateMagiude(integrationTime, dark, light,
//     standardLampFile, wavelengthFile, outputMagnitudeFile);
```

已有幅值文件的用户复校有两种模式：

- 完整光谱：每个波长的修正倍率为 `标准绝对光谱 / 当前实测绝对光谱`。
- 单独光度：全部波长使用同一个倍率 `标准亮度 / 当前实测亮度`。

## 核心计算代码（C# 参考）

```csharp
// wavelength：原幅值文件的波长网格。
// oldCoefficient：原幅值系数。
// measured、standard：绝对光谱，Linear() 负责线性插值。
public static double[] CorrectSpectrum(
    double[] wavelength,
    double[] oldCoefficient,
    Spectrum measured,
    Spectrum standard)
{
    var factor = new double[wavelength.Length];
    double peak = wavelength.Max(w => measured.Linear(w));
    double threshold = Math.Max(1e-12, peak * 1e-4);

    for (int i = 0; i < wavelength.Length; i++)
    {
        double actual = measured.Linear(wavelength[i]);
        factor[i] = actual > threshold
            ? standard.Linear(wavelength[i]) / actual
            : double.NaN;
    }

    // 低信号点：中间使用相邻有效倍率线性插值，两端使用最近有效倍率；
    // 有效点少于两个时直接失败。
    FillLowSignalFactors(wavelength, factor);

    return oldCoefficient.Zip(factor, (oldValue, correction) =>
    {
        double value = oldValue * correction;
        if (!double.IsFinite(value) || value < 0)
            throw new InvalidOperationException("幅值校正系数无效");
        return value;
    }).ToArray();
}

public static double[] CorrectBrightness(
    double[] oldCoefficient, double targetBrightness, double measuredBrightness)
{
    if (!double.IsFinite(targetBrightness) || !double.IsFinite(measuredBrightness) ||
        targetBrightness <= 0 || measuredBrightness <= 0)
        throw new InvalidOperationException("标准亮度和实测亮度必须是有限正数");

    double factor = targetBrightness / measuredBrightness;
    return oldCoefficient.Select(value => value * factor).ToArray();
}
```

## 必须保留的规则

- 波长必须严格递增并覆盖待校正范围；波长、光谱值和结果系数必须有限，光谱值和系数不能为负数。
- 实测光谱若是“相对光谱 × 绝对系数”，必须先还原为绝对光谱再计算。
- 低信号点不能直接做除法，完整光谱至少要有两个有效实测点。
- 先另存新文件并重新读取校验，成功后再切换并加载；失败时继续使用旧文件。
- 若沿用 `Magiude.dat`，字段顺序为：`UInt64 文件总长度`、`Single 积分时间`、`Int32 光度系数`、`UInt64 点数`、全部 `Double 波长`、全部 `Double 幅值系数`。
- 若目标项目使用其他文件格式，只迁移算法和安全流程，不照搬二进制格式。
