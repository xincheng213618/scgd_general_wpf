# 单色 / 四色用户校正核心实现

> 给 Codex 的迁移说明。目标项目有相机和光谱仪，但没有 ColorVision 的相机服务、光谱仪服务；不要搬服务层，只按目标项目现有 SDK 接入采集。

## 大体逻辑

1. 提供两种模式：`SinglePoint`（单色/单点）和 `RGBW`（四色）。
2. 每个色块保存一组数据：相机测得的亮色度 `Y、x、y`，以及光谱仪测得的参考 `Y、x、y`。光谱仪的原始幅值曲线可一并保存用于核对。
3. 单色只需要一组数据；四色需要 `R、G、B、W` 四组，采集顺序不限。
4. 相机和光谱仪直接调用目标项目已有接口采集，不要求存在设备服务。采集结果按色块配对后再计算。
5. 任一输入改变后，立即作废旧计算结果。计算成功后只替换原校正文件中的 `3×3` 亮色度矩阵，其他曝光、增益等字段保持不变。

## 核心计算代码（C# 参考）

```csharp
public readonly record struct Yxy(double Y, double x, double y);
public sealed record Sample(Yxy Camera, Yxy Reference);

// 矩阵均为 3×3、按行排列；Solve(A, b) 表示带主元选择的线性方程求解。
public static class UserColorCalibration
{
    public static double[] CorrectSinglePoint(double[] oldMatrix, Sample sample)
    {
        double[] cameraRaw = Solve(ToMatrix(oldMatrix), ToXyz(sample.Camera));
        double[] referenceRaw = Solve(ToMatrix(oldMatrix), ToXyz(sample.Reference));
        var result = new double[9];

        for (int channel = 0; channel < 3; channel++)
        {
            if (cameraRaw[channel] == 0)
                throw new InvalidOperationException("相机反解通道为 0，无法做单点校正");

            double ratio = referenceRaw[channel] / cameraRaw[channel];
            for (int row = 0; row < 3; row++)
                result[row * 3 + channel] = oldMatrix[row * 3 + channel] * ratio;
        }
        return Checked(result);
    }

    public static double[] CorrectRgbw(double[] oldMatrix, Sample red, Sample green,
        Sample blue, Sample white)
    {
        Sample[] samples = [red, green, blue, white];
        var a = new double[9, 9];
        var b = new double[9];
        var cameraRaw = new double[4][];

        for (int i = 0; i < 4; i++)
        {
            cameraRaw[i] = Solve(ToMatrix(oldMatrix), ToXyz(samples[i].Camera));
            Yxy reference = Validate(samples[i].Reference);
            double xOverY = reference.x / reference.y;
            double zOverY = (1 - reference.x - reference.y) / reference.y;

            int xRow = i * 2;
            int zRow = xRow + 1;
            for (int channel = 0; channel < 3; channel++)
            {
                double raw = cameraRaw[i][channel];
                a[xRow, channel] = raw;
                a[xRow, channel + 3] = -raw * xOverY;
                a[zRow, channel + 3] = -raw * zOverY;
                a[zRow, channel + 6] = raw;
            }
        }

        // 第 9 条方程用白色参考亮度约束 Y。
        for (int channel = 0; channel < 3; channel++)
            a[8, channel + 3] = cameraRaw[3][channel];
        b[8] = white.Reference.Y;

        return Checked(Solve(a, b));
    }

    private static double[] ToXyz(Yxy value)
    {
        value = Validate(value);
        return [value.Y * value.x / value.y, value.Y,
            value.Y * (1 - value.x - value.y) / value.y];
    }

    private static Yxy Validate(Yxy value)
    {
        if (!double.IsFinite(value.Y) || !double.IsFinite(value.x) ||
            !double.IsFinite(value.y) || value.y == 0)
            throw new InvalidOperationException("Y、x、y 必须是有限数值，且 y 不能为 0");
        return value;
    }

    private static double[] Checked(double[] values)
    {
        if (values.Any(value => !double.IsFinite(value)))
            throw new InvalidOperationException("校正结果包含无效数值");
        return values;
    }

    // ToMatrix：把 9 个按行排列的数转成 double[3,3]。
    // Solve：优先复用目标项目的线性代数库；矩阵不可逆时必须报错，不能返回旧值或全 0。
}
```

## 必须保留的规则

- 相机数据是亮色度 `Yxy`；光谱仪数据是参考值，原始来源是光谱幅值。
- `R/G/B/W` 不要求固定采集顺序，但最终必须按色块正确配对，不能复用同一次光谱结果。
- 允许有限负数；只拒绝 `NaN`、无穷大、`y = 0`、缺失数据和不可逆矩阵。
- 先生成新结果并校验，再保存或替换；失败时不得破坏原校正文件。
- 至少测试：恒等矩阵单色、完整 RGBW、乱序采集、输入修改后结果失效、缺失/重复数据、非法数值、不可逆矩阵。
