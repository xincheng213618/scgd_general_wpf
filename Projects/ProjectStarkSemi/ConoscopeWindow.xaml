<Window x:Class="ProjectStarkSemi.ConoscopeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:im="clr-namespace:ColorVision.ImageEditor;assembly=ColorVision.ImageEditor"
        xmlns:local="clr-namespace:ProjectStarkSemi"
        xmlns:Conoscope="clr-namespace:ProjectStarkSemi.Conoscope"
        xmlns:properties="clr-namespace:ProjectStarkSemi.Properties"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:xcad="https://github.com/Dirkster99/AvalonDock"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance  Conoscope:ConoscopeManager}"
        Title="" Height="750" Width="1582" Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized" Closed="Window_Closed">
        <Window.Resources>
        <DataTemplate x:Key="DocumentHeaderTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Foreground="{DynamicResource GlobalTextBrush}" Text="{Binding Title}" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource GlobalBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" WindowChrome.IsHitTestVisibleInChrome="True">
            <DockPanel Margin="6,2">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                    <Button Command="{Binding EditConoscopeConfigCommand}" Padding="2" Height="32" Width="32" Margin="0,0,8,0" ToolTip="设置">
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="rotateTransform2" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.5" FillBehavior="Stop"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <TextBlock Text="&#xE713;" HorizontalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="18" Foreground="{DynamicResource GlobalTextBrush}">
                            <TextBlock.RenderTransform>
                                <RotateTransform x:Name="rotateTransform2" CenterX="9" CenterY="9"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Menu x:Name="menu" Padding="2" Background="Transparent" VerticalAlignment="Center" />
                    <Separator Margin="8,0" />
                    <Button Content="打开" Click="OpenFile_Click" Padding="10,4" Margin="0,0,6,0"/>
                    <Button Content="方位角导出" Click="btnExportAngleMode_Click" Padding="10,4" Margin="0,0,6,0"/>
                    <Button Content="极角导出" Click="btnExportCircleMode_Click" Padding="10,4" Margin="0,0,6,0"/>
                    <Button Content="高级导出" Click="btnAdvancedExport_Click" Padding="10,4" Margin="0,0,10,0"/>
                    <TextBlock Text="型号" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox x:Name="cbModelType" Width="100" SelectionChanged="cbModelType_SelectionChanged" SelectedValue="{Binding Config.CurrentModel}" Margin="0,0,10,0"/>
                    <Button x:Name="btnOpenObservationCamera" Content="{x:Static properties:Resources.OpenObservationCamera}" Click="btnOpenObservationCamera_Click" Padding="10,4"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <xcad:DockingManager x:Name="DockingManager" Grid.Row="1" Margin="-6,-6" Padding="0" Background="Transparent" BorderThickness="0" DocumentHeaderTemplate="{StaticResource DocumentHeaderTemplate}">
            <xcad:LayoutRoot x:Name="_layoutRoot">
                <xcad:LayoutPanel Orientation="Horizontal">
                    <xcad:LayoutAnchorablePaneGroup DockWidth="320">
                        <xcad:LayoutAnchorablePane>
                            <xcad:LayoutAnchorable x:Name="ControlPanelPane" Title="控制面板" CanAutoHide="True" CanClose="False" CanFloat="True" ContentId="ControlPanel">
                                <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                                    <StackPanel x:Name="StackPanelControl" Width="300">
                                        <StackPanel Margin="0,0,0,5">
                                            <Border x:Name="DisPlayBorder" Style="{DynamicResource BorderModuleArea}">
                                                <StackPanel>
                                                    <Button Content="ExecuteFlow" x:Name="ButtonRun" FontWeight="Bold" FontSize="16" Click="Button_FlowRun_Click" />
                                                    <ProgressBar Maximum="100" Value="{Binding FlowEngineManager.BatchProgress}"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </StackPanel>
                                </ScrollViewer>
                            </xcad:LayoutAnchorable>
                        </xcad:LayoutAnchorablePane>
                    </xcad:LayoutAnchorablePaneGroup>

                    <xcad:LayoutPanel Orientation="Vertical">
                        <xcad:LayoutDocumentPaneGroup>
                            <xcad:LayoutDocumentPane>
                                <xcad:LayoutDocument Title="图像显示" ContentId="ImageView" CanClose="False">
                                    <Grid x:Name="ImageViewHost">
                                        <im:ImageView x:Name="ImageView" />
                                        <Border x:Name="CoordinateDragOverlay" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Collapsed" IsHitTestVisible="False" Background="#CC202020" BorderBrush="#FFFFD400" BorderThickness="1" CornerRadius="4" Padding="8,4">
                                            <TextBlock x:Name="CoordinateDragOverlayText" Foreground="White" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>
                                </xcad:LayoutDocument>
                            </xcad:LayoutDocumentPane>
                        </xcad:LayoutDocumentPaneGroup>

                        <xcad:LayoutAnchorablePaneGroup DockHeight="450">
                            <xcad:LayoutAnchorablePane>
                                <xcad:LayoutAnchorable x:Name="ChannelPanelPane" Title="通道与导出" CanAutoHide="True" CanClose="True" CanFloat="True" ContentId="ChannelPanel">
                                    <Grid Margin="5">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="5">
                                                <CheckBox x:Name="chkShowRed" Content="R" Foreground="Red" IsChecked="{Binding Config.IsShowRedChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                                <CheckBox x:Name="chkShowGreen" Content="G" Foreground="Green" IsChecked="{Binding Config.IsShowGreenChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                                <CheckBox x:Name="chkShowBlue" Content="B" Foreground="Blue" IsChecked="{Binding Config.IsShowBlueChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                                <CheckBox x:Name="chkShowXlue" Content="X" Foreground="Gold" IsChecked="{Binding Config.IsShowXChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                                <CheckBox x:Name="chkShowYlue" Content="Y" Foreground="Gray" IsChecked="{Binding Config.IsShowYChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                                <CheckBox x:Name="chkShowZlue" Content="Z" Foreground="Violet" IsChecked="{Binding Config.IsShowZChannel}" Margin="5,0" Checked="RgbChannelVisibility_Changed" Unchecked="RgbChannelVisibility_Changed"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <GroupBox Grid.Row="1" Header="导出设置" Margin="0,0,0,5">
                                            <StackPanel Orientation="Horizontal">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="导出模式:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                                    <ComboBox x:Name="cbExportMode" MinWidth="120" SelectedIndex="0">
                                                        <ComboBoxItem Content="按方位角导出" Tag="Angle"/>
                                                        <ComboBoxItem Content="按极角导出" Tag="Circle"/>
                                                    </ComboBox>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                                                    <TextBlock Text="导出通道:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                                    <ComboBox x:Name="cbExportChannel" MinWidth="120" SelectedIndex="4">
                                                        <ComboBoxItem Content="R通道" Tag="R"/>
                                                        <ComboBoxItem Content="G通道" Tag="G"/>
                                                        <ComboBoxItem Content="B通道" Tag="B"/>
                                                        <ComboBoxItem Content="X通道" Tag="X"/>
                                                        <ComboBoxItem Content="Y通道" Tag="Y"/>
                                                        <ComboBoxItem Content="Z通道" Tag="Z"/>
                                                    </ComboBox>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal">
                                                    <Button Content="方位角导出" Click="btnExportAngleMode_Click" Padding="10,5" Margin="5,0,5,0"/>
                                                    <Button Content="极角导出" Click="btnExportCircleMode_Click" Padding="10,5"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </GroupBox>
                                    </Grid>
                                </xcad:LayoutAnchorable>

                                <xcad:LayoutAnchorable x:Name="ReferencePlotPane" Title="参考曲线" CanAutoHide="True" CanClose="True" CanFloat="True" ContentId="ReferencePlot">
                                    <Grid Margin="5">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        <StackPanel>
                                            <StackPanel Orientation="Horizontal">
                                                <TextBlock Text="参考模式:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                                <TextBlock x:Name="tbReferenceMode" MinWidth="80" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                                <TextBlock Text="当前值:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                                <TextBlock x:Name="tbReferenceValue" MinWidth="80" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                                <Button Content="导出当前" Click="btnExportCurrentReference_Click" Padding="10,2" Margin="5,0,5,0"/>
                                            </StackPanel>
                                        </StackPanel>
                                        <ScottPlot:WpfPlot Grid.Row="1" x:Name="wpfPlotReference" />
                                    </Grid>
                                </xcad:LayoutAnchorable>

                                <xcad:LayoutAnchorable x:Name="SettingPanelPane" Title="设置" CanAutoHide="True" CanClose="True" CanFloat="True" ContentId="SettingPanel">
                                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                                        <StackPanel Margin="5">
                                            <Grid x:Name="GridSetting" Margin="0,0,0,10" />
                                            <GroupBox Header="{x:Static properties:Resources.ImageFilter}">
                                                <StackPanel>
                                                    <DockPanel Margin="0,0,0,10">
                                                        <TextBlock Text="{x:Static properties:Resources.FilterType}" VerticalAlignment="Center" Width="80"/>
                                                        <ComboBox x:Name="cbFilterType" SelectionChanged="cbFilterType_SelectionChanged" SelectedIndex="0">
                                                            <ComboBoxItem Content="{x:Static properties:Resources.NoFilter}"/>
                                                            <ComboBoxItem Content="{x:Static properties:Resources.LowPassFilter}"/>
                                                            <ComboBoxItem Content="{x:Static properties:Resources.MovingAverage}"/>
                                                            <ComboBoxItem Content="{x:Static properties:Resources.GaussianFilter}"/>
                                                            <ComboBoxItem Content="{x:Static properties:Resources.MedianFilter}"/>
                                                            <ComboBoxItem Content="{x:Static properties:Resources.BilateralFilter}"/>
                                                        </ComboBox>
                                                    </DockPanel>
                                                    <DockPanel Margin="0,0,0,5">
                                                        <TextBlock Text="{x:Static properties:Resources.KernelSize}" VerticalAlignment="Center" Width="80"/>
                                                        <TextBlock Text="{Binding ElementName=sliderKernelSize, Path=Value}" Width="30" TextAlignment="Right" VerticalAlignment="Center"/>
                                                        <Slider x:Name="sliderKernelSize" Minimum="1" Maximum="31" Value="5" TickFrequency="2" IsSnapToTickEnabled="True" Margin="5,0,0,0"/>
                                                    </DockPanel>
                                                    <DockPanel Margin="0,0,0,5">
                                                        <TextBlock Text="σ (Sigma)" VerticalAlignment="Center" Width="80"/>
                                                        <TextBlock Text="{Binding ElementName=sliderSigma, Path=Value, StringFormat=F1}" Width="30" TextAlignment="Right" VerticalAlignment="Center"/>
                                                        <Slider x:Name="sliderSigma" Minimum="0.1" Maximum="10" Value="1.0" TickFrequency="0.1" IsSnapToTickEnabled="True" Margin="5,0,0,0" IsEnabled="False"/>
                                                    </DockPanel>
                                                    <DockPanel Margin="0,0,0,5">
                                                        <TextBlock Text="d" VerticalAlignment="Center" Width="80"/>
                                                        <TextBlock Text="{Binding ElementName=sliderD, Path=Value}" Width="30" TextAlignment="Right" VerticalAlignment="Center"/>
                                                        <Slider x:Name="sliderD" Minimum="1" Maximum="15" Value="5" TickFrequency="1" IsSnapToTickEnabled="True" Margin="5,0,0,0" IsEnabled="False"/>
                                                    </DockPanel>
                                                    <DockPanel Margin="0,0,0,5">
                                                        <TextBlock Text="σColor" VerticalAlignment="Center" Width="80"/>
                                                        <TextBlock Text="{Binding ElementName=sliderSigmaColor, Path=Value, StringFormat=F0}" Width="30" TextAlignment="Right" VerticalAlignment="Center"/>
                                                        <Slider x:Name="sliderSigmaColor" Minimum="1" Maximum="200" Value="75" TickFrequency="1" IsSnapToTickEnabled="True" Margin="5,0,0,0" IsEnabled="False"/>
                                                    </DockPanel>
                                                    <DockPanel Margin="0,0,0,5">
                                                        <TextBlock Text="σSpace" VerticalAlignment="Center" Width="80"/>
                                                        <TextBlock Text="{Binding ElementName=sliderSigmaSpace, Path=Value, StringFormat=F0}" Width="30" TextAlignment="Right" VerticalAlignment="Center"/>
                                                        <Slider x:Name="sliderSigmaSpace" Minimum="1" Maximum="200" Value="75" TickFrequency="1" IsSnapToTickEnabled="True" Margin="5,0,0,0" IsEnabled="False"/>
                                                    </DockPanel>
                                                    <Button x:Name="btnApplyFilter" Content="{x:Static properties:Resources.ApplyFilter}" Click="btnApplyFilter_Click"/>
                                                </StackPanel>
                                            </GroupBox>
                                        </StackPanel>
                                    </ScrollViewer>
                                </xcad:LayoutAnchorable>
                            </xcad:LayoutAnchorablePane>
                        </xcad:LayoutAnchorablePaneGroup>
                    </xcad:LayoutPanel>
                </xcad:LayoutPanel>
            </xcad:LayoutRoot>
        </xcad:DockingManager>

        <StatusBar Grid.Row="2" Height="25">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem Grid.Column="0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static properties:Resources.CurrentModel}" Margin="0,0,5,0"/>
                    <TextBlock x:Name="tbCurrentModel" Text="VA60" FontWeight="Bold"/>
                </StackPanel>
            </StatusBarItem>

            <Separator Grid.Column="1" />

            <StatusBarItem Grid.Column="2">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static properties:Resources.MeasurementCameraStatus}" Margin="0,0,5,0"/>
                    <TextBlock x:Name="tbMeasurementCameraStatus" Text="{x:Static properties:Resources.NotOpened}" Foreground="Gray"/>
                </StackPanel>
            </StatusBarItem>

            <Separator Grid.Column="3" x:Name="ObservationCameraSeparator"/>

            <StatusBarItem Grid.Column="4" x:Name="ObservationCameraStatusItem">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Static properties:Resources.ObservationCameraStatus}" Margin="0,0,5,0"/>
                    <TextBlock x:Name="tbObservationCameraStatus" Text="{x:Static properties:Resources.NotOpened}" Foreground="Gray"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
