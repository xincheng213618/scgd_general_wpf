# Conoscope 需求对比清单

## 范围

对比当前 `ProjectStarkSemi` 中 Conoscope 相关实现与最新需求，整理：

- 已实现
- 部分实现
- 未实现
- 备注与代码位置

> 说明：本文档基于当前代码状态整理，不代表最终方案拆分顺序。

---

## 总览

| 模块 | 状态 | 说明 |
|---|---|---|
| 界面功能分区 | 部分实现 | 已重构为AvalonDock面板系统（SettingPanel/ChannelPanel/ReferencePlot/ControlPanel），但尚未按”相机/分析/文件/预处理/标定参数”分区 |
| 型号选择 VA60/VA80 | 已实现 | 已有型号枚举、切换与角度范围联动 |
| 观察相机界面 | 部分实现 | 已能打开独立窗口，但缺少尺寸圈选、参数联动、仅 VA60 显示等约束 |
| 图像 3D 图显示 | 未实现 | 当前只有 2D 图像与伪彩显示 |
| 色差计算 | 未实现 | 没有基准图、基准光源、逐点色差计算逻辑 |
| ND 切换 | 未实现 | 无 ND 下拉、无模板联动 |
| 测量光斑选择 | 未实现 | 无 3/2/1/0.5mm 光斑下拉 |
| 测量模板自动适配 | 未实现 | 当前只有模板选择，无按 ND 自动匹配 |
| 对比度测试 | 未实现 | 无黑白记录与对比度计算流程 |
| 色域计算 | 未实现 | 无 RGB 记录、无色域公式与标准色域覆盖计算 |
| 关注点设置 | 未实现 | 只有参考线/圆辅助图形，没有完整关注点系统 |
| 色品图显示 | 未实现 | 无 1931/1976 色品图显示 |
| XYZ/xy/对比度/色差/色域显示 | 部分实现 | 已有 RGB/XYZ 通道，ExportChannel已定义CieX/CieY(xy)和CieU/CieV(uv)，但无对比度/色差/色域 |
| 数据导出 | 部分实现 | 已支持多通道与高级导出（含CieX/CieY/CieU/CieV），但缺少对比度/色差/色域按计算状态导出 |

---

## 1. 界面功能分区

### 需求
主要分为：

1. 相机
2. 分析
3. 文件
4. 预处理
5. 标定参数

### 当前状态
**部分实现**

代码已重构为基于AvalonDock的面板系统，由`Layout/DockLayoutManager.cs`管理布局持久化。当前面板结构：

| 面板 | ContentId | 内容 |
|---|---|---|
| 图像显示 | ImageView | 锥光镜图像显示（LayoutDocument，不可关闭） |
| 设置面板 | SettingPanel | 模板选择、参考线参数编辑、标定参数 |
| 通道与导出 | ChannelPanel | 通道勾选、导出按钮 |
| 参考曲线 | ReferencePlot | 方位角/极角分布曲线 |
| 控制面板 | ControlPanel | 其他控制功能 |

| 分区 | 当前情况 | 代码位置 |
|---|---|---|
| 相机 | 有测量相机、观察相机、模板选择、取图入口 | `ConoscopeWindow.xaml` |
| 文件 | 有打开、导出、当前截线导出、高级导出 | `Conoscope/ConoscopeExportService.cs`, `ConoscopeWindow.xaml.cs` |
| 预处理 | 已有滤波类型和参数 | `Conoscope/ImageFilterType.cs` |
| 标定参数 | 有模板选择、参考线参数编辑 | `ConoscopeManager.cs`, `ConoscopeConfig.cs` |
| 分析 | 当前只有方位角/极角曲线与导出，不包含色差/对比度/色域 | `ConoscopeView.xaml.cs` 中图表逻辑 |

布局菜单功能已实现（`Menus/LayoutMenuItems.cs`）：保存/应用/重置布局，切换各面板显隐。

### 结论
需要做的是**重新组织界面结构**，不是从零做全部功能。

---

## 2. 型号选择

### 需求
增加型号下拉选择，目前只有 VA60 / VA80。

### 当前状态
**已实现**

| 项目 | 当前情况 | 代码位置 |
|---|---|---|
| 型号枚举 | 已有 `VA60`、`VA80` | `Conoscope/ConoscopeModelType.cs` |
| 当前型号配置 | 已有 `CurrentModel` | `Conoscope/ConoscopeConfig.cs` |
| 下拉绑定 | 已接入 `cbModelType` | `ConoscopeWindow.xaml`, `ConoscopeWindow.xaml.cs:178` |
| 型号切换事件 | 已有 `ModelTypeChanged` | `ConoscopeConfig.cs` |
| 角度范围联动 | 已根据型号更新曲线范围 | `ConoscopeWindow.xaml.cs:165-181` |

### 结论
这项已具备基础功能，只需要和后续界面分区、观察相机显隐、模板适配逻辑进一步联动。

---

## 3. 观察相机界面

### 需求摘要
- 上方增加打开观察相机按键
- 仅 VA60 显示，VA80 不显示
- 观察相机窗口可拖动、可调整大小、非阻塞主界面
- 外部设置增加观察相机尺寸系数、中心点坐标
- 观察界面增加尺寸下拉：3 / 2 / 1 / 0.5 mm
- 根据尺寸和系数画红色圆

### 当前状态
**已实现**

| 子项 | 当前情况 | 状态 | 代码位置 |
|---|---|---|---|
| 打开观察相机按钮 | Conoscope 窗口上方已有按钮 | 已实现 | `ConoscopeWindow.xaml`, `btnOpenObservationCamera_Click` |
| 独立窗口 | 已有 `MVSViewWindow` | 已实现 | `MVSViewWindow.xaml/.cs` |
| 非阻塞主界面 | `Show()` 打开，不是模态 | 已实现 | `ConoscopeWindow.xaml.cs`, `MenuMVSVideo` |
| 窗口拖动/缩放 | 普通 WPF Window，天然支持 | 已实现 | `MVSViewWindow.xaml` |
| 仅 VA60 显示按钮 | 按 `HasObservationCamera` 显隐，VA60 默认 true，VA80 默认 false；独立菜单入口也做了可见性和执行防护 | 已实现 | `ConoscopeWindow.xaml.cs`, `MVSViewWindow.xaml.cs` |
| 观察相机尺寸系数 | 已在型号参数中配置，默认 0.02 mm/像素 | 已实现 | `ConoscopeModelProfile.cs` |
| 观察相机中心点坐标 | 已在型号参数中配置 X/Y，默认 640/512 | 已实现 | `ConoscopeModelProfile.cs` |
| 尺寸下拉 3/2/1/0.5mm | 观察相机窗口提供四个尺寸选项 | 已实现 | `MVSViewWindow.xaml` |
| 红色圆叠加 | 按配置中心点绘制红色圆；像素直径 = 选择尺寸 / 尺寸系数 | 已实现 | `MVSViewWindow.xaml.cs` |

### 结论
观察相机需求已闭环；后续如果要支持隐藏红圈或更多光栅尺寸，可在 `MVSViewWindowConfig` 中扩展选项。

---

## 4. 图像 3D 图显示

### 需求
伪彩色图像支持 3D 显示，可平移/旋转。

### 当前状态
**未实现**

| 项目 | 当前情况 |
|---|---|
| 2D 图像显示 | 已有 `ImageView` |
| 伪彩开关 | 有 `ImageView.Config.IsPseudo = true` |
| 3D 视图 | 未见 |
| 平移/旋转 | 未见 |

### 结论
目前仅有 2D 伪彩，不包含 3D 曲面显示。

---

## 5. 色差计算功能

### 需求摘要
- 支持固定基准 / 实测图基准
- 基准光源支持 D65 / D50 / A / D75 / 图像中心 / 自定义
- 支持保存基准图
- 后续逐点计算色差

### 当前状态
**未实现**

| 子项 | 当前情况 |
|---|---|
| 色差公式 | 未见实现 |
| 色差基准图保存 | 未见实现 |
| 固定 uv 基准 | 未见实现 |
| 中心点基准 | 未见实现 |
| 自定义坐标输入 | 未见实现 |
| 色差显示 | 未见实现 |
| 色差导出 | 未见实现 |

### 结论
需从数据结构、UI 流程、显示和导出全链路新增。

---

## 6. ND 切换

### 需求
增加 ND 下拉：0 / 8 / 64 / 1000，支持增减。

### 当前状态
**未实现**

未发现 ND 相关配置、UI 或模板联动代码。

---

## 7. 测量光斑选择

### 需求
测试光斑下拉：3 / 2 / 1 / 0.5 mm，支持增减。

### 当前状态
**未实现**

未发现光斑尺寸选择的配置和界面。

---

## 8. 测量模板自动适配

### 需求
根据当前 ND 自动选择对应模板测试。

### 当前状态
**未实现**

| 项目 | 当前情况 |
|---|---|
| 模板手动选择 | 已实现 |
| 自动匹配 ND 模板 | 未实现 |

相关代码：`MenuItem_Template`、`ComboxCalibrationTemplate`

---

## 9. 对比度测试功能

### 需求摘要
- 增加黑 / 白按钮
- 测量后记录黑白数据
- 点击计算对比度，计算 白亮度 / 黑亮度

### 当前状态
**未实现**

未发现黑白缓存、对比度计算、结果显示、导出逻辑。

---

## 10. 色域计算功能

### 需求摘要
- 增加 R / G / B 按钮记录三张图
- 计算色域
- 支持 NTSC / sRGB / DCI-P3 / PAL / Adobe RGB

### 当前状态
**未实现**

| 子项 | 当前情况 |
|---|---|
| RGB 基准图记录 | 未实现 |
| 色域类型下拉 | 未实现 |
| 标准色域参数 | 未实现 |
| 色域计算 | 未实现 |
| 色域显示 | 未实现 |
| 色域导出 | 未实现 |

---

## 11. 关注点设置

### 需求摘要
- 支持实时关注点，尺寸可调
- 坐标以极角和方位角显示
- 显示 XYZ、xy，以及已完成计算的对比度/色差/色域
- 支持关注点模板配置、手动布点、批量点阵布点

### 当前状态
**未实现**

### 已有接近能力
| 项目 | 当前情况 | 代码位置 |
|---|---|---|
| 参考线 / 圆辅助 | 已有 | `ReferenceLineParam`, `ToolReferenceLine` |
| 方位角线 / 同心圆管理 | 已有 | `PolarAngleLine`, `ConcentricCircleLine` |
| 图像坐标转角度分析 | 部分已有 | `OpenConoscope` 和曲线更新逻辑 |

### 缺失能力
- 关注点对象模型
- 关注点模板
- 关注点实时刷新
- 点阵批量布点
- 关注点属性展示面板

---

## 12. 色品图显示

### 需求摘要
- 支持 1931 / 1976 色品图
- 勾选标准色域三角显示
- 选中关注点后，在色品图中高亮

### 当前状态
**未实现**

当前仅有：
- 方位角分布曲线
- 极角分布曲线

未见 CIE 色品图绘制与标准色域叠加。

---

## 13. XYZ / xy / 对比度 / 色差 / 色域显示

### 需求
图像通道显示增加下拉切换栏，支持 XYZ / xy / 对比度 / 色差 / 色域。

### 当前状态
**部分实现**

| 通道 | 当前状态 | 备注 |
|---|---|---|
| R | 已实现 | 通道勾选 |
| G | 已实现 | 通道勾选 |
| B | 已实现 | 通道勾选 |
| X | 已实现 | 通道勾选 |
| Y | 已实现 | 通道勾选 |
| Z | 已实现 | 通道勾选 |
| xy (CieX/CieY) | 部分实现 | ExportChannel已定义，导出支持；显示通道切换尚未实现 |
| uv (CieU/CieV) | 部分实现 | ExportChannel已定义，导出支持；显示通道切换尚未实现 |
| 对比度 | 未实现 | 无结果通道 |
| 色差 | 未实现 | 无结果通道 |
| 色域 | 未实现 | 无结果通道 |

### 备注
当前是多选 Checkbox 模式，不是“下拉切换栏”模式。

---

## 14. 数据导出

### 需求摘要
- 增加 XYZ / xy / uv 导出
- 对比度 / 色差 / 色域仅在已计算后导出
- 导出字段支持勾选

### 当前状态
**部分实现**

### 已实现
| 项目 | 当前情况 |
|---|---|
| 方位角导出 | 已实现 |
| 极角导出 | 已实现 |
| 当前截线导出 | 已实现 |
| 高级导出弹窗 | 已实现 |
| R/G/B/X/Y/Z 通道导出 | 已实现 |
| xy (CieX/CieY) 导出 | 已实现（`ExportChannel.CieX`, `ExportChannel.CieY`） |
| uv (CieU/CieV) 导出 | 已实现（`ExportChannel.CieU`, `ExportChannel.CieV`） |
| 按不同模式导出（Angle/Circle/CrossSection） | 已实现 |

### 未实现
| 项目 | 当前情况 |
|---|---|
| 对比度导出 | 未实现 |
| 色差导出 | 未实现 |
| 色域导出 | 未实现 |
| 按计算状态控制导出字段 | 未实现 |
| 用户勾选导出数据项 | 当前不完整 |

相关代码：`Conoscope/ConoscopeExportService.cs`、`AdvancedExportDialog.xaml/.cs`

---

## 当前代码里已经具备的基础能力

这些可以复用，后续不需要推倒重来：

| 能力 | 当前状态 | 代码位置 |
|---|---|---|
| VA60 / VA80 模型体系 | 已有 | `ConoscopeModelType.cs`, `ConoscopeConfig.cs` |
| 观察相机独立窗口 | 已有 | `MVSViewWindow.xaml/.cs` |
| 图像显示与伪彩 | 已有 | `ImageView` 相关逻辑 |
| 参考线 / 同心圆辅助分析 | 已有 | `ReferenceLineParam`, `PolarAngleLine`, `ConcentricCircleLine` |
| 方位角 / 极角图表 | 已有 | `wpfPlotDiameterLine`, `wpfPlotRCircle` |
| 模板选择 | 已有 | `ComboxCalibrationTemplate` |
| 预处理滤波 | 已有 | `ImageFilterType`, 相关 UI 和应用逻辑 |
| 基础导出 / 高级导出 | 已有 | `ConoscopeExportService`, `AdvancedExportDialog` |
| RGB / XYZ 通道显示 | 已有 | 通道勾选和配置 |
| xy / uv 通道导出 | 已有 | `ExportChannel.CieX`, `CieY`, `CieU`, `CieV` |
| 布局持久化 | 已有 | `DockLayoutManager`（保存/加载/重置） |
| 工作区标签页集成 | 已有 | `ConoscopeModuleService` |
| 配置编辑窗口 | 已有 | `ConoscopeConfigWindow` |
| 布局菜单项 | 已有 | `Menus/LayoutMenuItems.cs` |

---

## 建议优先级

### 第一阶段：界面与参数结构
| 优先级 | 项目 |
|---|---|
| P0 | 按“相机 / 分析 / 文件 / 预处理 / 标定参数”重组界面 |
| P0 | 观察相机功能补全：VA60 显示、尺寸下拉、中心点、尺寸系数、红圈叠加 |
| P0 | ND 下拉、光斑下拉、模板自动适配 |
| P1 | 显示通道结构改成更清晰的切换模式 |

### 第二阶段：分析功能
| 优先级 | 项目 |
|---|---|
| P0 | 对比度测试 |
| P0 | 色差测试 |
| P1 | 色域测试 |
| P1 | 关注点系统 |

### 第三阶段：可视化与导出
| 优先级 | 项目 |
|---|---|
| P1 | CIE 1931 / 1976 色品图 |
| P1 | 标准色域三角显示 |
| P2 | 3D 伪彩图 |
| P1 | 导出字段扩展到 xy / uv / 对比度 / 色差 / 色域 |

---

## 结论

当前代码已经完成了 Conoscope 的基础能力：

- 型号切换
- 图像显示
- 方位角/极角分析
- 观察相机窗口基础能力
- 滤波预处理
- 多种导出

但经过重构后，已经完整实现的主要有：

| 已完整实现项 |
|---|
| 型号选择 VA60 / VA80 |
| 基础观察相机窗口打开 |
| 基础预处理滤波 |
| 方位角 / 极角分析与导出 |
| R/G/B/X/Y/Z 通道显示 |
| xy (CieX/CieY) / uv (CieU/CieV) 通道导出 |
| AvalonDock可拖拽面板布局系统 |
| 布局持久化（保存/加载/重置） |
| 工作区标签页集成 |
| 配置编辑窗口 |

其余多数属于：

- 已有基础，需要扩展
- 或完全未开始实现

所以后续更适合按模块拆分推进，而不是一次性全部并到一个大改动里。
