<Window x:Class="ProjectStarkSemi.Conoscope.ConoscopeConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Conoscope 设置" Height="640" Width="720"
        MinHeight="520" MinWidth="620"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource GlobalBackground}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="当前型号" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <ComboBox x:Name="cbCurrentModel" Grid.Column="1" MinHeight="28" SelectionChanged="cbCurrentModel_SelectionChanged"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <GroupBox Header="全局设置" Grid.Column="0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ContentControl x:Name="GlobalSettingsHost" Margin="8"/>
                </ScrollViewer>
            </GroupBox>

            <GroupBox Header="型号参数" Grid.Column="2">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ContentControl x:Name="ModelProfileHost" Margin="8"/>
                </ScrollViewer>
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="关闭" Width="88" Height="30" IsDefault="True" Click="btnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>