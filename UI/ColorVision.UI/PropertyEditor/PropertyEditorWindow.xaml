<Window x:Class="ColorVision.UI.PropertyEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.UI"
        xmlns:properties ="clr-namespace:ColorVision.UI.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.PropertyEditorWindow}"  Height="450" Width="650" Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.InputBindings>
        <KeyBinding Key="F" Modifiers="Control" Command="ApplicationCommands.Find" />
    </Window.InputBindings>
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.Find" Executed="Find_Executed" />
    </Window.CommandBindings>
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
        <HierarchicalDataTemplate x:Key="TreeNodeTemplate" DataType="{x:Type local:PropertyTreeNode}" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Header}"/>
        </HierarchicalDataTemplate>
        <Style x:Key="PropertyEditorActionButton" TargetType="Button" BasedOn="{StaticResource ButtonDefault}">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="Padding" Value="12,3"/>
            <Setter Property="Margin" Value="3,0"/>
        </Style>
        <Style x:Key="PropertyEditorSearchBox" TargetType="TextBox" BasedOn="{StaticResource TextBoxBaseStyle}">
            <Setter Property="Background" Value="{DynamicResource GlobalBackground}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="SearchBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="SearchBorder" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="SearchBorder" Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="{DynamicResource  SecondaryRegionBrush}" Grid.RowSpan="3" BorderBrush="{DynamicResource BorderBrush}"  BorderThickness="1" />
        
        <!-- Search Bar and Sort Options -->
        <Grid Grid.Row="0" Margin="8,8,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="SearchBox" Grid.Column="0" Style="{StaticResource PropertyEditorSearchBox}"
                       Padding="30,4,34,4" Height="34" MinHeight="34" FontSize="14" BorderThickness="1"
                       AutomationProperties.Name="{x:Static properties:Resources.PropEditor_SearchPlaceholder}"
                       BorderBrush="{DynamicResource BorderBrush}" VerticalContentAlignment="Center"
                       TextChanged="SearchBox_TextChanged" PreviewKeyDown="SearchBox_PreviewKeyDown" Margin="0,0,0,0" />
            <Viewbox Width="14" Height="14" HorizontalAlignment="Left" Margin="10,0,0,0" Grid.Column="0" IsHitTestVisible="False">
                <Rectangle Width="16" Height="16">
                    <Rectangle.Resources>
                        <SolidColorBrush x:Key="canvas" Opacity="0" />
                        <SolidColorBrush x:Key="light-defaultgrey-10" Color="{DynamicResource defaultgreyColor}" Opacity="0.1" />
                    </Rectangle.Resources>
                    <Rectangle.Fill>
                        <DrawingBrush Stretch="None">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <DrawingGroup.Children>
                                        <GeometryDrawing Brush="{DynamicResource canvas}" Geometry="F1M16,16H0V0H16Z" />
                                        <GeometryDrawing Brush="{DynamicResource GlobalTextBrush}" Geometry="F1M11.592,10.7l3.854,3.854-.892.892L10.7,11.592a5.5,5.5,0,1,1,.892-.892ZM6.5,11A4.5,4.5,0,1,0,2,6.5,4.5,4.5,0,0,0,6.5,11Z" />
                                    </DrawingGroup.Children>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </Viewbox>
            <!-- Include the border and WPF text host's leading inset, matching actual input. -->
            <TextBlock x:Name="SearchPlaceholder" Grid.Column="0" Margin="33,0,34,0" IsHitTestVisible="False"
                       Text="{x:Static properties:Resources.PropEditor_SearchPlaceholder}" FontSize="{Binding FontSize, ElementName=SearchBox}"
                       Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" ToolTip="{x:Null}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <Button x:Name="SearchClearButton" Grid.Column="0" Width="28" Height="28" Margin="0,0,4,0"
                    HorizontalAlignment="Right" VerticalAlignment="Center" Click="ClearSearch_Click"
                    AutomationProperties.Name="{x:Static properties:Resources.PropEditor_ClearSearch}"
                    ToolTip="{x:Static properties:Resources.PropEditor_ClearSearch}">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource ButtonDefault}">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Text, ElementName=SearchBox}" Value="">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
                <TextBlock Text="&#xE8BB;" FontFamily="Segoe MDL2 Assets" FontSize="12" ToolTip="{x:Null}"
                           Foreground="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
            </Button>

            <ComboBox x:Name="SortComboBox" Grid.Column="1" Width="150"  Visibility="Collapsed"
                     Style="{DynamicResource ComboBox.Small}"
                     SelectionChanged="SortComboBox_SelectionChanged"/>
        </Grid>
        
        <Grid Grid.Row="1" Margin="8,8,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TreeView Name="treeView" SelectedItemChanged="TreeView_SelectedItemChanged" Padding="3" Width="150"
                      Background="{DynamicResource GlobalBackground}" ItemsSource="{Binding TreeNodes}" ItemTemplate="{StaticResource TreeNodeTemplate}">
                <TreeView.Resources>
                    <local:TreeViewItemMarginConverter x:Key="TreeViewItemMarginConverter" />
                    <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Width" Value="16" />
                        <Setter Property="Height" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Viewbox x:Name="ExpandViewbox" Height="12" Width="12" VerticalAlignment="Center" >
                                        <Rectangle  Height="12" Width="12" x:Name="ExpandPath" Fill="{DynamicResource DIScrollbarArrowCollapsed}"/>
                                    </Viewbox>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="ToggleButton.IsChecked" Value="True">
                                            <Setter TargetName="ExpandPath" Property="Fill" Value="{DynamicResource DIScrollbarArrowDownRight}" />
                                            <Setter TargetName="ExpandViewbox" Property="Margin" Value="0,2,0,0" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.Resources>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}"  BasedOn="{StaticResource TreeViewItemBaseStyle}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="ContextMenu" Value="{Binding ContextMenu}"/>
                        <Setter Property="Padding" Value="4,0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="MinHeight" Value="26"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
                        <!-- A header-only focus outline below replaces the default dotted adorner. -->
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <EventSetter Event="RequestBringIntoView" Handler="TreeViewItem_RequestBringIntoView"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition MinHeight="{TemplateBinding MinHeight}" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border Name="Bd" Margin="0,1" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" CornerRadius="4">
                                            <DockPanel LastChildFill="True" Margin="{Binding Converter={StaticResource TreeViewItemMarginConverter}, RelativeSource={RelativeSource TemplatedParent}}" >
                                                <ToggleButton Name="Expander" ClickMode="Press" Margin="0,0,3,0" Style="{StaticResource ExpandCollapseToggleStyle}" IsChecked="{Binding IsExpanded , RelativeSource={RelativeSource TemplatedParent}}" />
                                                <ContentPresenter Name="PART_Header" VerticalAlignment="Center" ContentSource="Header" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </DockPanel>
                                        </Border>
                                        <Border x:Name="SelectionIndicator" Width="3" Margin="2,6,0,6" CornerRadius="1.5"
                                                HorizontalAlignment="Left" Background="{DynamicResource PrimaryBrush}"
                                                Visibility="Collapsed" IsHitTestVisible="False"/>
                                        <ItemsPresenter Name="ItemsHost" Grid.Row="1"/>

                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger SourceName="Bd" Property="UIElement.IsMouseOver" Value="true">
                                            <Setter TargetName="Bd" Value="{DynamicResource SecondaryRegionBrush}" Property="Background" />
                                        </Trigger>
                                        <Trigger Property="TreeViewItem.IsExpanded" Value="false">
                                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                        <Trigger Property="ItemsControl.HasItems" Value="false">
                                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                                        </Trigger>
                                        <Trigger Property="TreeViewItem.IsSelected" Value="true">
                                            <Setter TargetName="Bd" Value="{DynamicResource SecondaryRegionBrush}" Property="Background" />
                                            <Setter TargetName="SelectionIndicator" Property="Visibility" Value="Visible"/>
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="TreeViewItem.IsSelected" Value="true" />
                                                <Condition Property="TreeViewItem.IsSelectionActive" Value="false" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="SelectionIndicator" Property="Opacity" Value="0.55"/>
                                        </MultiTrigger>
                                        <Trigger Property="IsKeyboardFocused" Value="True">
                                            <Setter TargetName="Bd" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="false">
                                            <Setter Property="UIElement.Opacity" Value=".4" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                </TreeView.ItemContainerStyle>
            </TreeView>
            <Border x:Name="EditorSurface" Grid.Column="1" Background="{DynamicResource GlobalBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Margin" Value="8,0,0,0"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Visibility, ElementName=treeView}" Value="Collapsed">
                                <Setter Property="Margin" Value="0"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <ScrollViewer x:Name="PropertyScrollViewer" Margin="5">
                        <StackPanel x:Name="PropertyPanel"/>
                    </ScrollViewer>
                    <StackPanel x:Name="SearchEmptyState" Visibility="Collapsed" Margin="20" VerticalAlignment="Center"
                                AutomationProperties.LiveSetting="Polite">
                        <TextBlock Text="{x:Static properties:Resources.PropEditor_NoSearchResults}" FontWeight="SemiBold"
                                   TextAlignment="Center" TextWrapping="Wrap" TextTrimming="None" ToolTip="{x:Null}"/>
                        <TextBlock Text="{x:Static properties:Resources.PropEditor_SearchEmptyHint}" Margin="0,6,0,14"
                                   Foreground="{DynamicResource SecondaryTextBrush}" TextAlignment="Center"
                                   TextWrapping="Wrap" TextTrimming="None" ToolTip="{x:Null}"/>
                        <Button x:Name="EmptySearchClearButton" Content="{x:Static properties:Resources.PropEditor_ClearSearch}"
                                Style="{StaticResource PropertyEditorActionButton}" HorizontalAlignment="Center" Click="ClearSearch_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

        </Grid>

        <Grid Grid.Row="2" Margin="5,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button x:Name="ResetButton" Content="{x:Static properties:Resources.Reset}" Style="{StaticResource PropertyEditorActionButton}" Click="Reset_Click"/>
                <Button x:Name="ResetDefaultsButton" Content="{x:Static properties:Resources.PropEditor_ResetToDefault}" Style="{StaticResource PropertyEditorActionButton}"
                        Click="ResetToFactory_Click" ToolTip="{x:Static properties:Resources.PropEditor_ResetTooltip}"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="ConfirmButton" Content="{x:Static properties:Resources.OK}" Style="{StaticResource PropertyEditorActionButton}"
                        Foreground="White" Background="{DynamicResource PrimaryBrush}" BorderBrush="{DynamicResource PrimaryBrush}" Click="OK_Click"/>
                <Button x:Name="CancelButton" Content="{x:Static properties:Resources.Cancel}" Style="{StaticResource PropertyEditorActionButton}" Click="Cancel_Click"/>
            </StackPanel>
        </Grid>
    </Grid>

</Window>
