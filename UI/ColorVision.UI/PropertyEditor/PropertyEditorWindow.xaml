<Window x:Class="ColorVision.UI.PropertyEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.UI"
        xmlns:ui ="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:properties ="clr-namespace:ColorVision.UI.Properties"
        mc:Ignorable="d"
        Title="配置"  Height="450" Width="650" Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
        <HierarchicalDataTemplate x:Key="TreeNodeTemplate" DataType="{x:Type local:PropertyTreeNode}" ItemsSource="{Binding Children}">
            <TextBlock Text="{Binding Header}"/>
        </HierarchicalDataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Border Background="{DynamicResource  SecondaryRegionBrush}" Grid.RowSpan="3" BorderBrush="{DynamicResource BorderBrush}"  BorderThickness="1" />
        
        <!-- Search Bar and Sort Options -->
        <Grid Grid.Row="0" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <hc:TextBox x:Name="SearchBox" Grid.Column="0" Style="{DynamicResource TextBoxPlus.Small}" 
                       Padding="20,1,2,1" BorderThickness="1" 
                       hc:InfoElement.Placeholder="搜索属性..." 
                       BorderBrush="#CCCEDB" VerticalContentAlignment="Center" 
                       TextChanged="SearchBox_TextChanged" Margin="0,0,5,0" />
            <Viewbox Width="12" Height="12" HorizontalAlignment="Left" Margin="4,0,0,0" Grid.Column="0">
                <Rectangle Width="16" Height="16">
                    <Rectangle.Resources>
                        <SolidColorBrush x:Key="canvas" Opacity="0" />
                        <SolidColorBrush x:Key="light-defaultgrey-10" Color="{DynamicResource defaultgreyColor}" Opacity="0.1" />
                    </Rectangle.Resources>
                    <Rectangle.Fill>
                        <DrawingBrush Stretch="None">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <DrawingGroup.Children>
                                        <GeometryDrawing Brush="{DynamicResource canvas}" Geometry="F1M16,16H0V0H16Z" />
                                        <GeometryDrawing Brush="{DynamicResource GlobalTextBrush}" Geometry="F1M11.592,10.7l3.854,3.854-.892.892L10.7,11.592a5.5,5.5,0,1,1,.892-.892ZM6.5,11A4.5,4.5,0,1,0,2,6.5,4.5,4.5,0,0,0,6.5,11Z" />
                                    </DrawingGroup.Children>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Rectangle.Fill>
                </Rectangle>
            </Viewbox>
            
            <ComboBox x:Name="SortComboBox" Grid.Column="1" Width="150" 
                     Style="{DynamicResource ComboBox.Small}"
                     SelectionChanged="SortComboBox_SelectionChanged"/>
        </Grid>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TreeView Name="treeView" SelectedItemChanged="TreeView_SelectedItemChanged" Margin="5,5,0,0" Width="150" ItemsSource="{Binding TreeNodes}" ItemTemplate="{StaticResource TreeNodeTemplate}">
                <TreeView.Resources>
                    <local:TreeViewItemMarginConverter x:Key="TreeViewItemMarginConverter" />
                    <Style x:Key="ExpandCollapseToggleStyle" TargetType="{x:Type ToggleButton}">
                        <Setter Property="Focusable" Value="False" />
                        <Setter Property="Width" Value="16" />
                        <Setter Property="Height" Value="16" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Viewbox x:Name="ExpandViewbox" Height="12" Width="12" VerticalAlignment="Center" >
                                        <Rectangle  Height="12" Width="12" x:Name="ExpandPath" Fill="{DynamicResource DIScrollbarArrowCollapsed}"/>
                                    </Viewbox>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="ToggleButton.IsChecked" Value="True">
                                            <Setter TargetName="ExpandPath" Property="Fill" Value="{DynamicResource DIScrollbarArrowDownRight}" />
                                            <Setter TargetName="ExpandViewbox" Property="Margin" Value="0,2,0,0" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </TreeView.Resources>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="{x:Type TreeViewItem}"  BasedOn="{StaticResource TreeViewItemBaseStyle}">
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="ContextMenu" Value="{Binding ContextMenu}"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="MinHeight" Value="19"/>
                        <EventSetter Event="RequestBringIntoView" Handler="TreeViewItem_RequestBringIntoView"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type TreeViewItem}">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition MinHeight="{TemplateBinding MinHeight}" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <Border Name="Bd"  BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" CornerRadius="{Binding Path=(hc:BorderElement.CornerRadius), RelativeSource={RelativeSource TemplatedParent}}">
                                            <DockPanel LastChildFill="True" Margin="{Binding Converter={StaticResource TreeViewItemMarginConverter}, RelativeSource={RelativeSource TemplatedParent}}" >
                                                <ToggleButton Name="Expander" ClickMode="Press" Margin="0,0,3,0" Style="{StaticResource ExpandCollapseToggleStyle}" IsChecked="{Binding IsExpanded , RelativeSource={RelativeSource TemplatedParent}}" />
                                                <ContentPresenter Name="PART_Header" VerticalAlignment="Center" ContentSource="Header" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                            </DockPanel>
                                        </Border>
                                        <ItemsPresenter Name="ItemsHost" Grid.Row="1"/>

                                    </Grid>
                                    <ControlTemplate.Triggers>
                                        <Trigger SourceName="Bd" Property="UIElement.IsMouseOver" Value="true">
                                            <Setter TargetName="Bd" Value="{DynamicResource SecondaryRegionBrush}" Property="Background" />
                                        </Trigger>
                                        <DataTrigger Binding="{Binding DisableExpanded}" Value="True">
                                            <Setter TargetName="Expander" Property="Visibility" Value="Collapsed" />
                                        </DataTrigger>
                                        <Trigger Property="TreeViewItem.IsExpanded" Value="false">
                                            <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
                                        </Trigger>
                                        <Trigger Property="ItemsControl.HasItems" Value="false">
                                            <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
                                        </Trigger>
                                        <Trigger Property="TreeViewItem.IsSelected" Value="true">
                                            <Setter TargetName="Bd" Value="{DynamicResource PrimaryBrush}" Property="Background" />
                                            <Setter Value="{DynamicResource TextIconBrush}" Property="Foreground" />
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="TreeViewItem.IsSelected" Value="true" />
                                                <Condition Property="TreeViewItem.IsSelectionActive" Value="false" />
                                            </MultiTrigger.Conditions>
                                            <Setter TargetName="Bd" Value="{DynamicResource DarkDefaultBrush}" Property="Background" />
                                            <Setter Value="{DynamicResource PrimaryTextBrush}" Property="Foreground" />
                                        </MultiTrigger>
                                        <Trigger Property="IsEnabled" Value="false">
                                            <Setter Property="UIElement.Opacity" Value=".4" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>

                </TreeView.ItemContainerStyle>
            </TreeView>
            <Border  Grid.Column="1" Margin="5,5,5,0"  Background="{DynamicResource GlobalBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="5">
                <ScrollViewer x:Name="PropertyScrollViewer" Margin="5">
                    <StackPanel x:Name="PropertyPanel">

                    </StackPanel>
                </ScrollViewer>
            </Border>

        </Grid>

        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
                <Button Content="恢复出厂" Foreground="White" Background="{StaticResource PrimaryBrush}" BorderThickness="0" Click="ResetToFactory_Click" ToolTip="重置到类默认值" />
                <Button Content="{x:Static properties:Resources.Reset}" Foreground="White" Background="{StaticResource PrimaryBrush}" BorderThickness="0" Click="Reset_Click" Margin="5,0,0,0" />
                <Button Content="{x:Static properties:Resources.OK}" Foreground="White" Background="{StaticResource PrimaryBrush}" BorderThickness="0" Click="OK_Click" Margin="5,0,0,0"/>
                <Button Content="{x:Static properties:Resources.Cancel}" Foreground="White" Background="{StaticResource PrimaryBrush}" BorderThickness="0" Click="Cancel_Click" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>

</Window>
