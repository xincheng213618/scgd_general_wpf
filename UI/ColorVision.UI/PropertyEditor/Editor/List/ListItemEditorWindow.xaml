<!-- ListItemEditorWindow.xaml -->
<Window x:Class="ColorVision.UI.PropertyEditor.Editor.List.ListItemEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="编辑项" Height="200" Width="600" 
        Background="{DynamicResource GlobalBackground}"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Editor Type Selection -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10" x:Name="EditorTypePanel" Visibility="Collapsed">
            <TextBlock Text="编辑器类型:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="EditorTypeComboBox" MinWidth="200" 
                      Style="{DynamicResource ComboBox.Small}"
                      SelectionChanged="EditorTypeComboBox_SelectionChanged"/>
        </StackPanel>

        <!-- Editor Content Container -->
        <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" Padding="10" BorderThickness="1" CornerRadius="5">
            <StackPanel x:Name="EditorPanel">
                <!-- Editor controls will be added here dynamically -->
            </StackPanel>
        </Border>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="确定" Width="80" Click="OkButton_Click" 
                    Margin="0,0,5,0" Background="{StaticResource PrimaryBrush}" 
                    Foreground="White" BorderThickness="0"/>
            <Button Content="取消" Width="80" Click="CancelButton_Click" 
                    Background="{StaticResource PrimaryBrush}" 
                    Foreground="White" BorderThickness="0"/>
        </StackPanel>
    </Grid>
</Window>
