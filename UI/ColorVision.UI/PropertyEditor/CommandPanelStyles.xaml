<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <SolidColorBrush x:Key="PropertyEditorCommandSurface" Color="{DynamicResource UpdateDialogSurfaceColor}" />
    <SolidColorBrush x:Key="PropertyEditorCommandSurfaceHover" Color="{DynamicResource UpdateDialogSurfaceHoverColor}" />
    <SolidColorBrush x:Key="PropertyEditorCommandBorder" Color="{DynamicResource UpdateDialogBorderColor}" Opacity="0.5" />
    <SolidColorBrush x:Key="PropertyEditorCommandTextPrimary" Color="{DynamicResource UpdateDialogTextPrimaryColor}" />
    <SolidColorBrush x:Key="PropertyEditorCommandTextSecondary" Color="{DynamicResource UpdateDialogTextSecondaryColor}" />
    <SolidColorBrush x:Key="PropertyEditorCommandAccent" Color="{DynamicResource UpdateDialogAccentColor}" />
    <Style x:Key="PropertyEditorCommandSection" TargetType="Border">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource PropertyEditorCommandBorder}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Padding" Value="0,0,0,6" />
        <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
    <Style x:Key="PropertyEditorCommandHeading" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource PropertyEditorCommandTextPrimary}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="2,0,2,4" />
    </Style>
    <Style x:Key="PropertyEditorCommandButton" TargetType="Button">
        <Setter Property="MinHeight" Value="48" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="9,5" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Foreground" Value="{DynamicResource PropertyEditorCommandTextPrimary}" />
        <Setter Property="Background" Value="{DynamicResource PropertyEditorCommandSurface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource PropertyEditorCommandBorder}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                        <ContentPresenter Margin="{TemplateBinding Padding}" Content="{TemplateBinding Content}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Background" Value="{DynamicResource PropertyEditorCommandSurfaceHover}" />
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource PropertyEditorCommandAccent}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="ButtonBorder" Property="BorderBrush" Value="{DynamicResource PropertyEditorCommandAccent}" />
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
