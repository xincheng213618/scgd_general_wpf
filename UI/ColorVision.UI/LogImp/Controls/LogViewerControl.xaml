<UserControl x:Class="ColorVision.UI.LogImp.Controls.LogViewerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:ColorVision.UI.LogImp.Controls"
             xmlns:models="clr-namespace:ColorVision.UI.LogImp.Models"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Foreground="{DynamicResource GlobalTextBrush}"
             Focusable="True"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True">
    <UserControl.Resources>
        <local:LogEntryForegroundConverter x:Key="LogEntryForegroundConverter"/>
        <Style x:Key="LogEntryTextBlockStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Padding" Value="2,0"/>
            <Setter Property="TextWrapping" Value="{Binding TextWrapping, RelativeSource={RelativeSource AncestorType={x:Type local:LogViewerControl}}}"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static models:LogEntryLevel.Fatal}">
                    <Setter Property="FontWeight" Value="Bold"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding Level}" Value="{x:Static models:LogEntryLevel.Error}">
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Background="{DynamicResource GlobalBackground}">
        <TextBox x:Name="PlainTextBox"
                 TextWrapping="{Binding TextWrapping, RelativeSource={RelativeSource AncestorType={x:Type local:LogViewerControl}}}"
                 Background="{DynamicResource GlobalBackground}"
                 Foreground="{DynamicResource GlobalTextBrush}"
                 BorderThickness="0"
                 Padding="2,0"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalContentAlignment="Top"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"/>

        <ListBox x:Name="EntriesListBox"
                 ItemsSource="{Binding VisibleEntries, RelativeSource={RelativeSource AncestorType={x:Type local:LogViewerControl}}}"
                 Background="{DynamicResource GlobalBackground}"
                 BorderThickness="0"
                 Visibility="Collapsed"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 ScrollViewer.HorizontalScrollBarVisibility="Auto"
                 ScrollViewer.CanContentScroll="True"
                 VirtualizingPanel.IsVirtualizing="True"
                 VirtualizingPanel.VirtualizationMode="Recycling"
                 VirtualizingPanel.ScrollUnit="Item"
                 SelectionMode="Extended"
                 HorizontalContentAlignment="Stretch">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemContainerStyle>
                <Style TargetType="{x:Type ListBoxItem}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="VerticalContentAlignment" Value="Top"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="BorderThickness" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:LogEntry}">
                    <TextBlock Text="{Binding Text}" Style="{StaticResource LogEntryTextBlockStyle}">
                        <TextBlock.Foreground>
                            <MultiBinding Converter="{StaticResource LogEntryForegroundConverter}">
                                <Binding Path="Level"/>
                                <Binding Path="UseLevelColors" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="Foreground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="WarningForeground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="ErrorForeground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="FatalForeground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="DebugForeground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                                <Binding Path="TraceForeground" RelativeSource="{RelativeSource AncestorType={x:Type local:LogViewerControl}}"/>
                            </MultiBinding>
                        </TextBlock.Foreground>
                    </TextBlock>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
