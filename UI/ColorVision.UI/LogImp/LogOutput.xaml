<UserControl x:Class="ColorVision.UI.LogImp.LogOutput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorVision.UI.LogImp"
             xmlns:logControls="clr-namespace:ColorVision.UI.LogImp.Controls"
             xmlns:properties ="clr-namespace:ColorVision.UI.Properties"
             xmlns:hc ="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d"         
             xmlns:ui ="clr-namespace:ColorVision.UI"
             d:DataContext="{d:DesignInstance ui:LogConfig}"
             d:DesignHeight="450" d:DesignWidth="800" Initialized="UserControl_Initialized" Focusable="True">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>
    <Grid x:Name="RootGrid" Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border x:Name="SearchPanel" Grid.Row="0" Margin="0,0,0,5" Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <hc:SearchBar x:Name="SearchBar1" TextChanged="SearchBar1_TextChanged" hc:InfoElement.Placeholder="{x:Static properties:Resources.FilterSupportsRegex}" hc:InfoElement.ShowClearButton="True" Style="{StaticResource SearchBarPlus}" IsRealTime="True"/>
                <Button x:Name="CloseSearchButton" Grid.Column="1" Margin="5,0,0,0" Width="30" Height="28" Padding="0" ToolTip="{x:Static properties:Resources.Close}">
                    <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="12" HorizontalAlignment="Center"/>
                </Button>
            </Grid>
        </Border>

        <logControls:LogViewerControl x:Name="LogViewer" Grid.Row="1" TextWrapping="{Binding TextWrapping}"/>
    </Grid>
</UserControl>
