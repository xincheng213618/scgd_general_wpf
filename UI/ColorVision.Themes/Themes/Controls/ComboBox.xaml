<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:hc="https://handyorg.github.io/handycontrol">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/ColorVision.Themes;component/Themes/Integrations/HandyControl.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ComboBoxItemBaseStyle" TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource CV.HandyControl.ComboBoxItem}">
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Style.Triggers>
            <Trigger Property="IsVisible" Value="True">
                <Setter Property="HorizontalContentAlignment" Value="{Binding HorizontalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
                <Setter Property="VerticalContentAlignment" Value="{Binding VerticalContentAlignment, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ComboBoxItem.Small" TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource ComboBoxItemBaseStyle}">
        <Setter Property="Padding" Value="6,0"/>
        <Setter Property="MinHeight" Value="20"/>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}" BasedOn="{StaticResource ComboBoxItemBaseStyle}"/>

    <Style TargetType="{x:Type ComboBox}" BasedOn="{StaticResource CV.HandyControl.ComboBox}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemBaseStyle}"/>
    </Style>

    <Style TargetType="{x:Type hc:ComboBox}" BasedOn="{StaticResource CV.HandyControl.ComboBoxPlus}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemBaseStyle}"/>
    </Style>

    <Style x:Key="ComboBox.Small" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource CV.HandyControl.ComboBox.Small}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem.Small}"/>
    </Style>

    <Style x:Key="ComboBoxExtend.Small" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource CV.HandyControl.ComboBoxExtend.Small}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem.Small}"/>
    </Style>

    <Style x:Key="ComboBoxPlus.Small" TargetType="{x:Type hc:ComboBox}" BasedOn="{StaticResource CV.HandyControl.ComboBoxPlus.Small}">
        <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItem.Small}"/>
    </Style>

    <Style x:Key="ComboBoxBaseStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource ComboBox.Small}">
        <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,1"/>
        <Setter Property="MinHeight" Value="24"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>
</ResourceDictionary>
