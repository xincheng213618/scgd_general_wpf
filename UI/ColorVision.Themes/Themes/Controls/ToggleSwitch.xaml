<!--
    This Source Code Form is subject to the terms of the MIT License.
    If a copy of the MIT was not distributed with this file, You can obtain one at https://opensource.org/licenses/MIT.
    Copyright (C) Leszek Pomianowski and WPF UI Contributors.
    All Rights Reserved.

    Based on Microsoft XAML for Win UI.
    Copyright (c) Microsoft Corporation. All Rights Reserved.
-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:ColorVision.Themes.Controls"
    xmlns:system="clr-namespace:System;assembly=System.Runtime">

    <system:Double x:Key="ToggleSwitchWidth">40</system:Double>
    <system:Double x:Key="ToggleSwitchHeight">20</system:Double>
    <Thickness x:Key="ToggleSwitchPadding">0</Thickness>
    <Thickness x:Key="ToggleSwitchBorderThickness">1</Thickness>
    <Thickness x:Key="ToggleSwitchContentMargin">8,0,0,0</Thickness>

    <SolidColorBrush x:Key="ToggleSwitchKnobOffBrush" Color="#737373" />
    <SolidColorBrush x:Key="ToggleSwitchKnobOffPointerOverBrush" Color="#5F5F5F" />
    <SolidColorBrush x:Key="ToggleSwitchKnobOffPressedBrush" Color="#4F4F4F" />
    <SolidColorBrush x:Key="ToggleSwitchKnobOnBrush" Color="White" />
    <SolidColorBrush x:Key="ToggleSwitchDisabledBrush" Color="#B8B8B8" />

    <Style x:Key="DefaultColorVisionToggleSwitchStyle" TargetType="{x:Type controls:ToggleSwitch}">
        <Setter Property="Background" Value="{DynamicResource StatusBarBackgroundBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}" />
        <Setter Property="BorderBrush" Value="Transparent" />
        <Setter Property="BorderThickness" Value="{StaticResource ToggleSwitchBorderThickness}" />
        <Setter Property="Padding" Value="{StaticResource ToggleSwitchPadding}" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="LabelPosition" Value="Right" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="FontSize" Value="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="OverridesDefaultStyle" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ToggleSwitch}">
                    <Grid x:Name="Root" Margin="{TemplateBinding Padding}" Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition x:Name="ToggleColumn" Width="Auto" />
                            <ColumnDefinition x:Name="ContentColumn" Width="*" />
                        </Grid.ColumnDefinitions>

                        <Grid
                            x:Name="ToggleGrid"
                            Grid.Column="0"
                            Width="{StaticResource ToggleSwitchWidth}"
                            Height="{StaticResource ToggleSwitchHeight}">
                            <Rectangle
                                x:Name="ToggleRectangle"
                                Width="{StaticResource ToggleSwitchWidth}"
                                Height="{StaticResource ToggleSwitchHeight}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Fill="{DynamicResource ButtonBackground}"
                                RadiusX="10"
                                RadiusY="10"
                                Stroke="{DynamicResource ButtonBorderBrush}"
                                StrokeThickness="1" />
                            <Rectangle
                                x:Name="ActiveToggleRectangle"
                                Width="{StaticResource ToggleSwitchWidth}"
                                Height="{StaticResource ToggleSwitchHeight}"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Fill="{TemplateBinding Background}"
                                Opacity="0"
                                RadiusX="10"
                                RadiusY="10"
                                StrokeThickness="0" />
                            <Rectangle
                                x:Name="ToggleEllipse"
                                Width="12"
                                Height="12"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Fill="{StaticResource ToggleSwitchKnobOffBrush}"
                                RadiusX="7"
                                RadiusY="7"
                                StrokeThickness="0">
                                <Rectangle.RenderTransform>
                                    <TranslateTransform X="-9" />
                                </Rectangle.RenderTransform>
                            </Rectangle>
                            <Rectangle
                                x:Name="ActiveToggleEllipse"
                                Width="12"
                                Height="12"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Fill="{StaticResource ToggleSwitchKnobOnBrush}"
                                Opacity="0"
                                RadiusX="7"
                                RadiusY="7"
                                StrokeThickness="0">
                                <Rectangle.RenderTransform>
                                    <TranslateTransform X="-9" />
                                </Rectangle.RenderTransform>
                            </Rectangle>
                        </Grid>

                        <ContentPresenter
                            x:Name="ContentPresenter"
                            Grid.Column="1"
                            Margin="{StaticResource ToggleSwitchContentMargin}"
                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                            Content="{TemplateBinding Content}"
                            ContentTemplate="{TemplateBinding ContentTemplate}"
                            RecognizesAccessKey="True"
                            TextElement.FontSize="{TemplateBinding FontSize}"
                            TextElement.FontWeight="{TemplateBinding FontWeight}"
                            TextElement.Foreground="{TemplateBinding Foreground}" />
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="LabelPosition" Value="Left">
                            <Setter TargetName="ToggleGrid" Property="Grid.Column" Value="1" />
                            <Setter TargetName="ContentPresenter" Property="Grid.Column" Value="0" />
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="0,0,8,0" />
                        </Trigger>
                        <Trigger Property="LabelPosition" Value="Right">
                            <Setter TargetName="ToggleGrid" Property="Grid.Column" Value="0" />
                            <Setter TargetName="ContentPresenter" Property="Grid.Column" Value="1" />
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="8,0,0,0" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="{x:Null}" />
                                <Condition Property="OnContent" Value="{x:Null}" />
                                <Condition Property="OffContent" Value="{x:Null}" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="" />
                                <Condition Property="OnContent" Value="" />
                                <Condition Property="OffContent" Value="" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Margin" Value="0" />
                        </MultiTrigger>

                        <Trigger Property="IsChecked" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard Name="SlideRight">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ToggleRectangle" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleRectangle" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ToggleEllipse" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleEllipse" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ToggleEllipse" Storyboard.TargetProperty="(Rectangle.RenderTransform).(TranslateTransform.X)" From="-9" To="9" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleEllipse" Storyboard.TargetProperty="(Rectangle.RenderTransform).(TranslateTransform.X)" From="-9" To="9" Duration="00:00:00.167" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="ToggleRectangle" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleRectangle" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ToggleEllipse" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleEllipse" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ToggleEllipse" Storyboard.TargetProperty="(Rectangle.RenderTransform).(TranslateTransform.X)" From="9" To="-9" Duration="00:00:00.167" />
                                        <DoubleAnimation Storyboard.TargetName="ActiveToggleEllipse" Storyboard.TargetProperty="(Rectangle.RenderTransform).(TranslateTransform.X)" From="9" To="-9" Duration="00:00:00.167" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <SeekStoryboard BeginStoryboardName="SlideRight" Offset="00:00:00.167" />
                        </EventTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="{x:Null}" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Content" Value="{Binding OnContent, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="{x:Null}" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Content" Value="{Binding OffContent, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Content" Value="{Binding OnContent, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="Content" Value="" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ContentPresenter" Property="Content" Value="{Binding OffContent, RelativeSource={RelativeSource TemplatedParent}}" />
                        </MultiTrigger>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleEllipse" Property="Height" Value="14" />
                            <Setter TargetName="ToggleEllipse" Property="Width" Value="14" />
                            <Setter TargetName="ActiveToggleEllipse" Property="Height" Value="14" />
                            <Setter TargetName="ActiveToggleEllipse" Property="Width" Value="14" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="True" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleEllipse" Property="Width" Value="17" />
                            <Setter TargetName="ToggleEllipse" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform X="-7.5" />
                                </Setter.Value>
                            </Setter>
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsPressed" Value="True" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ActiveToggleEllipse" Property="Width" Value="17" />
                            <Setter TargetName="ActiveToggleEllipse" Property="RenderTransform">
                                <Setter.Value>
                                    <TranslateTransform X="7.5" />
                                </Setter.Value>
                            </Setter>
                        </MultiTrigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="False" />
                                <Condition Property="IsChecked" Value="False" />
                                <Condition Property="IsEnabled" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleRectangle" Property="Fill" Value="{DynamicResource GlobalBorderBrush1}" />
                            <Setter TargetName="ToggleRectangle" Property="Stroke" Value="{DynamicResource ButtonBorderBrush}" />
                            <Setter TargetName="ToggleEllipse" Property="Fill" Value="{StaticResource ToggleSwitchKnobOffPointerOverBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                                <Condition Property="IsChecked" Value="False" />
                                <Condition Property="IsEnabled" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ToggleRectangle" Property="Fill" Value="{DynamicResource GlobalBorderBrush}" />
                            <Setter TargetName="ToggleRectangle" Property="Stroke" Value="{DynamicResource ButtonBorderBrush}" />
                            <Setter TargetName="ToggleEllipse" Property="Fill" Value="{StaticResource ToggleSwitchKnobOffPressedBrush}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="False" />
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsEnabled" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" Value="0.9" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True" />
                                <Condition Property="IsPressed" Value="True" />
                                <Condition Property="IsChecked" Value="True" />
                                <Condition Property="IsEnabled" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter Property="Opacity" Value="0.78" />
                        </MultiTrigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="ToggleRectangle" Property="Stroke" Value="{StaticResource ToggleSwitchDisabledBrush}" />
                            <Setter TargetName="ToggleRectangle" Property="Fill" Value="{DynamicResource ButtonBackground}" />
                            <Setter TargetName="ToggleEllipse" Property="Fill" Value="{StaticResource ToggleSwitchDisabledBrush}" />
                            <Setter TargetName="ActiveToggleRectangle" Property="Fill" Value="{StaticResource ToggleSwitchDisabledBrush}" />
                            <Setter TargetName="ActiveToggleEllipse" Property="Fill" Value="White" />
                            <Setter TargetName="Root" Property="Opacity" Value="0.55" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style BasedOn="{StaticResource DefaultColorVisionToggleSwitchStyle}" TargetType="{x:Type controls:ToggleSwitch}" />
</ResourceDictionary>
