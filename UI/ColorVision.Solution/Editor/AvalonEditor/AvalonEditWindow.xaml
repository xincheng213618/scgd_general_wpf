<Window x:Class="ColorVision.Solution.Editor.AvalonEditor.AvalonEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ColorVision.Solution.Editor.AvalonEditor"
        xmlns:workspace="clr-namespace:ColorVision.Solution.Workspace"
        Title="编辑"
        Height="560"
        Width="860"
        MinHeight="320"
        MinWidth="520"
        Background="{DynamicResource GlobalBackground}">
    <Window.CommandBindings>
        <CommandBinding Command="Save"
                        CanExecute="Save_CanExecute"
                        Executed="Save_Executed"/>
        <CommandBinding Command="{x:Static workspace:SolutionDocumentCommands.Reload}"
                        CanExecute="Reload_CanExecute"
                        Executed="Reload_Executed"/>
    </Window.CommandBindings>

    <local:AvalonEditControll x:Name="EditorControl"/>
</Window>
