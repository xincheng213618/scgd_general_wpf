<Window x:Class="ColorVision.Solution.Explorer.AddNewProjectWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:properties="clr-namespace:ColorVision.Solution.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.Sol_AddProject_Title}" Height="450" Width="620"
        Background="{DynamicResource GlobalBackground}"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Category filter -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Text="{x:Static properties:Resources.Sol_AddProject_Category}" DockPanel.Dock="Left" Margin="0,0,8,0" FontWeight="Bold"/>
            <ComboBox x:Name="CategoryComboBox" SelectionChanged="CategoryComboBox_SelectionChanged"
                      Background="{DynamicResource GlobalBackground}"/>
        </DockPanel>

        <!-- Search bar -->
        <hc:SearchBar x:Name="SearchBar" Grid.Row="1" Margin="0,0,0,8"
                      hc:InfoElement.Placeholder="{x:Static properties:Resources.Sol_AddProject_Search}"
                      hc:InfoElement.ShowClearButton="True"
                      Style="{StaticResource SearchBarPlus}"
                      IsRealTime="True"
                      TextChanged="SearchBar_TextChanged"/>

        <!-- Template list -->
        <ListView x:Name="TemplateListView" Grid.Row="2" Margin="0,0,0,10"
                  Background="Transparent"
                  SelectionChanged="TemplateListView_SelectionChanged"
                  MouseDoubleClick="TemplateListView_MouseDoubleClick">
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemBaseStyle.Small}">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridViewColumnHeaderBase}">
                            <Setter Property="Background" Value="Transparent"/>
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn Header="{x:Static properties:Resources.Sol_AddProject_Name}" Width="200">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static properties:Resources.Sol_AddProject_CategoryCol}" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Category}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static properties:Resources.Sol_AddProject_Desc}" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Description}" TextTrimming="CharacterEllipsis"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Description panel -->
        <TextBlock x:Name="DescriptionText" Grid.Row="3" Margin="0,0,0,10"
                   TextWrapping="Wrap" FontStyle="Italic"
                   Foreground="{DynamicResource SecondaryTextBrush}"/>

        <!-- Name input -->
        <DockPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="{x:Static properties:Resources.Sol_AddProject_ProjectName}" DockPanel.Dock="Left" Margin="0,0,8,0" FontWeight="Bold"/>
            <TextBox x:Name="ProjectNameTextBox" Padding="4,3"
                     Foreground="{DynamicResource GlobalTextBrush}"
                     Background="{DynamicResource GlobalBackground}"
                     KeyDown="ProjectNameTextBox_KeyDown"/>
        </DockPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="AddButton" Content="{x:Static properties:Resources.Sol_AddProject_Create}" Width="80" Height="30" Margin="0,0,8,0"
                    Click="AddButton_Click" IsDefault="True"/>
            <Button Content="{x:Static properties:Resources.Sol_AddProject_Cancel}" Width="80" Height="30"
                    Click="CancelButton_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
