<Window x:Class="ColorVision.Solution.Download.AddDownloadDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:properties="clr-namespace:ColorVision.Solution.Properties"
        Title="{x:Static properties:Resources.AddDownload}" Height="280" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{DynamicResource GlobalBackground}">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        </Style>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.DownloadUrl}" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox x:Name="UrlTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Text="{x:Static properties:Resources.SaveDirectory}" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <Grid Grid.Row="1" Grid.Column="1" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="SaveDirTextBox"/>
            <Button Grid.Column="1" Content="..." Width="30" Margin="5,0,0,0" Click="BrowseDir_Click"/>
        </Grid>

        <TextBlock Grid.Row="2" Text="{x:Static properties:Resources.UserName}" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <TextBox x:Name="UserNameTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="3" Text="{x:Static properties:Resources.Password}" VerticalAlignment="Center" Margin="0,0,10,8"/>
        <PasswordBox x:Name="PasswordTextBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static properties:Resources.OK}" Width="80" Margin="0,0,10,0" Click="OK_Click" IsDefault="True"/>
            <Button Content="{x:Static properties:Resources.Cancel}" Width="80" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
