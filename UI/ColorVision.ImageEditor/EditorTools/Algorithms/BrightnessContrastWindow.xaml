<Window x:Class="ColorVision.ImageEditor.EditorTools.Algorithms.BrightnessContrastWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="亮度对比度调整" Height="250" Width="500" 
        Background="{DynamicResource GlobalBackground}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="亮度对比度调整" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="亮度" VerticalAlignment="Center" Margin="0,0,10,0" MinWidth="60"/>
            <TextBox DockPanel.Dock="Right" Text="{Binding ElementName=BrightnessSlider, Path=Value, StringFormat=F0}" 
                     Width="80" Margin="5,0,0,0"/>
            <hc:PreviewSlider x:Name="BrightnessSlider" 
                              TickFrequency="1" 
                              IsSnapToTickEnabled="True" 
                              SmallChange="1" 
                              LargeChange="10" 
                              Value="0" 
                              Minimum="-100" 
                              Maximum="150" 
                              ValueChanged="BrightnessSlider_ValueChanged"/>
        </DockPanel>

        <DockPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="对比度" VerticalAlignment="Center" Margin="0,0,10,0" MinWidth="60"/>
            <TextBox DockPanel.Dock="Right" Text="{Binding ElementName=ContrastSlider, Path=Value, StringFormat=F0}" 
                     Width="80" Margin="5,0,0,0"/>
            <hc:PreviewSlider x:Name="ContrastSlider" 
                              TickFrequency="1" 
                              IsSnapToTickEnabled="True" 
                              SmallChange="1" 
                              LargeChange="10" 
                              Value="0" 
                              Minimum="-50" 
                              Maximum="100" 
                              ValueChanged="ContrastSlider_ValueChanged"/>
        </DockPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="应用" Width="80" Margin="0,0,10,0" Click="Apply_Click"/>
            <Button Content="取消" Width="80" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
