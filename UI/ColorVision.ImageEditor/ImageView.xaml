<UserControl x:Class="ColorVision.ImageEditor.ImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorVision.ImageEditor"
             xmlns:converter="clr-namespace:ColorVision.ImageEditor.Draw.Converter"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:ImageViewModel}"
             d:DesignHeight="600" d:DesignWidth="1000" Initialized="UserControl_Initialized" AllowDrop="True">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource TextBox.Small}">
            <EventSetter Event="PreviewKeyDown" Handler="TextBox_PreviewKeyDown"/>
        </Style>
        <GeometryDrawing x:Key="GridSquare"
                 Brush="{DynamicResource GlobalTextBrush}"
                 Geometry="M0,0 L0,1 1,1 1,0 Z" />
        <DrawingBrush x:Key="TransparentGridBrush"
              TileMode="Tile"
              ViewportUnits="Absolute"
              Viewport="0 0 20 20"
              Opacity="0.1">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <!-- Gray square -->
                    <GeometryDrawing Brush="Gray" Geometry="M0,0 L0,10 10,10 10,0 Z" />
                    <!-- Transparent square -->
                    <GeometryDrawing Brush="Transparent" Geometry="M10,0 L10,10 20,10 20,0 Z" />
                    <!-- Another row -->
                    <GeometryDrawing Brush="Transparent" Geometry="M0,10 L0,20 10,20 10,10 Z" />
                    <GeometryDrawing Brush="Gray" Geometry="M10,10 L10,20 20,20 20,10 Z" />
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
        <GeometryDrawing x:Key="SmallGridGeometry"
                         Geometry="M0,0 L0,1 0.03,1 0.03,0.03 1,0.03 1,0 Z"
                         Brush="{DynamicResource GlobalTextBrush}" />

        <GeometryDrawing x:Key="LargeGridGeometry"
                         Geometry="M0,0 L0,1 0.015,1 0.015,0.015 1,0.015 1,0 Z"
                         Brush="{DynamicResource GlobalTextBrush}" />

        <DrawingBrush x:Key="SmallGridLinesDrawingBrush"
                      TileMode="Tile"
                      ViewportUnits="Absolute"
                      Viewport="0 0 20 20"
                      Drawing="{StaticResource SmallGridGeometry}" Opacity="0.1" />

        <DrawingBrush x:Key="LargeGridLinesDrawingBrush"
                      TileMode="Tile"
                      ViewportUnits="Absolute"
                      Opacity="0.5"
                      Viewport="0 0 200 200"
                      Drawing="{StaticResource LargeGridGeometry}" />
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="ImageContentGrid" Grid.Row="1" >
            <Grid Background="{StaticResource LargeGridLinesDrawingBrush}" Visibility="Collapsed"/>
            <Grid Background="{StaticResource SmallGridLinesDrawingBrush}"  Visibility="Collapsed"/>

            <Grid x:Name="ZoomGrid" Background="{StaticResource TransparentGridBrush}">
                <local:Zoombox x:Name="Zoombox1"  x:FieldModifier="public"  MaxZoom="{Binding MaxZoom}"  MinZoom="{Binding MinZoom}" Grid.ColumnSpan="2">
                    <local:DrawCanvas x:Name="ImageShow"  x:FieldModifier="public" FocusVisualStyle="{x:Null}"   RenderOptions.BitmapScalingMode="HighQuality" RenderOptions.ClearTypeHint="Auto">
                    </local:DrawCanvas>

                </local:Zoombox>

                <ComboBox x:Name="ComboBoxLayers" HorizontalAlignment="Left" BorderThickness="0" VerticalAlignment="Top"  Style="{StaticResource ComboBox.Small}" x:FieldModifier="public"  SelectedIndex="0"  Grid.Column="1"/>
                <ToolBar x:Name="ToolBarAl"  x:FieldModifier="public" Style="{StaticResource ToolBarBaseStyle}" Background="Transparent" ToolBarTray.IsLocked="True"  VerticalAlignment="Bottom" Grid.Row="0"  HorizontalAlignment="Left" Visibility="Visible">
                    <ComboBox x:Name="ComboxPOITemplate"  x:FieldModifier="public" Style="{StaticResource ComboBox.Small}" Width="100" HorizontalAlignment="Left" SelectedValuePath="Value"  DisplayMemberPath="Key"/>
                </ToolBar>

                <ToolBarTray x:Name="ToolBarDraw" Visibility="{Binding ImageEditMode,Converter={StaticResource bool2VisibilityConverter}}" HorizontalAlignment="Center" VerticalAlignment="Bottom" Grid.Column="1"  >
                    <ToolBar ToolBarTray.IsLocked="True"  >
                    </ToolBar>

                </ToolBarTray>

                <ToolBarTray x:Name="ToolBarTop" VerticalAlignment="Top" Grid.Row="0"  HorizontalAlignment="Center" Grid.Column="1">
                    <ToolBar  ToolBarTray.IsLocked="True" Margin="0,2,4,0" >
                        <ToggleButton IsChecked="{Binding ImageEditMode}"  Margin="1,0">
                            <Image Style="{StaticResource ToolBarImage}" Source="{DynamicResource DrawingImagedrag}" Margin="-5"/>
                        </ToggleButton>
                    </ToolBar>
                </ToolBarTray>

                <ToolBarTray x:Name="ToolBarLeft" HorizontalAlignment="Left" Orientation="Vertical" Margin="2,50,0,0"  VerticalAlignment="Top">
                    <ToolBar ToolBarTray.IsLocked="True" Padding="2,3"  >
                    </ToolBar>
                </ToolBarTray>

                <ToolBarTray x:Name="ToolBarRight" Orientation="Vertical" Margin="0,50,2,0" Grid.Row="0" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Column="1">
                    <ToolBar ToolBarTray.IsLocked="True" Padding="2,3" >
                        <ToggleButton x:Name="AdvancedButton" Margin="0,0,0,2"  Padding="5">
                            <Image Style="{StaticResource ToolBarImage}"  Source="{DynamicResource DIExpand}" />
                        </ToggleButton>
                        <Button x:Name="Button1931" x:FieldModifier="public" Margin="0,0,0,2" Padding="5" >
                            <Image Style="{StaticResource ToolBarImage}"  Source="/ColorVision.ImageEditor;component/Assets/Image/CIE1931xy1.png"/>
                        </Button>
                        <StackPanel>
                            <ToggleButton x:Name="Pseudo"  Click="ToggleButton_Click"  IsChecked="{Binding Config.IsPseudo}" Margin="0,0,0,2" >
                                <Image  Style="{StaticResource ToolBarImage}"   Source="/ColorVision.ImageEditor;component/Assets/png/PowerToy.png" Margin="-5"/>
                            </ToggleButton>
                            <Grid  Height="200"  Visibility="{Binding Config.IsPseudo,Converter={StaticResource bool2VisibilityConverter}}">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <TextBox Grid.Row="0" Width="28" FontSize="8" Text="{Binding ValueEnd, ElementName=PseudoSlider, StringFormat=#0,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource BaseStyle}"  Background="Transparent"  BorderThickness="0"  HorizontalContentAlignment="Center" />
                                <TextBox Grid.Row="2" Width="28" FontSize="8" Text="{Binding ValueStart, ElementName=PseudoSlider, StringFormat=#0,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource BaseStyle}" Background="Transparent"  BorderThickness="0" HorizontalContentAlignment="Center"/>
                                <Grid Grid.Row="1"   Width="18">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="0"  x:Name="RowDefinitionStart"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="0"  x:Name="RowDefinitionEnd"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="White" CornerRadius="10,10,0,0"/>
                                    <Border Grid.Row="1"  Background="Transparent">
                                        <Image x:Name="ColormapTypesImage" Source="/ColorVision.ImageEditor;component/Assets/Image/pictureBox1.Image.png" Stretch="Fill" VerticalAlignment="Top"/>
                                    </Border>
                                    <Border Grid.Row="2" Background="Black" CornerRadius="0,0,10,10 "/>
                                </Grid>
                                <hc:RangeSlider Grid.Row="1"  x:Name="PseudoSlider" x:FieldModifier="public" FocusVisualStyle="{x:Null}" ValueChanged="PseudoSlider_ValueChanged"  Style="{StaticResource RangeSliderBaseStyle}" Background="Transparent" hc:TipElement.Visibility="Visible" hc:TipElement.Placement="Left" hc:TipElement.StringFormat="#0" ValueEnd="255"  Maximum="255" ValueStart="0"  Minimum="0" Orientation="Vertical" HorizontalAlignment="Center" />
                            </Grid>
                        </StackPanel>
                    </ToolBar>
                </ToolBarTray>
            </Grid>
        </Grid>

        <Grid Grid.Column="1" Width="300" Visibility="{Binding ElementName=AdvancedButton, Path=IsChecked,Converter={StaticResource bool2VisibilityConverter}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ScrollViewer Margin="5,0">
                <StackPanel x:Name="AdvancedStackPanel">

                    <Border Grid.Column="1" Background="{DynamicResource GlobalBorderBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"  Margin="0,5" CornerRadius="5">
                        <StackPanel Margin="5">
                            <DockPanel Margin="0,0,0,5">
                                <ui:ToggleSwitch IsChecked="{Binding Config.IsLayoutUpdated}" DockPanel.Dock="Right" />
                                <TextBlock Text="自动刷新" Width="100" HorizontalAlignment="Left"/>
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <ui:ToggleSwitch IsChecked="{Binding Config.IsShowText}" DockPanel.Dock="Right" />
                                <TextBlock Text="显示文字" Width="100" HorizontalAlignment="Left"/>
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <ui:ToggleSwitch IsChecked="{Binding Config.IsShowMsg}" DockPanel.Dock="Right" />
                                <TextBlock Text="显示消息" Width="100" HorizontalAlignment="Left"/>
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <TextBlock Text="MaxZoom" Width="100"/>
                                <TextBox  Text="{Binding MaxZoom}"/>
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <TextBlock Text="MinZoom" Width="100"/>
                                <TextBox  Text="{Binding MinZoom}" />
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <TextBlock Text="伪彩色" Width="100"/>
                                <ComboBox x:Name="ComColormapTypes" SelectedValuePath="Value" Style="{StaticResource ComboBox.Small}" Text="{Binding  Config.ColormapTypes}" DisplayMemberPath="Key"/>
                            </DockPanel>
                        </StackPanel>
                    </Border>
                    <Border Grid.Column="1" Background="{DynamicResource GlobalBorderBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"  Margin="0,5" CornerRadius="5">
                        <StackPanel Margin="5">
                            <DockPanel Margin="0,5,0,5">
                                <TextBlock Text="CVICE范围" Width="100"/>
                                <TextBox  Text="{Binding MouseMagnifier.Radius}"/>
                            </DockPanel>
                            <DockPanel Margin="0,0,0,5">
                                <TextBlock Text="CVICE Type:" Width="100"/>
                                <hc:ComboBox x:Name="ComboxeType"  SelectedValue="{Binding MouseMagnifier.MagnigifierType}"  Style="{StaticResource ComboBox.Small}"  SelectedValuePath="Key"  DisplayMemberPath="Value"  />
                            </DockPanel>
                            <TextBlock Text="设置为圆的时候，输入的值是半径" HorizontalAlignment="Right"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
            <StackPanel VerticalAlignment="Bottom" Grid.Row="1" Margin="5"  Orientation="Horizontal">
                <Button Content="应用当前图片" Click="Apply_Click"  Margin="0,0,10,0"/>
                <Button Content="重新加载" Click="Reload_Click" />
            </StackPanel>

        </Grid>
    </Grid>
</UserControl>
