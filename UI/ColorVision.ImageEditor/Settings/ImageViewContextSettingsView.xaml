<UserControl x:Class="ColorVision.ImageEditor.Settings.ImageViewContextSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:properties="clr-namespace:ColorVision.ImageEditor.Properties"
             Foreground="{DynamicResource GlobalTextBrush}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="Transparent" TextElement.Foreground="{DynamicResource GlobalTextBrush}">
        <Border Background="{DynamicResource ButtonBackground}"
                BorderBrush="{DynamicResource ButtonBorderBrush}"
                BorderThickness="1"
                Padding="12"
                Visibility="{Binding IsEmpty, Converter={StaticResource BooleanToVisibilityConverter}}">
            <TextBlock Text="无数据" Opacity="0.7" Foreground="{DynamicResource GlobalTextBrush}"/>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      Background="Transparent"
                      Foreground="{DynamicResource GlobalTextBrush}"
                      Visibility="{Binding HasEntries, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding Sections}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,14">
                            <DockPanel Margin="0,0,0,5">
                                <TextBlock Text="{Binding ScopeName}" FontWeight="SemiBold" Foreground="{DynamicResource GlobalTextBrush}"/>
                                <TextBlock DockPanel.Dock="Right" Text="{Binding CountText}" Opacity="0.65" Foreground="{DynamicResource GlobalTextBrush}"/>
                            </DockPanel>

                            <Border Background="{DynamicResource ButtonBackground}"
                                    BorderBrush="{DynamicResource ButtonBorderBrush}"
                                    BorderThickness="1">
                                <ItemsControl ItemsSource="{Binding Entries}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8,5">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="180"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding Key}" FontWeight="SemiBold" Margin="0,0,16,0" Foreground="{DynamicResource GlobalTextBrush}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Value}" TextWrapping="Wrap" FontFamily="Consolas" Foreground="{DynamicResource GlobalTextBrush}"/>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Border>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
