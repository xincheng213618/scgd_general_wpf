<Window x:Class="ColorVision.ImageEditor.Settings.ImageViewSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="图像设置"
        Width="760"
        Height="560"
        MinWidth="640"
        MinHeight="480"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TabControl x:Name="SettingsTabControl" Grid.Row="0" />

        <StackPanel Grid.Row="1" Margin="0,12,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button MinWidth="96" Margin="0,0,8,0" Click="Save_Click" Content="保存" />
            <Button MinWidth="96" Click="Close_Click" Content="关闭" />
        </StackPanel>
    </Grid>
</Window>