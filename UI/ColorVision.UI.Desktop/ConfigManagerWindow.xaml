<Window x:Class="ColorVision.UI.Desktop.ConfigManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ColorVision.UI.Desktop"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.UI.Desktop.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.ConfigurationManagement}"
        Width="1200" Height="600"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Top toolbar  -->
        <Grid Grid.Row="0" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!--  Search box on the left  -->
            <Grid x:Name="SearchGrid" Grid.Column="0" Margin="0,0,5,0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <hc:TextBox x:Name="Searchbox" Padding="25,5,5,5" VerticalContentAlignment="Center" hc:InfoElement.Placeholder="{x:Static properties:Resources.Search}" BorderBrush="#CCCEDB" BorderThickness="1" Style="{DynamicResource TextBoxPlusBaseStyle}" TextChanged="Searchbox_TextChanged" />
                <Viewbox Width="14" Height="14" Margin="5,0,0,0" HorizontalAlignment="Left">
                    <Rectangle Width="16" Height="16">
                        <Rectangle.Resources>
                            <SolidColorBrush x:Key="canvas" Opacity="0" />
                            <SolidColorBrush x:Key="light-defaultgrey-10" Opacity="0.1" Color="{DynamicResource defaultgreyColor}" />
                            <SolidColorBrush x:Key="light-defaultgrey" Opacity="1" Color="{DynamicResource defaultgreyColor}" />
                        </Rectangle.Resources>
                        <Rectangle.Fill>
                            <DrawingBrush Stretch="None">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <DrawingGroup x:Name="canvas">
                                            <GeometryDrawing Brush="{DynamicResource canvas}" Geometry="F1M0,0H16V16H0Z" />
                                        </DrawingGroup>
                                        <DrawingGroup x:Name="level_1">
                                            <GeometryDrawing Brush="{DynamicResource light-defaultgrey-10}" Geometry="F1M11.5,6.5a5,5,0,1,1-5-5A5,5,0,0,1,11.5,6.5Z" />
                                            <GeometryDrawing Brush="{DynamicResource light-defaultgrey}" Geometry="F1M1,6.5a5.5,5.5,0,0,0,9.017,4.224l5.129,5.13.708-.708-5.13-5.129A5.5,5.5,0,1,0,1,6.5Zm1,0A4.5,4.5,0,1,1,6.5,11,4.505,4.505,0,0,1,2,6.5Z" />
                                        </DrawingGroup>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Viewbox>
            </Grid>


            <!--  Column count selector in the middle  -->
            <StackPanel Grid.Column="1" Margin="0,0,5,0" Orientation="Horizontal">
                <TextBlock Margin="0,0,5,0" VerticalAlignment="Center" Foreground="{DynamicResource PrimaryTextBrush}" Text="列数:" />
                <ComboBox x:Name="ColumnCountComboBox" Width="60" SelectedIndex="2" SelectionChanged="ColumnCountComboBox_SelectionChanged" Style="{DynamicResource ComboBox.Small}">
                    <ComboBoxItem Content="1" />
                    <ComboBoxItem Content="2" />
                    <ComboBoxItem Content="3" />
                    <ComboBoxItem Content="4" />
                    <ComboBoxItem Content="5" />
                    <ComboBoxItem Content="6" />
                </ComboBox>
            </StackPanel>

            <!--  Action buttons on the right  -->
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Margin="0,0,5,0" Padding="8,5" Click="Save_Click" Content="{x:Static properties:Resources.SaveConfiguration}" />
                <Button Padding="8,5" Click="OpenFolder_Click" Content="{x:Static properties:Resources.OpenConfigFolder}" />
            </StackPanel>
        </Grid>

        <!--  Main content area: Left Assembly List + Middle Config Grid + Right PropertyGrid  -->
        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="150" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="*" MinWidth="350" />
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="350" MinWidth="300" />
            </Grid.ColumnDefinitions>

            <!--  Left: Assembly Index List  -->
            <Border Grid.Column="0" Padding="5" Style="{StaticResource BorderModuleArea}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Margin="5,0,0,5" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Text="程序集" />
                    <ListView x:Name="AssemblyListView" Grid.Row="1" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto" SelectionChanged="AssemblyListView_SelectionChanged">
                        <ListView.ItemContainerStyle>
                            <Style BasedOn="{StaticResource ListViewItemBaseStyle.Small}" TargetType="ListViewItem">
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Margin" Value="0,2" />
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.View>
                            <GridView>
                                <GridView.ColumnHeaderContainerStyle>
                                    <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Style>
                                </GridView.ColumnHeaderContainerStyle>
                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate DataType="{x:Type local:AssemblyGroup}">
                                            <TextBlock FontSize="12" Foreground="{DynamicResource PrimaryTextBrush}" Text="{Binding DisplayName}" TextWrapping="Wrap" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>

                        </ListView.View>
                    </ListView>
                </Grid>
            </Border>

            <!--  First Splitter  -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" />

            <!--  Middle: Config Grid (ScrollViewer with dynamic content)  -->
            <Border Grid.Column="2" Padding="5" Style="{StaticResource BorderModuleArea}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Margin="5,0,0,5" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Text="配置列表" />
                    <ScrollViewer x:Name="ConfigScrollViewer" Grid.Row="1" Background="Transparent" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ConfigContainer" Margin="5" />
                    </ScrollViewer>
                </Grid>
            </Border>

            <!--  Second Splitter  -->
            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}" ResizeBehavior="PreviousAndNext" ResizeDirection="Columns" />

            <!--  Right: PropertyGrid  -->
            <Border Grid.Column="4" Padding="5" Style="{StaticResource BorderModuleArea}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock x:Name="PropertyTitle" Grid.Row="0" Margin="5,0,0,5" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}" Text="选择一个配置查看详情" />
                    <Border x:Name="PropertyContainer" Grid.Row="1" Background="{DynamicResource GlobalBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3">
                        <!--  PropertyGrid will be loaded here dynamically  -->
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!--  Bottom status bar  -->
        <DockPanel Grid.Row="2" Margin="10,0,10,5">
            <TextBlock x:Name="SummaryText" Foreground="{DynamicResource PrimaryTextBrush}" Text="" />
            <TextBlock x:Name="SummaryText1" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryTextBrush}" Text="" />
        </DockPanel>
    </Grid>
</Window>
