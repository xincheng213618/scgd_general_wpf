<Window x:Class="ColorVision.UI.Desktop.Feedback.FeedbackWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.UI.Desktop.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.SendFeedback}" 
        Background="{DynamicResource GlobalBackground}" 
        Height="560" Width="520" 
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Initialized="Window_Initialized">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="{x:Static properties:Resources.SendFeedback}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Message area -->
        <TextBox x:Name="MessageTextBox" Grid.Row="1" 
                 AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Margin="0,0,0,10" MinHeight="100"
                 Text="{x:Static properties:Resources.FeedbackPlaceholder}"
                 GotFocus="MessageTextBox_GotFocus"/>

        <!-- Attached files list -->
        <Grid Grid.Row="2" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="{x:Static properties:Resources.Attachments}" VerticalAlignment="Center" FontWeight="SemiBold"/>
            <Button x:Name="AddFileButton" Grid.Column="1" Content="{x:Static properties:Resources.AddFile}" Click="AddFile_Click" Margin="5,0" Padding="8,3"/>
            <Button x:Name="AddScreenshotButton" Grid.Column="2" Content="{x:Static properties:Resources.CaptureScreenshot}" Click="CaptureScreenshot_Click" Margin="0,0,5,0" Padding="8,3"/>
            <Button x:Name="PackLogsButton" Grid.Column="3" Content="{x:Static properties:Resources.PackLogs}" Click="PackLogs_Click" Padding="8,3"/>
        </Grid>

        <ListBox x:Name="AttachmentsList" Grid.Row="3" MaxHeight="120" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding FileName}" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text="{Binding SizeText}" Foreground="Gray" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <Button Content="✕" Click="RemoveAttachment_Click" Tag="{Binding}" Padding="3,0" FontSize="10" Background="Transparent" BorderThickness="0" Cursor="Hand"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Status -->
        <TextBlock x:Name="StatusText" Grid.Row="4" Foreground="Gray" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{x:Static properties:Resources.SendFeedback}" x:Name="SendButton" Click="Send_Click" Padding="15,5" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="{x:Static properties:Resources.Cancel}" Click="Cancel_Click" Padding="15,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
