<Window x:Class="ColorVision.UI.Desktop.TimedButtons.TimedButtonOperationStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.UI.Desktop.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.Stats_Title}"
        Height="640"
        Width="1080"
        MinHeight="480"
        MinWidth="820"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized"
        Closed="Window_Closed">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBarTray IsLocked="True">
            <ToolBar Padding="3,2">
                <TextBlock Text="{x:Static properties:Resources.Stats_OperationKey}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox x:Name="SearchTextBox" Width="280" VerticalAlignment="Center" KeyDown="SearchTextBox_KeyDown"/>
                <TextBlock Text="{x:Static properties:Resources.Stats_Start}" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <DatePicker x:Name="StartDatePicker" Width="130" VerticalAlignment="Center"/>
                <TextBlock Text="{x:Static properties:Resources.Stats_End}" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <DatePicker x:Name="EndDatePicker" Width="130" VerticalAlignment="Center"/>
                <Button Content="{x:Static properties:Resources.Stats_Query}" Click="Query_Click" Margin="6,0,0,0"/>
                <Button Content="{x:Static properties:Resources.Stats_Refresh}" Click="Refresh_Click" Margin="6,0,0,0"/>
                <Button Content="{x:Static properties:Resources.Stats_ClearFilter}" Click="ClearFilter_Click" Margin="6,0,0,0"/>
                <Separator Margin="8,0"/>
                <Button x:Name="ExportButton" Content="{x:Static properties:Resources.Stats_ExportCsv}" Click="ExportCsv_Click" Margin="0,0,0,0" IsEnabled="False"/>
                <Button x:Name="DeleteSelectedButton" Content="{x:Static properties:Resources.Stats_DeleteSelected}" Click="DeleteSelected_Click" Margin="0,0,0,0" IsEnabled="False"/>
                <Button x:Name="ClearButton" Content="{x:Static properties:Resources.Stats_ClearAll}" Click="ClearButton_Click" Margin="6,0,0,0" IsEnabled="False"/>
            </ToolBar>
        </ToolBarTray>

        <Border Grid.Row="1" Margin="0,8,0,8" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock x:Name="SummaryText" DockPanel.Dock="Left" FontWeight="SemiBold"/>
                <TextBlock DockPanel.Dock="Right" Text="{x:Static properties:Resources.Stats_DeleteNote}" Opacity="0.72"/>
            </DockPanel>
        </Border>

        <DataGrid x:Name="StatsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  Background="Transparent"
                  BorderThickness="1"
                  SelectionChanged="StatsDataGrid_SelectionChanged">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="{x:Static properties:Resources.Stats_DeleteSelected}" Click="DeleteSelected_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_OperationKey}" Binding="{Binding OperationKey}" Width="2*"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_Warmup}" Binding="{Binding WarmupElapsedText}" Width="100"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_Last}" Binding="{Binding LastElapsedText}" Width="100"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_Average}" Binding="{Binding AverageElapsedText}" Width="100"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_Fastest}" Binding="{Binding BestElapsedText}" Width="100"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_Slowest}" Binding="{Binding WorstElapsedText}" Width="100"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_StableSamples}" Binding="{Binding SuccessCount}" Width="90"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_WarmupSamples}" Binding="{Binding WarmupCount}" Width="90"/>
                <DataGridTextColumn Header="{x:Static properties:Resources.Stats_LastCompleted}" Binding="{Binding LastCompletedAtText}" Width="170"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>