<Window x:Class="ColorVision.UI.Desktop.TimedButtons.TimedButtonOperationStatsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="按钮耗时统计"
        Height="640"
        Width="1080"
        MinHeight="480"
        MinWidth="820"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized"
        Closed="Window_Closed">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ToolBarTray IsLocked="True">
            <ToolBar Padding="3,2">
                <TextBlock Text="操作键" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox x:Name="SearchTextBox" Width="280" VerticalAlignment="Center" KeyDown="SearchTextBox_KeyDown"/>
                <TextBlock Text="开始" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <DatePicker x:Name="StartDatePicker" Width="130" VerticalAlignment="Center"/>
                <TextBlock Text="结束" VerticalAlignment="Center" Margin="10,0,6,0"/>
                <DatePicker x:Name="EndDatePicker" Width="130" VerticalAlignment="Center"/>
                <Button Content="查询" Click="Query_Click" Margin="6,0,0,0"/>
                <Button Content="刷新" Click="Refresh_Click" Margin="6,0,0,0"/>
                <Button Content="清除筛选" Click="ClearFilter_Click" Margin="6,0,0,0"/>
                <Separator Margin="8,0"/>
                <Button x:Name="ExportButton" Content="导出 CSV" Click="ExportCsv_Click" Margin="0,0,0,0" IsEnabled="False"/>
                <Button x:Name="DeleteSelectedButton" Content="删除选中" Click="DeleteSelected_Click" Margin="0,0,0,0" IsEnabled="False"/>
                <Button x:Name="ClearButton" Content="清空全部" Click="ClearButton_Click" Margin="6,0,0,0" IsEnabled="False"/>
            </ToolBar>
        </ToolBarTray>

        <Border Grid.Row="1" Margin="0,8,0,8" Padding="10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
            <DockPanel>
                <TextBlock x:Name="SummaryText" DockPanel.Dock="Left" FontWeight="SemiBold"/>
                <TextBlock DockPanel.Dock="Right" Text="删除或清空后，按钮提示会立即刷新。" Opacity="0.72"/>
            </DockPanel>
        </Border>

        <DataGrid x:Name="StatsDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  Background="Transparent"
                  BorderThickness="1"
                  SelectionChanged="StatsDataGrid_SelectionChanged">
            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="删除选中" Click="DeleteSelected_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>
            <DataGrid.Columns>
                <DataGridTextColumn Header="操作键" Binding="{Binding OperationKey}" Width="2*"/>
                <DataGridTextColumn Header="预热" Binding="{Binding WarmupElapsedText}" Width="100"/>
                <DataGridTextColumn Header="上次" Binding="{Binding LastElapsedText}" Width="100"/>
                <DataGridTextColumn Header="平均" Binding="{Binding AverageElapsedText}" Width="100"/>
                <DataGridTextColumn Header="最快" Binding="{Binding BestElapsedText}" Width="100"/>
                <DataGridTextColumn Header="最慢" Binding="{Binding WorstElapsedText}" Width="100"/>
                <DataGridTextColumn Header="稳定样本" Binding="{Binding SuccessCount}" Width="90"/>
                <DataGridTextColumn Header="预热样本" Binding="{Binding WarmupCount}" Width="90"/>
                <DataGridTextColumn Header="最近完成" Binding="{Binding LastCompletedAtText}" Width="170"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>