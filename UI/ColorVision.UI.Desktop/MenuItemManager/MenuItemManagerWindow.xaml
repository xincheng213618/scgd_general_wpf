<Window x:Class="ColorVision.UI.Desktop.MenuItemManager.MenuItemManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        Title="Menu Manager" Height="680" Width="1180"
        Background="{DynamicResource GlobalBackground}" 
        UseLayoutRounding="True"
        Initialized="Window_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8,8,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <hc:TextBox x:Name="SearchBox" Grid.Column="0" Margin="0,0,8,0"
                       Style="{DynamicResource TextBoxPlusBaseStyle}" 
                       hc:InfoElement.Placeholder="Search menu names or paths..."
                       VerticalContentAlignment="Center" 
                       TextChanged="SearchBox_TextChanged"/>

            <Button Grid.Column="1" Content="Apply" Click="Apply_Click" Margin="0,0,6,0" Padding="14,6" IsDefault="True"/>
            <Button Grid.Column="2" Content="Reset" Click="Reset_Click" Padding="10,5" Opacity="0.66"/>
        </Grid>

        <Grid Grid.Row="1" Margin="8,0,8,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" MinWidth="180"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*" MinWidth="440"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="340" MinWidth="260"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource BorderModuleArea}" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Menu Structure" FontSize="14" FontWeight="Bold" Margin="5,0,0,5" 
                              Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <TreeView x:Name="MenuTreeView" Grid.Row="1" Background="Transparent" BorderThickness="0"
                             AllowDrop="True"
                             PreviewMouseLeftButtonDown="DragSource_PreviewMouseLeftButtonDown"
                             MouseMove="DragSource_MouseMove"
                             DragOver="MenuTreeView_DragOver"
                             Drop="MenuTreeView_Drop"
                             ToolTip="Drag a menu item here to move it under a menu."
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <TreeView.ItemContainerStyle>
                            <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                                <Setter Property="IsExpanded" Value="False"/>
                                <Setter Property="Padding" Value="2"/>
                            </Style>
                        </TreeView.ItemContainerStyle>
                    </TreeView>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}" 
                         ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <Border Grid.Column="2" Style="{StaticResource BorderModuleArea}" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="ListTitle" Text="Child Menu Items" FontSize="14" FontWeight="Bold" Margin="5,0,0,5" 
                              Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <DataGrid x:Name="MenuItemDataGrid" Grid.Row="1" 
                             AutoGenerateColumns="False"
                             CanUserAddRows="False" CanUserDeleteRows="False"
                             CanUserReorderColumns="False"
                             SelectionMode="Single"
                             SelectionUnit="FullRow"
                             SelectionChanged="MenuItemDataGrid_SelectionChanged"
                             CellEditEnding="MenuItemDataGrid_CellEditEnding"
                             AllowDrop="True"
                             PreviewMouseLeftButtonDown="DragSource_PreviewMouseLeftButtonDown"
                             MouseMove="DragSource_MouseMove"
                             DragOver="MenuItemDataGrid_DragOver"
                             Drop="MenuItemDataGrid_Drop"
                             ToolTip="Drag rows to reorder them, or drop a menu item here to append it."
                             Background="Transparent" BorderThickness="0"
                             GridLinesVisibility="Horizontal"
                             HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                             RowHeaderWidth="0"
                             HeadersVisibility="Column"
                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                             ScrollViewer.VerticalScrollBarVisibility="Auto">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsVisible}" Value="False">
                                        <Setter Property="Foreground" Value="{DynamicResource SecondaryTextBrush}"/>
                                        <Setter Property="Opacity" Value="0.72"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTemplateColumn Header="Visible" Width="64">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <CheckBox IsChecked="{Binding IsVisible, UpdateSourceTrigger=PropertyChanged}"
                                                  HorizontalAlignment="Center"
                                                  Click="VisibleCheckBox_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="Header" Width="160" Binding="{Binding Header}" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Current Path" Width="*" MinWidth="220" Binding="{Binding CurrentPath}" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}" 
                         ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <Border Grid.Column="4" Style="{StaticResource BorderModuleArea}" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" x:Name="DetailTitle" Text="Select a menu item" 
                              FontSize="14" FontWeight="Bold" Margin="5,0,0,5" 
                              Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="DetailPanel" Margin="5">
                            <TextBlock Text="Select a menu item from the tree or list to edit it." 
                                      Foreground="{DynamicResource SecondaryTextBrush}" 
                                      TextWrapping="Wrap"/>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom status bar -->
        <DockPanel Grid.Row="2" Margin="10,0,10,6">
            <TextBlock x:Name="StatusText" Text="" Foreground="{DynamicResource SecondaryTextBrush}"/>
        </DockPanel>
    </Grid>
</Window>
