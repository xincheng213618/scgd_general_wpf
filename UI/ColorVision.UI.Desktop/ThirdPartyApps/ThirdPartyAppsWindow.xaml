<Window x:Class="ColorVision.UI.Desktop.ThirdPartyApps.ThirdPartyAppsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.UI.Desktop.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.ThirdPartyApps}" Height="760" Width="1180"
        MinHeight="560" MinWidth="860"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.Resources>
        <Style x:Key="PanelBorderStyle" TargetType="{x:Type Border}">
            <Setter Property="Background" Value="{DynamicResource GlobalBorderBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource GlobalBorderBrush1}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
        </Style>

        <Style x:Key="ToolbarIconButtonStyle" TargetType="{x:Type Button}">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style x:Key="SectionLabelStyle" TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="Opacity" Value="0.68"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="236"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="2" Style="{StaticResource PanelBorderStyle}" Padding="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Width="36" Height="36" CornerRadius="8"
                            Background="{DynamicResource ButtonBackground}"
                            BorderBrush="{DynamicResource ButtonBorderBrush}"
                            BorderThickness="1">
                        <TextBlock Text="&#xE71D;" FontFamily="Segoe MDL2 Assets" FontSize="18"
                                   Foreground="{DynamicResource PrimaryBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="{x:Static properties:Resources.ThirdPartyApps}" FontSize="19"
                                   FontWeight="SemiBold" Foreground="{DynamicResource GlobalTextBrush}"/>
                        <TextBlock x:Name="AppCountText" Margin="0,3,0,0" FontSize="12"
                                   Foreground="{DynamicResource GlobalTextBrush}" Opacity="0.65"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Margin="0,18,0,0" Height="40" CornerRadius="6"
                        Background="{DynamicResource GlobalBackground}"
                        BorderBrush="{DynamicResource GlobalBorderBrush1}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="34"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                                   Foreground="{DynamicResource PrimaryBrush}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <TextBox x:Name="SearchBox" Grid.Column="1" BorderThickness="0"
                                 Background="Transparent" Foreground="{DynamicResource GlobalTextBrush}"
                                 VerticalContentAlignment="Center" FontSize="13"
                                 TextChanged="SearchBox_TextChanged"/>
                    </Grid>
                </Border>

                <TextBlock x:Name="GroupsLabelText" Grid.Row="2" Margin="0,18,0,8"
                           Style="{StaticResource SectionLabelStyle}"/>

                <ListBox x:Name="GroupListBox" Grid.Row="3" Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         SelectionChanged="GroupListBox_SelectionChanged">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="{x:Type ListBoxItem}">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,2"/>
                            <Setter Property="Cursor" Value="Hand"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                        <Border x:Name="NavBorder" Padding="10,9" CornerRadius="6"
                                                Background="Transparent" BorderBrush="Transparent"
                                                BorderThickness="3,0,0,0">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding DisplayName}" FontSize="13"
                                                           Foreground="{DynamicResource GlobalTextBrush}"
                                                           TextTrimming="CharacterEllipsis"
                                                           ToolTip="{Binding DisplayName}"/>
                                                <TextBlock Grid.Column="1" Text="{Binding Count}" FontSize="12"
                                                           Foreground="{DynamicResource GlobalTextBrush}"
                                                           Opacity="0.55" Margin="10,0,0,0"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="NavBorder" Property="Background"
                                                        Value="{DynamicResource ButtonBackground}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="NavBorder" Property="Background"
                                                        Value="{DynamicResource ButtonBackground}"/>
                                                <Setter TargetName="NavBorder" Property="BorderBrush"
                                                        Value="{DynamicResource PrimaryBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="1" Grid.Row="0" Style="{StaticResource PanelBorderStyle}"
                Margin="14,0,0,12" Padding="14,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="CurrentGroupText" FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource GlobalTextBrush}"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button x:Name="BtnAddScript" Style="{StaticResource ToolbarIconButtonStyle}"
                            Content="&#xE756;" Click="BtnAddScript_Click"/>
                    <Button x:Name="BtnAddApp" Style="{StaticResource ToolbarIconButtonStyle}"
                            Content="&#xE710;" Click="BtnAddApp_Click"/>
                    <Button x:Name="BtnRefresh" Style="{StaticResource ToolbarIconButtonStyle}"
                            Content="&#xE72C;" Click="BtnRefresh_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Column="1" Grid.Row="1" Style="{StaticResource PanelBorderStyle}"
                Margin="14,0,0,0" Padding="10">
            <ListBox x:Name="AppsListBox" Background="Transparent" BorderThickness="0"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     MouseDoubleClick="AppsListBox_MouseDoubleClick"
                     PreviewMouseRightButtonUp="AppsListBox_PreviewMouseRightButtonUp">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="{x:Type ListBoxItem}">
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                    <Border x:Name="ItemBorder" Width="168" Height="108" Margin="6"
                                            CornerRadius="8" BorderThickness="1"
                                            BorderBrush="{DynamicResource GlobalBorderBrush1}"
                                            Background="{DynamicResource ButtonBackground}"
                                            Cursor="Hand">
                                        <Grid Margin="12" ToolTip="{Binding Name}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="44"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Grid Width="40" Height="40" HorizontalAlignment="Left"
                                                  VerticalAlignment="Top">
                                                <Border x:Name="IconPlaceholder" CornerRadius="9"
                                                        Background="{DynamicResource GlobalBackground}"
                                                        BorderBrush="{DynamicResource GlobalBorderBrush1}"
                                                        BorderThickness="1" Visibility="Collapsed">
                                                    <TextBlock Text="&#xE71D;" FontFamily="Segoe MDL2 Assets"
                                                               FontSize="17" Foreground="{DynamicResource PrimaryBrush}"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"/>
                                                </Border>
                                                <Image x:Name="AppIcon" Source="{Binding IconSource}" Width="38" Height="38"
                                                       RenderOptions.BitmapScalingMode="HighQuality"
                                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>

                                            <Grid Grid.Column="1" Margin="10,0,0,0">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <StackPanel>
                                                    <TextBlock Text="{Binding Name}" FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource GlobalTextBrush}"
                                                               TextTrimming="CharacterEllipsis"/>
                                                    <TextBlock Text="{Binding Group}" FontSize="11" Margin="0,5,0,0"
                                                               Foreground="{DynamicResource GlobalTextBrush}"
                                                               Opacity="0.62"
                                                               TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                <StackPanel x:Name="UnavailableBadge" Grid.Row="1"
                                                            Orientation="Horizontal" Visibility="Collapsed">
                                                    <Ellipse Width="6" Height="6" Fill="#FFE0A000"
                                                             Margin="0,0,5,0"/>
                                                    <TextBlock Text="{x:Static properties:Resources.NotInstalled}"
                                                               FontSize="11"
                                                               Foreground="{DynamicResource GlobalTextBrush}"
                                                               Opacity="0.7"/>
                                                </StackPanel>
                                            </Grid>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <DataTrigger Binding="{Binding IconSource}" Value="{x:Null}">
                                            <Setter TargetName="AppIcon" Property="Visibility" Value="Collapsed"/>
                                            <Setter TargetName="IconPlaceholder" Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding IsInstalled}" Value="False">
                                            <Setter TargetName="UnavailableBadge" Property="Visibility" Value="Visible"/>
                                            <Setter TargetName="ItemBorder" Property="Opacity" Value="0.64"/>
                                        </DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ItemBorder" Property="BorderBrush"
                                                    Value="{DynamicResource PrimaryBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="ItemBorder" Property="BorderBrush"
                                                    Value="{DynamicResource PrimaryBrush}"/>
                                            <Setter TargetName="ItemBorder" Property="BorderThickness" Value="2"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>
    </Grid>
</Window>
