<UserControl x:Class="ColorVision.Engine.Services.Devices.Camera.Templates.CameraRunParam.CameraRunParamEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:winf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:engine="clr-namespace:ColorVision.Engine"
             mc:Ignorable="d"
             d:DesignHeight="520"
             d:DesignWidth="520"
             Width="520">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Margin="0,0,0,6"
                Padding="12,10"
                Background="{DynamicResource SecondaryRegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="{engine:EngineLang Key='统一曝光 (ms)'}"/>
                <hc:TextBox x:Name="UnifiedExposureTextBox"
                            Grid.Column="1"
                            MinWidth="120"
                            Margin="12,0"
                            Padding="8,3"
                            VerticalContentAlignment="Center"
                            hc:InfoElement.Placeholder="ExpTime / R / G / B"
                            KeyDown="UnifiedExposureTextBox_KeyDown"/>
                <Button x:Name="ApplyUnifiedExposureButton"
                        Grid.Column="2"
                        MinWidth="88"
                        Padding="12,4"
                        Content="{engine:EngineLang 同步四路}"
                        Click="ApplyUnifiedExposure_Click"/>
            </Grid>
        </Border>

        <WindowsFormsHost Grid.Row="1">
            <winf:PropertyGrid x:Name="PropertyGrid1"/>
        </WindowsFormsHost>
    </Grid>
</UserControl>
