<UserControl x:Class="ColorVision.Engine.Services.Devices.Motor.DisplayMotor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorVision.Engine.Services.Devices.Motor"
             xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:DeviceMotor}"
             d:DesignHeight="450" d:DesignWidth="300" Initialized="UserControl_Initialized">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Small}"/>
    </UserControl.Resources>
    <StackPanel Margin="0,3">
        <DockPanel Margin="0,0,5,0">
            <Grid DockPanel.Dock="Right" >
                <ToggleButton  Style="{StaticResource ButtonMQTTConnect}"  Height="10" Width="10"  HorizontalAlignment="Center" IsChecked="{Binding  DService.IsAlive,Mode=OneWay}" IsEnabled="False" />
                <Image Source="{DynamicResource DrawingImageProperty}" Height="18" Margin="0,0,0,0"/>
                <Button Background="Transparent" BorderThickness="0" Command="{Binding PropertyCommand}" />
            </Grid>
            <Button DockPanel.Dock="Right" Command="{Binding RefreshCommand}" Background="Transparent"  BorderThickness="0" Padding="3">
                <Button.Triggers>
                    <EventTrigger RoutedEvent="Button.Click">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetName="rotateTransform1" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.5" FillBehavior="Stop"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Button.Triggers>
                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" Foreground="{DynamicResource GlobalTextBrush}" FontWeight="Bold"  FontSize="12" ToolTip="{x:Static properties:Resources.Refresh}">
                    <TextBlock.RenderTransform>
                        <RotateTransform x:Name="rotateTransform1" CenterX="6" CenterY="6"/>
                    </TextBlock.RenderTransform>
                </TextBlock>
            </Button>

            <Grid>
                <ToggleButton  x:Name="ToggleButton0"  Style="{StaticResource ButtonPageControl1}" Content="{Binding Config.Name}"  FontSize="15"  IsChecked="{Binding IsDisplayOpen}" />
                <Grid  Height="18"   HorizontalAlignment="Left" MouseLeftButtonDown="Grid_MouseLeftButtonDown" Visibility="{Binding IsDisplayOpen,Converter={StaticResource bool2VisibilityConverter}}" Background="{DynamicResource GlobalBackground}" Margin="4,0,0,0"/>
            </Grid>
        </DockPanel>
        <Border x:Name="DisPlayBorder" Style="{DynamicResource BorderModuleArea}" Visibility="{Binding IsDisplayOpen,Converter={StaticResource bool2VisibilityConverter}}" >
            <StackPanel   Margin="5" >
                <Button x:Name="ButtonSwitch" Margin="0,2" Content="{x:Static properties:Resources.Connect}"  Click="Open_Click"/>
                <DockPanel Margin="0,2">
                    <Button  DockPanel.Dock="Right" Content="{x:Static properties:Resources.Move}"  Click="Move_Click" />
                    <CheckBox  x:Name="CheckBoxIsAbs" Content="{x:Static properties:Resources.AbsolutePosition}" />
                    <TextBox x:Name="TextPos" Text="0"  BorderThickness="0,0,0,1" Margin="5,0"/>
                </DockPanel>
                <DockPanel Margin="0,2">
                    <Button  DockPanel.Dock="Right" Content="{x:Static properties:Resources.AdjustAperture}"  Click="Move1_Click" />
                    <TextBlock Text="{x:Static properties:Resources.Aperture}"  Width="40"  />
                    <TextBox x:Name="TextDiaphragm" Text="0"  BorderThickness="0,0,0,1"  Margin="5,0"/>
                </DockPanel>
                <Button  Margin="0,2" Content="{x:Static properties:Resources.HomePosition}"  Click="GoHome_Click" />
                <DockPanel>
                    <Button DockPanel.Dock="Right" Content="{x:Static properties:Resources.AcquirePosition}"  Click="GetPosition_Click" />
                    <TextBlock Text="{x:Static properties:Resources.CurrentPosition}" Width="80"/>
                    <TextBox IsReadOnly="True" Text="{Binding Config.Position}"  Background="Transparent" BorderThickness="0,0,0,0"  Margin="5,0"/>
                </DockPanel>
            </StackPanel>
        </Border>
    </StackPanel>

</UserControl>
