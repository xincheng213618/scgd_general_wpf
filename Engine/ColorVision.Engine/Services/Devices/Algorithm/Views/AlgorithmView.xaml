<UserControl x:Class="ColorVision.Engine.Services.Devices.Algorithm.Views.AlgorithmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:local="clr-namespace:ColorVision.Engine.Services.Devices.Algorithm.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:media="clr-namespace:ColorVision.ImageEditor;assembly=ColorVision.ImageEditor"
             xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
             xmlns:se="clr-namespace:ColorVision.Engine.Services"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:ViewAlgorithmConfig}"
             d:DesignHeight="600" d:DesignWidth="1280"
             Background="{DynamicResource BorderBrush}"
             Initialized="UserControl_Initialized">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
            <Setter Property="ToolTip" Value="{Binding Text, RelativeSource={RelativeSource Self}}" />
        </Style>
    </UserControl.Resources>

    <Grid x:Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="ListCol1" Width="*" />
            <ColumnDefinition x:Name="ListCol2" Width="Auto" />
        </Grid.ColumnDefinitions>
        <Grid x:Name="Grid1" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="ListRow1" Height="*" />
                <RowDefinition x:Name="ListRow2" Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <ToolBarTray Grid.Row="0" IsLocked="True">
                    <ToolBar Padding="3,2">
                        <ToggleButton IsChecked="{Binding IsShowListView, Converter={StaticResource Boolean2BooleanReConverter}}" Style="{StaticResource ToggleButtonSwitch}" />
                        <ToggleButton IsChecked="{Binding IsShowSideListView, Converter={StaticResource Boolean2BooleanReConverter}}" Style="{StaticResource ToggleButtonSwitch}" />

                        <Button Margin="2,0,0,0" Click="Exchange_Click" Content="{x:Static properties:Resources.Exchange}" />

                        <Button Margin="2,0,0,0" Click="Inquire_Click" Content="{x:Static properties:Resources.Inquire}" />
                        <Button Margin="2,0,0,0" Click="SearchAdvanced_Click" Content="{x:Static properties:Resources.AdvanceSearch}" />

                        <Button x:Name="Button222" Click="SideSave_Click" Content="{x:Static properties:Resources.SaveDataColumn}" />
                    </ToolBar>
                </ToolBarTray>
                <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                    <Button Width="25" Height="25" Margin="0,1,5,0" Padding="2" Click="Button_Click">
                        <Rectangle Width="16" Height="16" Fill="{StaticResource DISave}" />
                    </Button>
                    <Button Width="25" Height="25" Margin="0,1,5,0" Padding="2" Command="{Binding ClearListCommand}">
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation AutoReverse="True" Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleX" To="0.7" Duration="0:0:0.1" />
                                        <DoubleAnimation AutoReverse="True" Storyboard.TargetName="ScaleTransform" Storyboard.TargetProperty="ScaleY" To="0.7" Duration="0:0:0.1" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <Image RenderTransformOrigin="0.5,0.5" Source="{DynamicResource DrawingImageDelete}" Stretch="Uniform">
                            <Image.RenderTransform>
                                <ScaleTransform x:Name="ScaleTransform" ScaleX="1" ScaleY="1" />
                            </Image.RenderTransform>
                        </Image>
                    </Button>
                    <Button Grid.Column="1" Width="25" Height="25" Margin="0,1,5,0" Padding="2" HorizontalAlignment="Left" Command="{Binding EditCommand}">
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation FillBehavior="Stop" Storyboard.TargetName="rotateTransform2" Storyboard.TargetProperty="Angle" From="0" To="360" Duration="0:0:0.5" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                        <TextBlock HorizontalAlignment="Center" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="{DynamicResource GlobalTextBrush}" Text="&#xE713;">
                            <TextBlock.RenderTransform>
                                <RotateTransform x:Name="rotateTransform2" CenterX="8" CenterY="8" />
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Button>
                </StackPanel>



                <ListView x:Name="listView1" Grid.Row="1" Height="{Binding Height}" Padding="0" BorderThickness="0" PreviewKeyDown="listView1_PreviewKeyDown" ScrollViewer.VerticalScrollBarVisibility="Visible" SelectionChanged="listView1_SelectionChanged" Visibility="{Binding IsShowListView, Converter={StaticResource bool2VisibilityConverter}}">
                    <ListView.Resources>
                        <ContextMenu x:Key="ItemContextMenu" Opened="ContextMenu_Opened" />
                    </ListView.Resources>
                    <ListView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource ListViewItemBaseStyle.Small}" TargetType="{x:Type ListViewItem}">
                            <Setter Property="ContextMenu" Value="{Binding ContextMenu}" />
                            <Setter Property="Opacity" Value="1" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsFileExists}" Value="False">
                                    <Setter Property="Opacity" Value="0.7" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                                    <Setter Property="Visibility" Value="Visible" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                                    <EventSetter Event="Click" Handler="GridViewColumnSort" />
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                            <GridViewColumn Width="50" Header="{x:Static properties:Resources.SerialNumber1}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding Id}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Width="100" Header="{x:Static properties:Resources.CreateTime}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding CreateTime, StringFormat=yyyy/MM/dd/HH:mm:ss}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="ResultType">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding ResultType}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="ResultDesc">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding ResultDesc}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{x:Static properties:Resources.File}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding FilePath}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{x:Static properties:Resources.Template}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding POITemplateName}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="img_result">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding ResultImagFile}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="{x:Static properties:Resources.Duration}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding TotalTime}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Width="160" Header="{x:Static properties:Resources.BatchNumber}">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                        <TextBlock Text="{Binding Batch}" />
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>


                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
            <GridSplitter Grid.Row="1" Height="2" HorizontalAlignment="Stretch" VerticalAlignment="Top" Panel.ZIndex="999" Background="#1FFFFFFF" DragCompleted="GridSplitter_DragCompleted" />
        </Grid>
        <ListView x:Name="listViewSide" Grid.Column="1" Width="250" Padding="0" BorderThickness="0" Visibility="{Binding IsShowSideListView, Converter={StaticResource bool2VisibilityConverter}}">
            <ListView.Resources>
                <ContextMenu x:Key="ItemContextMenu" Opened="ContextMenu1_Opened" />
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style BasedOn="{StaticResource ListViewItemBaseStyle.Small}" TargetType="{x:Type ListViewItem}">
                    <Setter Property="ContextMenu" Value="{Binding ContextMenu}" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="BorderThickness" Value="0" />
                            <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                        </Style>
                    </GridView.ColumnHeaderContainerStyle>
                </GridView>
            </ListView.View>
        </ListView>

        <TextBox x:Name="TextBoxside" Grid.Row="1" Grid.Column="1" VerticalAlignment="Stretch" VerticalContentAlignment="Top" Panel.ZIndex="9" BorderThickness="0" IsReadOnly="True" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto" TextWrapping="WrapWithOverflow" Visibility="Collapsed" />

        <GridSplitter Grid.Column="1" Width="2" HorizontalAlignment="Left" Panel.ZIndex="999" Background="#1FFFFFFF" DragCompleted="GridSplitter_DragCompleted1" />

    </Grid>
</UserControl>
