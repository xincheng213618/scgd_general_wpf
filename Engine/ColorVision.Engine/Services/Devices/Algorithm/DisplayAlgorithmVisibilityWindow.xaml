<Window x:Class="ColorVision.Engine.Services.Devices.Algorithm.DisplayAlgorithmVisibilityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
        mc:Ignorable="d"
        Title="{x:Static properties:Resources.AlgorithmVisibilitySettings}" Height="550" Width="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource GlobalBackground}"
        ResizeMode="CanResizeWithGrip"
        Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{x:Static properties:Resources.AlgorithmVisibilitySettings}" FontSize="14" FontWeight="Bold" Margin="0,0,0,5"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,5">
            <TextBlock Text="{x:Static properties:Resources.Group}" Margin="0,0,5,0"/>
            <ComboBox x:Name="CB_GroupFilter" Width="130" Margin="0,0,10,0" SelectionChanged="CB_GroupFilter_SelectionChanged"/>
            <Button Content="{x:Static properties:Resources.ShowGroup}" Click="ShowGroup_Click" Margin="0,0,5,0" Width="80"/>
            <Button Content="{x:Static properties:Resources.HideGroup}" Click="HideGroup_Click" Width="80"/>
        </DockPanel>

        <ListView x:Name="AlgorithmListView" Grid.Row="2" Margin="0,5" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="50">
                        <GridViewColumn.Header>
                            <TextBlock Text="{x:Static properties:Resources.Visible}"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsVisible}" HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="140">
                        <GridViewColumn.Header>
                            <TextBlock Text="{x:Static properties:Resources.Name}"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding DisplayName, UpdateSourceTrigger=LostFocus}" MinWidth="100" BorderThickness="0" Background="Transparent"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="90">
                        <GridViewColumn.Header>
                            <TextBlock Text="{x:Static properties:Resources.Group}"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Group}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="60">
                        <GridViewColumn.Header>
                            <TextBlock Text="Order"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBox Text="{Binding Order, UpdateSourceTrigger=LostFocus}" MinWidth="40" BorderThickness="0" Background="Transparent"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="100">
                        <GridViewColumn.Header>
                            <TextBlock Text="Key"/>
                        </GridViewColumn.Header>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Name}" Foreground="Gray"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static properties:Resources.ShowAll}" Click="ShowAll_Click" Margin="0,0,10,0" Width="80"/>
            <Button Content="{x:Static properties:Resources.HideAll}" Click="HideAll_Click" Margin="0,0,10,0" Width="80"/>
            <Button Content="{x:Static properties:Resources.Close}" Click="Close_Click" Width="80" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
