<Window x:Class="ColorVision.Engine.Services.PhyCameras.Configs.EditFilterWheelConfig"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Engine.Services.PhyCameras.Configs"
        xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
        mc:Ignorable="d"
        Title="Filter Wheel Configuration" 
        Background="{DynamicResource GlobalBackground}" 
        Height="500" Width="600" 
        Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="{x:Static properties:Resources.Add}" Click="Button_Add_Click" Margin="0,0,5,0" Width="80"/>
            <Button Content="{x:Static properties:Resources.Delete}" Click="Button_Delete_Click" Margin="0,0,5,0" Width="80"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1" 
                  x:Name="DataGridHoleMapping" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Background="{DynamicResource SecondaryRegionBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Hole Index" Binding="{Binding HoleIndex}" Width="*" IsReadOnly="False"/>
                <DataGridTextColumn Header="Hole Name" Binding="{Binding HoldName}" Width="2*" IsReadOnly="False"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Bottom buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="{x:Static properties:Resources.Submit}" 
                    Foreground="White" 
                    Background="{StaticResource PrimaryBrush}" 
                    Click="Button_Save_Click" 
                    Width="100" 
                    Margin="0,0,5,0"/>
            <Button Content="{x:Static properties:Resources.Cancel}" 
                    Click="Button_Cancel_Click" 
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
