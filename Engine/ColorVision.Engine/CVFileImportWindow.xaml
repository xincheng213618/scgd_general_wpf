<Window x:Class="ColorVision.Engine.CVFileImportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Engine"
        mc:Ignorable="d"
        Title="相机配置导入预览" Height="500" Width="600" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题区域 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="检测到相机配置文件" FontSize="18" FontWeight="Bold" Foreground="#333"/>
            <TextBlock Text="请确认以下信息，并决定是否导入该相机配置：" Margin="0,5,0,0" Foreground="#666"/>
        </StackPanel>

        <!-- 信息展示区域 -->
        <DataGrid x:Name="InfoGrid" Grid.Row="1" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" 
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  Background="White" BorderBrush="#DDD" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="属性 (Property)" Binding="{Binding Key}" Width="200" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="值 (Value)" Binding="{Binding Value}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="取消" Width="100" Height="30" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="导入相机" Width="100" Height="30" Background="#007ACC" Foreground="White" Click="Import_Click"/>
        </StackPanel>
    </Grid>
</Window>
