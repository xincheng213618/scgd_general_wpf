<Window x:Class="ColorVision.Database.DatabaseCleanupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Database"
        xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:DatabaseCleanupWindowViewModel}"
        Title="{x:Static properties:Resources.DataCleanup}"
        Width="1080"
        Height="720"
        MinWidth="900"
        MinHeight="620"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Small}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Margin="0,0,0,10">
            <TextBlock Text="{x:Static properties:Resources.DataCleanup}" FontSize="18" FontWeight="SemiBold"/>
            <TextBlock Margin="0,6,0,0"
                       Opacity="0.82"
                       Text="{x:Static properties:Resources.DataCleanupDesc}"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#30363D" BorderThickness="1" CornerRadius="6" Padding="10">
            <TabControl ItemsSource="{Binding Sources}" SelectedItem="{Binding SelectedSource}">
                <TabControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type local:DatabaseCleanupSourceViewModel}">
                        <TextBlock Text="{Binding DisplayName}" Margin="8,2"/>
                    </DataTemplate>
                </TabControl.ItemTemplate>

                <TabControl.ContentTemplate>
                    <DataTemplate DataType="{x:Type local:DatabaseCleanupSourceViewModel}">
                        <Grid Margin="0,8,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Opacity="0.82"/>

                            <StackPanel Grid.Row="1" Margin="0,12,0,12">
                                <WrapPanel Margin="0,0,0,8">
                                    <TextBlock Margin="0,0,8,0" Text="{x:Static properties:Resources.RetainMonths}"/>
                                    <TextBox Width="72" Margin="0,0,12,0" Text="{Binding KeepMonthsText, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Button Content="{x:Static properties:Resources.RefreshStats}" Command="{Binding RefreshCommand}" Margin="0,0,6,0"/>
                                    <Button Content="{x:Static properties:Resources.CleanupHistory}" Command="{Binding CleanupHistoryCommand}" Margin="0,0,6,0"/>
                                    <Button Content="{x:Static properties:Resources.ClearAllData}" Command="{Binding CleanupAllCommand}" Background="Firebrick" Foreground="White"/>
                                </WrapPanel>
                                <TextBlock Text="{Binding Status}" TextWrapping="Wrap"/>
                            </StackPanel>

                            <DataGrid Grid.Row="2"
                                      AutoGenerateColumns="False"
                                      IsReadOnly="True"
                                      CanUserAddRows="False"
                                      CanUserDeleteRows="False"
                                      CanUserResizeRows="False"
                                      HeadersVisibility="Column"
                                      GridLinesVisibility="Horizontal"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      RowHeaderWidth="0"
                                      SelectionMode="Single"
                                      EnableRowVirtualization="True"
                                      ItemsSource="{Binding Tables}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="{x:Static properties:Resources.TableName}" Binding="{Binding TableName}" Width="*"/>
                                    <DataGridTextColumn Header="{x:Static properties:Resources.Status}" Binding="{Binding ExistsDisplay}" Width="90"/>
                                    <DataGridTextColumn Header="{x:Static properties:Resources.RowCount}" Binding="{Binding RowCountDisplay}" Width="140"/>
                                    <DataGridTextColumn Header="{x:Static properties:Resources.Size_Column}" Binding="{Binding SizeDisplay}" Width="140"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Border>
    </Grid>
</Window>