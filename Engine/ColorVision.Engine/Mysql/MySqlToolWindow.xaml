<Window x:Class="ColorVision.Database.MySqlToolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Database"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MySqlLocalServicesManager}"
        Title="MysqlToolWindow" Height="620" Width="1320"  Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Small}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
        <Style TargetType="{x:Type TextBlock}" >
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
        <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Margin="0,0,0,5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Config.ImagePath}"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="450"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Grid.Row="0">
                    <Button Content="迁移数据库备份(只包含流程和资源)" Command="{Binding BackupResourcesCommand}" Margin="0,0,5,0"/>
                    <Button Content="全盘备份" Command="{Binding BackupAllResourcesCommand}"   Margin="0,0,5,0"/>
                    <Button Content="加载备份" Command="{Binding RestoreSelectCommand}" Background="Red" Foreground="White"/>
                </StackPanel>

                <ListView Grid.Row="1" x:Name="listView1" Background="Transparent" Margin="0" Padding="0" BorderThickness="0" ItemsSource="{Binding Backups}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemBaseStyle.Small}">
                            <Setter Property="ContextMenu" Value="{Binding ContextMenu}"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.View>
                        <GridView>
                            <GridView.ColumnHeaderContainerStyle>
                                <Style TargetType="{x:Type GridViewColumnHeader}"  BasedOn="{StaticResource  GridViewColumnHeaderBase}">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                </Style>
                            </GridView.ColumnHeaderContainerStyle>
                            <GridViewColumn Header="文件名">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Name}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="创建时间">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding CreationTimeDisplay}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                            <GridViewColumn Header="还原">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="还原" Command="{Binding RestoreCommand}" Margin="0,0,5,0"/>
                                            <Button Content="选中" Command="{Binding SelectCommand}"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

            <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="#3F3F46"/>

            <Grid Grid.Column="2" Margin="10,5,5,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel>
                    <TextBlock Text="数据表清理" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Margin="0,6,0,0" Text="仅显示并处理算法结果明细、测量结果和测量批次等数据表，不会触碰资源、模板、字典、配置等无关表。" TextWrapping="Wrap" Opacity="0.8"/>
                </StackPanel>

                <StackPanel Grid.Row="1" Margin="0,12,0,12">
                    <WrapPanel Margin="0,0,0,8">
                        <TextBlock Margin="0,0,8,0" Text="保留月数"/>
                        <TextBox Width="72" Margin="0,0,12,0" Text="{Binding CleanupKeepMonthsText, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="刷新统计" Command="{Binding RefreshCleanupTablesCommand}" Margin="0,0,6,0"/>
                        <Button Content="清理历史数据" Command="{Binding CleanupHistoryCommand}" Margin="0,0,6,0"/>
                        <Button Content="清空全部数据表" Command="{Binding CleanupAllResultTablesCommand}" Background="Firebrick" Foreground="White"/>
                    </WrapPanel>
                    <TextBlock Text="{Binding CleanupStatus}" TextWrapping="Wrap"/>
                </StackPanel>

                <DataGrid Grid.Row="2"
                          ItemsSource="{Binding CleanupTables}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserResizeRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Background="Transparent"
                          BorderThickness="0"
                          RowHeaderWidth="0"
                          SelectionMode="Single"
                          EnableRowVirtualization="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="名称" Binding="{Binding DisplayName}" Width="150"/>
                        <DataGridTextColumn Header="表名" Binding="{Binding TableName}" Width="250"/>
                        <DataGridTextColumn Header="状态" Binding="{Binding ExistsDisplay}" Width="70"/>
                        <DataGridTextColumn Header="行数" Binding="{Binding RowCountDisplay}" Width="110"/>
                        <DataGridTextColumn Header="占用" Binding="{Binding SizeDisplay}" Width="110"/>
                        <DataGridTextColumn Header="时间列" Binding="{Binding TimeColumnDisplay}" Width="95"/>
                        <DataGridTextColumn Header="时间范围" Binding="{Binding TimeRangeDisplay}" Width="210"/>
                        <DataGridTextColumn Header="清理规则" Binding="{Binding CleanupMode}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>

    </Grid>
</Window>
