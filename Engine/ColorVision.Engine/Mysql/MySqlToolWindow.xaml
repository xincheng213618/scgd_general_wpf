<Window x:Class="ColorVision.Database.MySqlToolWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Database"
        xmlns:engine="clr-namespace:ColorVision.Engine"
        xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:MySqlLocalServicesManager}"
        Title="{x:Static properties:Resources.MysqlTool}" Height="620" Width="860" Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.Resources>
        <local:SqlFileNameWithoutExtensionConverter x:Key="SqlFileNameWithoutExtensionConverter"/>
        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource TextBox.Small}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
        <Style TargetType="{x:Type TextBlock}" >
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
                <Button Content="{x:Static properties:Resources.MigrationBackup}" Command="{Binding BackupResourcesCommand}" Margin="0,0,5,0"/>
                <Button Content="{x:Static properties:Resources.FullBackup}" Command="{Binding BackupAllResourcesCommand}" Margin="0,0,5,0"/>
                <Button Content="{x:Static properties:Resources.LoadBackup}" Command="{Binding RestoreSelectCommand}" Background="Red" Foreground="White" Margin="0,0,5,0"/>
                <Button Content="{x:Static properties:Resources.DataCleanup}" Click="OpenCleanupWindow_Click"/>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="listView1" Background="Transparent" Margin="0,10,0,0" Padding="0" BorderThickness="0" KeyDown="ListView1_KeyDown">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemBaseStyle.Small}">
                        <Setter Property="ContextMenu" Value="{Binding ContextMenu}"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridViewColumnHeaderBase}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Width="380">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader x:Name="FileNameColumnHeader" Tag="Name" Content="{x:Static properties:Resources.FileName_Mysql}" Click="GridViewColumnHeader_Click"/>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name, Converter={StaticResource SqlFileNameWithoutExtensionConverter}}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="180">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader x:Name="CreationTimeColumnHeader" Tag="CreationTime" Content="{x:Static properties:Resources.CreateDate}" Click="GridViewColumnHeader_Click"/>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CreationTimeDisplay}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Width="230" Header="{x:Static properties:Resources.Restore_Mysql}">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="{x:Static properties:Resources.Restore_Mysql}" Command="{Binding RestoreCommand}" Margin="0,0,5,0"/>
                                        <Button Content="{x:Static properties:Resources.Selected}" Command="{Binding SelectCommand}" Margin="0,0,5,0"/>
                                        <Button Content="{engine:EngineLang Key='重命名'}" Tag="{Binding}" Click="RenameBackup_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
        </ListView>
    </Grid>
</Window>
