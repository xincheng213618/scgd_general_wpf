<Window x:Class="ColorVision.Engine.Templates.TemplateManagerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:ColorVision.Engine.Templates"
        xmlns:properties ="clr-namespace:ColorVision.Engine.Properties"
        mc:Ignorable="d"
        Title="模板管理窗口" Height="600" Width="1200" Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top toolbar -->
        <Grid Grid.Row="0" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Search box on the left -->
            <Grid x:Name="SearchGrid" Grid.Column="0" Margin="0,0,5,0" HorizontalAlignment="Stretch" VerticalAlignment="Center">
                <hc:TextBox x:Name="Searchbox" Style="{DynamicResource TextBoxPlus.Small}" Padding="25,5,5,5" BorderThickness="1" 
                           hc:InfoElement.Placeholder="{x:Static properties:Resources.Search}" BorderBrush="{DynamicResource BorderBrush}" 
                           VerticalContentAlignment="Center" TextChanged="Searchbox_TextChanged"/>
                <Viewbox Width="14" Height="14" HorizontalAlignment="Left" Margin="5,0,0,0">
                    <Rectangle Width="16" Height="16">
                        <Rectangle.Resources>
                            <SolidColorBrush x:Key="canvas" Opacity="0" />
                            <SolidColorBrush x:Key="light-defaultgrey-10" Color="{DynamicResource defaultgreyColor}" Opacity="0.1" />
                            <SolidColorBrush x:Key="light-defaultgrey" Color="{DynamicResource defaultgreyColor}" Opacity="1" />
                        </Rectangle.Resources>
                        <Rectangle.Fill>
                            <DrawingBrush Stretch="None">
                                <DrawingBrush.Drawing>
                                    <DrawingGroup>
                                        <DrawingGroup x:Name="canvas">
                                            <GeometryDrawing Brush="{DynamicResource canvas}" Geometry="F1M0,0H16V16H0Z" />
                                        </DrawingGroup>
                                        <DrawingGroup x:Name="level_1">
                                            <GeometryDrawing Brush="{DynamicResource light-defaultgrey-10}" Geometry="F1M11.5,6.5a5,5,0,1,1-5-5A5,5,0,0,1,11.5,6.5Z" />
                                            <GeometryDrawing Brush="{DynamicResource light-defaultgrey}" Geometry="F1M1,6.5a5.5,5.5,0,0,0,9.017,4.224l5.129,5.13.708-.708-5.13-5.129A5.5,5.5,0,1,0,1,6.5Zm1,0A4.5,4.5,0,1,1,6.5,11,4.505,4.505,0,0,1,2,6.5Z" />
                                        </DrawingGroup>
                                    </DrawingGroup>
                                </DrawingBrush.Drawing>
                            </DrawingBrush>
                        </Rectangle.Fill>
                    </Rectangle>
                </Viewbox>
            </Grid>
            
            <!-- Column count selector on the right -->
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBlock Text="列数:" VerticalAlignment="Center" Margin="0,0,5,0" Foreground="{DynamicResource PrimaryTextBrush}"/>
                <ComboBox x:Name="ColumnCountComboBox" Width="60" SelectedIndex="2" SelectionChanged="ColumnCountComboBox_SelectionChanged"
                         Style="{DynamicResource ComboBox.Small}">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                    <ComboBoxItem Content="4"/>
                    <ComboBoxItem Content="5"/>
                    <ComboBoxItem Content="6"/>
                </ComboBox>
            </StackPanel>
        </Grid>

        <!-- Main content area: Middle Template Grid + Right Editor Panel -->
        <Grid Grid.Row="1" Margin="5,0,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="350"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="400" MinWidth="350"/>
            </Grid.ColumnDefinitions>

            <!-- Middle: Template Grid (ScrollViewer with dynamic content) -->
            <Border Grid.Column="0" Style="{StaticResource BorderModuleArea}" Padding="5">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="模板列表" FontSize="14" FontWeight="Bold" Margin="5,0,0,5" Foreground="{DynamicResource PrimaryTextBrush}"/>
                    <ScrollViewer x:Name="TemplateScrollViewer" Grid.Row="1" 
                                 VerticalScrollBarVisibility="Auto" 
                                 HorizontalScrollBarVisibility="Disabled"
                                 Background="Transparent">
                        <StackPanel x:Name="TemplateContainer" Margin="5"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Splitter -->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}" 
                         ResizeBehavior="PreviousAndNext" ResizeDirection="Columns"/>

            <!-- Right: Embedded Template Editor Panel -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" x:Name="EditorTitle" Text="选择一个模板查看详情" 
                          FontSize="14" FontWeight="Bold" Margin="5,5,5,5" 
                          Foreground="{DynamicResource PrimaryTextBrush}"/>
                <Grid Grid.Row="1" x:Name="EditorContainer">
                    <!-- Template editor content will be loaded here dynamically -->
                    <TextBlock x:Name="PlaceholderText" Text="请选择一个模板以查看和编辑" 
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Foreground="{DynamicResource SecondaryTextBrush}" FontSize="12"/>
                </Grid>
            </Grid>
        </Grid>

        <!-- Bottom status bar -->
        <DockPanel Grid.Row="2" Margin="10,0,10,5">
            <TextBlock x:Name="SummaryText" Text="" Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock x:Name="SummaryText1" Text="" HorizontalAlignment="Right" Foreground="{DynamicResource PrimaryTextBrush}"/>
        </DockPanel>
    </Grid>
</Window>
