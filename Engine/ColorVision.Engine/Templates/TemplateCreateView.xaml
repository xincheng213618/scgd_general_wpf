<UserControl x:Class="ColorVision.Engine.Templates.TemplateCreateView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:winf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
             xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
             mc:Ignorable="d"
             d:DesignHeight="620"
             d:DesignWidth="900"
             PreviewKeyDown="TemplateCreateView_PreviewKeyDown"
             Background="{DynamicResource GlobalBackground}">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TextBlockBaseStyle}"/>
        <Style x:Key="TemplateCreateSourceItemStyle" TargetType="{x:Type ListBoxItem}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Margin" Value="0,0,8,8"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ListBoxItem}">
                        <Border x:Name="Card"
                                Width="178"
                                Height="72"
                                Padding="13,10"
                                Background="{DynamicResource RegionBrush}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>
                                <TextBlock Text="{Binding SourceLabel}"
                                           FontSize="10"
                                           Foreground="{DynamicResource ThirdlyTextBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <TextBlock Grid.Row="1"
                                           Margin="0,4,20,0"
                                           Text="{Binding Title}"
                                           FontSize="14"
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource GlobalTextBrush}"
                                           TextTrimming="CharacterEllipsis"/>
                                <Ellipse x:Name="SelectedIndicator"
                                         Width="14"
                                         Height="14"
                                         HorizontalAlignment="Right"
                                         VerticalAlignment="Top"
                                         Fill="{DynamicResource PrimaryBrush}"
                                         Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                                <Setter TargetName="Card" Property="BorderThickness" Value="2"/>
                                <Setter TargetName="Card" Property="Background" Value="#140B84D8"/>
                                <Setter TargetName="SelectedIndicator" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Card" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="20,18,20,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="SourceColumn" Width="*"/>
                <ColumnDefinition x:Name="PropertyColumn" Width="0"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{x:Static properties:Resources.NewTemplateName}"
                           FontWeight="SemiBold"/>
                <hc:TextBox x:Name="CreateNameTextBox"
                            Grid.Row="1"
                            Height="38"
                            Margin="0,6,0,0"
                            VerticalContentAlignment="Center"
                            BorderBrush="{DynamicResource BorderBrush}"
                            hc:InfoElement.ShowClearButton="True"
                            TextChanged="CreateNameTextBox_TextChanged"
                            PreviewKeyDown="CreateNameTextBox_PreviewKeyDown"/>
                <TextBlock x:Name="ValidationTextBlock"
                           Grid.Row="2"
                           Margin="2,5,0,0"
                           Foreground="#D13438"
                           FontSize="11"
                           Visibility="Collapsed"/>

                <Grid x:Name="SourcePanel" Grid.Row="3" Grid.RowSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,20,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{x:Static properties:Resources.CreateSource}"
                                   FontWeight="SemiBold"
                                   VerticalAlignment="Center"/>
                        <hc:TextBox x:Name="SourceSearchTextBox"
                                    Grid.Column="1"
                                    Width="160"
                                    Height="30"
                                    Padding="9,1"
                                    VerticalContentAlignment="Center"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    hc:InfoElement.Placeholder="搜索"
                                    hc:InfoElement.ShowClearButton="True"
                                    TextChanged="SourceSearchTextBox_TextChanged"/>
                    </Grid>

                    <ListBox x:Name="SourceListBox"
                             Grid.Row="1"
                             Padding="0"
                             Background="Transparent"
                             BorderThickness="0"
                             ItemContainerStyle="{StaticResource TemplateCreateSourceItemStyle}"
                             SelectionChanged="SourceListBox_SelectionChanged"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             KeyboardNavigation.DirectionalNavigation="Contained">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                    </ListBox>
                </Grid>
            </Grid>

            <Border x:Name="PropertyPreviewBorder"
                    Grid.Column="1"
                    Margin="18,0,0,0"
                    Background="{DynamicResource RegionBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Visibility="Collapsed">
                <Grid>
                    <WindowsFormsHost x:Name="PropertyGridHost">
                        <winf:PropertyGrid x:Name="PropertyGrid1"/>
                    </WindowsFormsHost>
                    <Grid x:Name="CustomPreviewHost" Margin="5" Visibility="Collapsed"/>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="1"
                Padding="16,12"
                Background="{DynamicResource SecondaryRegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{x:Static properties:Resources.Cancel}"
                        MinWidth="92"
                        Height="36"
                        Margin="0,0,10,0"
                        Click="CancelButton_Click"/>
                <Button Content="{x:Static properties:Resources.Create}"
                        MinWidth="108"
                        Height="36"
                        Foreground="White"
                        Background="{DynamicResource PrimaryBrush}"
                        BorderBrush="{DynamicResource PrimaryBrush}"
                        Click="CreateButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
