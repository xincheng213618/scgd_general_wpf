<UserControl x:Class="ColorVision.Engine.Templates.Flow.FlowNodePropertyPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
             xmlns:st="clr-namespace:ST.Library.UI.NodeEditor;assembly=ST.Library.UI"
             mc:Ignorable="d" d:DesignHeight="400" d:DesignWidth="300"
             Background="{DynamicResource GlobalBackground}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button x:Name="TogglePropertyEditorMode" Height="28" HorizontalAlignment="Stretch" Click="TogglePropertyEditorMode_Click" Content="{x:Static properties:Resources.Switch}" />

        <Grid Grid.Row="1">
            <WindowsFormsHost x:Name="PropertyGridHost" Visibility="Visible">
                <st:STNodePropertyGrid x:Name="STNodePropertyGrid1" />
            </WindowsFormsHost>

            <ScrollViewer x:Name="SignStackScrollViewer" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto" Visibility="Collapsed">
                <StackPanel x:Name="SignStackPanelContainer" Orientation="Vertical" Margin="5"/>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
