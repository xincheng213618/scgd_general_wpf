<Window x:Class="ColorVision.Engine.Templates.Flow.FlowMessageListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Engine.Templates.Flow"
        mc:Ignorable="d"
        Title="流程MQTT消息查询" Height="720" Width="1280"
        Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <Window.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Margin="5,5,5,5">
            <ToolBarTray IsLocked="True">
                <ToolBar Padding="3,2">
                    <TextBlock Text="节点:" VerticalAlignment="Center" Margin="2,0,2,0"/>
                    <TextBox x:Name="FilterNodeName" Width="100" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="EventName:" VerticalAlignment="Center" Margin="2,0,2,0"/>
                    <TextBox x:Name="FilterEventName" Width="100" VerticalAlignment="Center" Margin="0,0,4,0"/>
                    <TextBlock Text="状态:" VerticalAlignment="Center" Margin="2,0,2,0"/>
                    <ComboBox x:Name="FilterState" Width="90" VerticalAlignment="Center" Margin="0,0,4,0">
                        <ComboBoxItem Content="全部" IsSelected="True"/>
                        <ComboBoxItem Content="Sended"/>
                        <ComboBoxItem Content="Success"/>
                        <ComboBoxItem Content="Fail"/>
                        <ComboBoxItem Content="Timeout"/>
                    </ComboBox>
                    <Separator/>
                    <Button Content="查询" Click="QueryButton_Click" Margin="2,0,0,0"/>
                    <Button Content="刷新" Click="RefreshButton_Click" Margin="2,0,0,0"/>
                    <Separator/>
                    <Button Content="清空记录" Click="DeleteAllButton_Click" Margin="2,0,0,0" Foreground="#E53935"/>
                    <Separator/>
                    <TextBlock Text="加载条数:" VerticalAlignment="Center" Margin="4,0,2,0"/>
                    <TextBox x:Name="LoadCount" Width="50" VerticalAlignment="Center" Text="500"/>
                </ToolBar>
            </ToolBarTray>
        </Grid>

        <!-- Main content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="400"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Message list -->
            <ListView x:Name="ListView1" Grid.Column="0" Background="Transparent" BorderThickness="1"
                      Margin="5,0,5,5" SelectionChanged="ListView1_SelectionChanged"
                      ScrollViewer.HorizontalScrollBarVisibility="Auto">
                <ListView.ItemContainerStyle>
                    <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemBaseStyle.Small}"/>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView>
                        <GridView.ColumnHeaderContainerStyle>
                            <Style TargetType="{x:Type GridViewColumnHeader}" BasedOn="{StaticResource GridViewColumnHeaderBase}">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Style>
                        </GridView.ColumnHeaderContainerStyle>
                        <GridViewColumn Header="BatchId" Width="60">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding BatchId}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="节点" Width="120">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NodeName}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="EventName" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding EventName}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="发送时间" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SendTime, StringFormat=HH:mm:ss.fff}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="接收时间" Width="130">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding RecvTime, StringFormat=HH:mm:ss.fff}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="耗时(ms)" Width="70">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ElapsedMs}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="状态" Width="70">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding State}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="状态码" Width="50">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding StatusCode}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="NodeId" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding NodeId}"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="Transparent"/>

            <!-- Right: Send/Recv detail panels -->
            <Grid Grid.Column="2" Margin="0,0,5,5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <TextBlock Text="发送数据 (Send)" FontWeight="Bold" Margin="2"/>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="2">
                        <TextBlock Text="Topic: " VerticalAlignment="Center"/>
                        <TextBlock x:Name="SendTopicText" VerticalAlignment="Center" Foreground="Gray"/>
                    </StackPanel>
                </Grid>
                <TextBox x:Name="SendPayloadBox" Grid.Row="1" IsReadOnly="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap" BorderThickness="1"
                         FontFamily="Consolas" FontSize="12"/>

                <Grid Grid.Row="2">
                    <TextBlock Text="接收数据 (Receive)" FontWeight="Bold" Margin="2"/>
                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal" Margin="2">
                        <TextBlock Text="Topic: " VerticalAlignment="Center"/>
                        <TextBlock x:Name="RecvTopicText" VerticalAlignment="Center" Foreground="Gray"/>
                    </StackPanel>
                </Grid>
                <TextBox x:Name="RecvPayloadBox" Grid.Row="3" IsReadOnly="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap" BorderThickness="1"
                         FontFamily="Consolas" FontSize="12"/>
            </Grid>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Height="28" Background="{DynamicResource GlobalBackground}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0">
                <TextBlock Text="总条数:" Margin="0,0,5,0"/>
                <TextBlock x:Name="TotalCountText" FontWeight="Bold" Margin="0,0,20,0"/>
                <TextBlock Text="当前显示:" Margin="0,0,5,0"/>
                <TextBlock x:Name="DisplayCountText" FontWeight="Bold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
