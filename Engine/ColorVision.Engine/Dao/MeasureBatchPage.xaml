<Page x:Class="ColorVision.Engine.MeasureBatchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:media="clr-namespace:ColorVision.ImageEditor;assembly=ColorVision.ImageEditor"
      xmlns:properties="clr-namespace:ColorVision.Engine.Properties"
      xmlns:se="clr-namespace:ColorVision.Engine.Services"
      mc:Ignorable="d"
      d:DesignHeight="720" d:DesignWidth="1280"
      Title="{x:Static properties:Resources.BatchResults}"
      Initialized="Page_Initialized"
      Loaded="Page_Loaded"
      Unloaded="Page_Unloaded">
    <Page.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        </Style>
        <Style x:Key="ResultSurfaceBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="5" />
            <Setter Property="ClipToBounds" Value="True" />
        </Style>
    </Page.Resources>

    <Grid Background="{DynamicResource SecondaryRegionBrush}">
        <TabControl x:Name="ResultTabs"
                    Margin="8"
                    Background="Transparent"
                    BorderThickness="0"
                    SelectionChanged="ResultTabs_SelectionChanged">
            <TabItem>
                <TabItem.Header>
                    <StackPanel Margin="8,3" Orientation="Horizontal">
                        <TextBlock Margin="0,0,7,0" FontFamily="Segoe MDL2 Assets" FontSize="15" Text="&#xEB9F;" />
                        <TextBlock Text="{x:Static properties:Resources.Image}" />
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition MinWidth="360" Width="2*" />
                        <ColumnDefinition Width="6" />
                        <ColumnDefinition MinWidth="420" Width="3*" />
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource ResultSurfaceBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Padding="12,0" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                <DockPanel>
                                    <TextBlock FontWeight="SemiBold" Text="图像结果" />
                                    <TextBlock Margin="10,0,0,0" HorizontalAlignment="Right" Opacity="0.65">
                                        <Run Text="(" /><Run Text="{Binding Items.Count, ElementName=listView1, Mode=OneWay}" /><Run Text=")" />
                                    </TextBlock>
                                </DockPanel>
                            </Border>
                            <ListView x:Name="listView1"
                                      Grid.Row="1"
                                      Padding="0"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      SelectionChanged="listView1_SelectionChanged"
                                      MouseDoubleClick="listView1_MouseDoubleClick"
                                      PreviewKeyDown="listView1_PreviewKeyDown">
                                <ListView.Resources>
                                    <ContextMenu x:Key="ItemContextMenu" Opened="ContextMenu_Opened" />
                                </ListView.Resources>
                                <ListView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource ListViewItemBaseStyle.Small}" TargetType="{x:Type ListViewItem}">
                                        <Setter Property="ContextMenu" Value="{Binding ContextMenu}" />
                                        <Setter Property="Opacity" Value="1" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFileExists}" Value="False">
                                                <Setter Property="Opacity" Value="0.7" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <GridView.ColumnHeaderContainerStyle>
                                            <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                                                <EventSetter Event="Click" Handler="GridViewColumnSort" />
                                            </Style>
                                        </GridView.ColumnHeaderContainerStyle>
                                        <GridViewColumn Width="50" Header="{x:Static properties:Resources.SerialNumber1}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding Id}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="70" Header="{x:Static properties:Resources.Result}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding ResultCode}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="120" Header="{x:Static properties:Resources.Info}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding ResultMsg}" ToolTip="{Binding ResultMsg}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="220" Header="{x:Static properties:Resources.File}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding FilePath}" ToolTip="{Binding FilePath}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="180" Header="{x:Static properties:Resources.ImageInfo}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding ImgFrameInfo}" ToolTip="{Binding ImgFrameInfo}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="100" Header="{x:Static properties:Resources.Duration}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding TotalTime}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="220" Header="{x:Static properties:Resources.Parameter}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding ReqParams}" ToolTip="{Binding ReqParams}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="100" Header="DeviceCode">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultImage}">
                                                    <TextBlock Text="{Binding DeviceCode}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="Transparent" />

                    <Border Grid.Column="2" Style="{StaticResource ResultSurfaceBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Padding="12,0" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                <TextBlock FontWeight="SemiBold" Text="{x:Static properties:Resources.QuickPreview}" />
                            </Border>
                            <media:ImageView x:Name="imagePreview" Grid.Row="1" Background="{DynamicResource RegionBrush}" />
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem>
                <TabItem.Header>
                    <StackPanel Margin="8,3" Orientation="Horizontal">
                        <TextBlock Margin="0,0,7,0" FontFamily="Segoe MDL2 Assets" FontSize="15" Text="&#xE9D2;" />
                        <TextBlock Text="算法结果" />
                    </StackPanel>
                </TabItem.Header>
                <Grid Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition MinWidth="360" Width="2*" />
                        <ColumnDefinition Width="6" />
                        <ColumnDefinition MinWidth="420" Width="3*" />
                    </Grid.ColumnDefinitions>

                    <Border Style="{StaticResource ResultSurfaceBorderStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="36" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Border Padding="12,0" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                <DockPanel>
                                    <TextBlock FontWeight="SemiBold" Text="算法结果" />
                                    <TextBlock Margin="10,0,0,0" HorizontalAlignment="Right" Opacity="0.65">
                                        <Run Text="(" /><Run Text="{Binding Items.Count, ElementName=listView2, Mode=OneWay}" /><Run Text=")" />
                                    </TextBlock>
                                </DockPanel>
                            </Border>
                            <ListView x:Name="listView2"
                                      Grid.Row="1"
                                      Padding="0"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      SelectionChanged="listView1_SelectionChanged"
                                      PreviewMouseDoubleClick="listView2_PreviewMouseDoubleClick"
                                      PreviewKeyDown="listView1_PreviewKeyDown">
                                <ListView.Resources>
                                    <ContextMenu x:Key="ItemContextMenu" Opened="ContextMenu_Opened" />
                                </ListView.Resources>
                                <ListView.ItemContainerStyle>
                                    <Style BasedOn="{StaticResource ListViewItemBaseStyle.Small}" TargetType="{x:Type ListViewItem}">
                                        <Setter Property="ContextMenu" Value="{Binding ContextMenu}" />
                                        <Setter Property="Opacity" Value="1" />
                                        <EventSetter Event="ContextMenuOpening" Handler="AlgorithmResult_ContextMenuOpening" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsFileExists}" Value="False">
                                                <Setter Property="Opacity" Value="0.7" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.View>
                                    <GridView>
                                        <GridView.ColumnHeaderContainerStyle>
                                            <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="BorderThickness" Value="0" />
                                                <Setter Property="ContextMenu" Value="{StaticResource ItemContextMenu}" />
                                                <EventSetter Event="Click" Handler="GridViewColumnSort" />
                                            </Style>
                                        </GridView.ColumnHeaderContainerStyle>
                                        <GridViewColumn Width="50" Header="{x:Static properties:Resources.SerialNumber1}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding Id}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="110" Header="ResultType">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding ResultType}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="150" Header="ResultDesc">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding ResultDesc}" ToolTip="{Binding ResultDesc}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="160" Header="{x:Static properties:Resources.BatchNumber}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding Batch}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="220" Header="{x:Static properties:Resources.File}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding FilePath}" ToolTip="{Binding FilePath}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="140" Header="{x:Static properties:Resources.Template}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding POITemplateName}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="160" Header="img_result">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding ResultImagFile}" ToolTip="{Binding ResultImagFile}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="140" Header="{x:Static properties:Resources.CreateTime}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding CreateTime, StringFormat=yyyy/MM/dd HH:mm:ss}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                        <GridViewColumn Width="100" Header="{x:Static properties:Resources.Duration}">
                                            <GridViewColumn.CellTemplate>
                                                <DataTemplate DataType="{x:Type se:ViewResultAlg}">
                                                    <TextBlock Text="{Binding TotalTime}" />
                                                </DataTemplate>
                                            </GridViewColumn.CellTemplate>
                                        </GridViewColumn>
                                    </GridView>
                                </ListView.View>
                            </ListView>
                        </Grid>
                    </Border>

                    <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="Transparent" />

                    <Grid Grid.Column="2">
                        <Grid.RowDefinitions>
                            <RowDefinition MinHeight="260" Height="3*" />
                            <RowDefinition Height="6" />
                            <RowDefinition MinHeight="150" Height="2*" />
                        </Grid.RowDefinitions>
                        <Border Style="{StaticResource ResultSurfaceBorderStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border Padding="12,0" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                    <TextBlock FontWeight="SemiBold" Text="图像渲染" />
                                </Border>
                                <media:ImageView x:Name="algorithmImagePreview" Grid.Row="1" Background="{DynamicResource RegionBrush}" />
                            </Grid>
                        </Border>

                        <GridSplitter Grid.Row="1" Height="6" HorizontalAlignment="Stretch" Background="Transparent" />

                        <Border Grid.Row="2" Style="{StaticResource ResultSurfaceBorderStyle}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="36" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <Border Padding="12,0" Background="{DynamicResource SecondaryRegionBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                    <TextBlock FontWeight="SemiBold" Text="结果数据" />
                                </Border>
                                <Grid Grid.Row="1">
                                    <ListView x:Name="algorithmDetailListView" Padding="0" Background="Transparent" BorderThickness="0">
                                        <ListView.View>
                                            <GridView>
                                                <GridView.ColumnHeaderContainerStyle>
                                                    <Style BasedOn="{StaticResource GridViewColumnHeaderBase}" TargetType="{x:Type GridViewColumnHeader}">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                        <Setter Property="Background" Value="Transparent" />
                                                        <Setter Property="BorderThickness" Value="0" />
                                                    </Style>
                                                </GridView.ColumnHeaderContainerStyle>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                    <TextBox x:Name="algorithmDetailTextBox"
                                             Padding="8"
                                             Background="{DynamicResource RegionBrush}"
                                             BorderThickness="0"
                                             IsReadOnly="True"
                                             ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                                             TextWrapping="WrapWithOverflow"
                                             Visibility="Collapsed" />
                                </Grid>
                            </Grid>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Page>
