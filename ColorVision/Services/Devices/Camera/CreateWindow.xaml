<Window x:Class="ColorVision.Services.Devices.Camera.CreateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision.Services.Devices.Camera"
        xmlns:hc ="https://handyorg.github.io/handycontrol"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:TerminalCamera}"
        Title="CreateWindow" Height="600" Width="400" Initialized="Window_Initialized">
    <Grid>
        <StackPanel>
            <StackPanel  Margin="0,5" >
                <DockPanel Margin="2" >
                    <TextBlock Text="标识" MinWidth="30"/>
                    <hc:ComboBox IsEditable="True" SelectedIndex="0" Text="{Binding CreatName}"  hc:InfoElement.ShowClearButton="True" Margin="0,0,10,0"  />
                </DockPanel>
                <DockPanel Margin="2" >
                    <TextBlock Text="名称" Width="30"/>
                    <hc:ComboBox  IsEditable="True" SelectedIndex="0" Text="{Binding CreatCode}"  hc:InfoElement.ShowClearButton="True" Margin="0,0,10,0"  />
                </DockPanel>
                <Button Content="创建" Command="{Binding CreateCommand}"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
