<Window x:Class="ColorVision.Copilot.CopilotExceptionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:properties="clr-namespace:ColorVision.Properties"
        Title="{x:Static properties:Resources.CopilotExceptionDiagnosis}"
        Width="860"
        Height="680"
        MinWidth="720"
        MinHeight="520"
        ShowInTaskbar="False"
        Background="{DynamicResource GlobalBackground}"
        mc:Ignorable="d">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Padding="16"
                Background="{DynamicResource LayoutBackground}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel>
                <TextBlock FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource GlobalTextBrush}"
                           Text="{x:Static properties:Resources.CopilotUnhandledException}" />
                <TextBlock Margin="0,8,0,0"
                           FontSize="15"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource GlobalTextBrush}"
                           Text="{Binding ExceptionTitle}"
                           TextWrapping="Wrap" />
                <TextBlock Margin="0,8,0,0"
                           Foreground="{DynamicResource GlobalTextBrush}"
                           Opacity="0.78"
                           Text="{Binding OccurredSummary}"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Margin="0,12,0,12"
                Padding="12,10"
                Background="{DynamicResource LayoutBackground}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="6">
            <TextBlock Foreground="{DynamicResource GlobalTextBrush}"
                       Text="{Binding DispatchStatus}"
                       TextWrapping="Wrap" />
        </Border>

        <TabControl Grid.Row="2">
            <TabItem Header="{x:Static properties:Resources.CopilotExceptionDetails}">
                <TextBox Margin="12"
                         FontFamily="Consolas"
                         Foreground="{DynamicResource GlobalTextBrush}"
                         Background="{DynamicResource GlobalBackground}"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding ExceptionDetails}"
                         TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </TabItem>
            <TabItem Header="{x:Static properties:Resources.CopilotRecentLogs}">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="RecentLinesRadioButton"
                                     Margin="0,0,12,0"
                                     VerticalAlignment="Center"
                                     Checked="RecentLinesRadioButton_Checked"
                                     Content="{x:Static properties:Resources.CopilotRecentNLines}"
                                     IsChecked="True" />
                        <TextBox x:Name="RecentLineCountTextBox"
                                 Width="72"
                                 VerticalContentAlignment="Center"
                                 Text="160" />
                        <TextBlock Margin="8,0,12,0"
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource GlobalTextBrush}"
                                   Text="{x:Static properties:Resources.CopilotLines}" />
                        <RadioButton x:Name="FullDayRadioButton"
                                     Margin="0,0,12,0"
                                     VerticalAlignment="Center"
                                     Checked="FullDayRadioButton_Checked"
                                     Content="{x:Static properties:Resources.CopilotTodayLog}" />
                        <Button Padding="12,4"
                                Click="RefreshLogButton_Click"
                                Content="{x:Static properties:Resources.CopilotRefreshLog}" />
                    </StackPanel>

                    <TextBlock Grid.Row="1"
                               Margin="0,10,0,0"
                               Foreground="{DynamicResource GlobalTextBrush}"
                               Text="{Binding RecentLogHeader}"
                               TextWrapping="Wrap" />

                    <TextBox Grid.Row="2"
                             Margin="0,10,0,0"
                             FontFamily="Consolas"
                             Foreground="{DynamicResource GlobalTextBrush}"
                             Background="{DynamicResource GlobalBackground}"
                             BorderThickness="0"
                             IsReadOnly="True"
                             Text="{Binding RecentLogContent}"
                             TextWrapping="NoWrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Auto" />
                </Grid>
            </TabItem>
            <TabItem Header="{x:Static properties:Resources.CopilotAiRequest}">
                <TextBox Margin="12"
                         FontFamily="Consolas"
                         Foreground="{DynamicResource GlobalTextBrush}"
                         Background="{DynamicResource GlobalBackground}"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding AiPromptPreview}"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto" />
            </TabItem>
        </TabControl>

        <StackPanel Grid.Row="3"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Margin="0,0,8,0"
                    Padding="14,6"
                    Click="CopyDetailsButton_Click"
                    Content="{x:Static properties:Resources.CopilotCopyDetails}" />
            <Button Margin="0,0,8,0"
                    Padding="14,6"
                    Click="AskAiButton_Click"
                    Content="{x:Static properties:Resources.CopilotAskAi}"
                    IsEnabled="{Binding CanAskAi}" />
                <Button Margin="0,0,8,0"
                    Padding="14,6"
                    Click="SearchGoogleButton_Click"
                    Content="{x:Static properties:Resources.CopilotGoogleSearch}"
                    IsEnabled="{Binding CanSearchGoogle}" />
            <Button Padding="14,6"
                    Click="CloseButton_Click"
                    Content="{x:Static properties:Resources.Close}" />
        </StackPanel>
    </Grid>
</Window>