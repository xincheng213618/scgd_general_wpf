<Window x:Class="ColorVision.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision"
        xmlns:effects="http://gu.se/Geometry"
        xmlns:ScottPlot="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"
        xmlns:winf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="ColorVision" Height="720" Width="1280"  Initialized="Window_Initialized">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Theme/Base.xaml"/>
                <ResourceDictionary Source="/Theme/Listview.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Menu x:Name="Menu1">
                <MenuItem Header="文件(_F)">
                </MenuItem>
                <MenuItem Header="模板(_T)">
                    <MenuItem Header="_AOI参数"  Click="MenuItem_Click_2"/>
                </MenuItem>
                <MenuItem Header="MQTTLog(_M)" Click="MenuItem_Click">
                </MenuItem>
                <MenuItem Header="PlotTest(_P)" Click="MenuItem1_Click">
                </MenuItem>
                <MenuItem Header="帮助(_H)">
                    <MenuItem Header="关于(_A)" Click="MenuItem_Click_1"/>
                </MenuItem>
            </Menu>
        </Grid>
        <Grid x:Name="ContentGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <Grid  Grid.Row="0" Background="#F6F6F7">
                <StackPanel>
                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton0"  Style="{StaticResource ButtonPageControl1}" Content="PG"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton0,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    
                    

                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton1"  Style="{StaticResource ButtonPageControl1}" Content="相机设置"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton1,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                                <DockPanel>
                                    <Label Content="相机类型："/>
                                    <ComboBox  x:Name="ComboxCameraType"  SelectedValuePath="Key" Height="20" DisplayMemberPath="Value"/>
                                </DockPanel>
                                <UniformGrid >
                                    <Button  Content="InitCamere" Click="SendDemo_Click"/>
                                    <Button  Content="AddCalibration" Click="SendDemo1_Click"/>
                                    <Button  Content="OpenCamere" Click="SendDemo2_Click"/>
                                    <Button  Content="GetData" Click="SendDemo3_Click"/>
                                </UniformGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton2"  Style="{StaticResource ButtonPageControl1}" Content="校正"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton2,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton3"  Style="{StaticResource ButtonPageControl1}" Content="光谱仪操作"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton3,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton4"  Style="{StaticResource ButtonPageControl1}" Content="源表设置"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton4,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                                <DockPanel>
                                    <Label Content="："/>
                                </DockPanel>
                                <UniformGrid >

                                </UniformGrid>
                            </StackPanel>
                        </Border>
                    </StackPanel>


                    <StackPanel Margin="0,5">
                        <ToggleButton  x:Name="ToggleButton999"  Style="{StaticResource ButtonPageControl1}" Content="测试"  FontSize="15" Foreground="Black" IsChecked="false" />
                        <Border Visibility="{Binding ElementName=ToggleButton999,Path=IsChecked,Converter={StaticResource bool2VisibilityConverter1}}"  Background="#E8E8E9" Margin="5,2" CornerRadius="5">
                            <StackPanel Margin="5">
                                <UniformGrid >
                                    <Button Content="打开测试图像"  Click="Button_Click"/>
                                    <Button Content="绘制测试图形"  Click="Button1_Click"/>
                                    <Button Content="读取CFG测试"  Click="Button3_Click"/>
                                    <Button Content="保存CFG测试"  Click="Button4_Click"/>
                                </UniformGrid>

                            </StackPanel>
                        </Border>
                    </StackPanel>


    
                </StackPanel>
            </Grid>


            <Grid Grid.Column="2" >
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="200"/>
                </Grid.RowDefinitions>
                <WindowsFormsHost x:Name="WindowsFormsHost1" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display"   Grid.Row="1" Visibility="Visible"  >
                    <winf:PropertyGrid x:Name="PropertyGrid2" Height="200"   >
                    </winf:PropertyGrid>
                </WindowsFormsHost>
                <WindowsFormsHost x:Name="WindowsFormsHost2" UseLayoutRounding="True" TextOptions.TextFormattingMode="Display"   Grid.Row="1" Visibility="Visible" Margin="0,145,0,0"  >

                </WindowsFormsHost>
                <DockPanel Grid.Row="0">
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal">
                        <Grid Width="50">
                            <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="序号"  />
                        </Grid>
                        <Grid Width="50">
                            <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="选中"  />
                        </Grid>
                        <Grid Width="100">
                            <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="位置"  />
                        </Grid>
                        <Grid Width="50">
                            <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="角度"  />
                        </Grid>
                    </StackPanel>
                    <ListView   x:Name="ListView1" ScrollViewer.HorizontalScrollBarVisibility="Disabled"  ManipulationBoundaryFeedback="SCManipulationBoundaryFeedback"  SelectionChanged="ListView1_SelectionChanged" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate >
                                            <TextBlock Text="{Binding Path=ID}" HorizontalAlignment="Center"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate >
                                            <CheckBox IsChecked="True" Click="CheckBox_Click" Tag="{Binding .}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="100">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate >
                                            <TextBlock  >
                                                    <Run Text="("/>
                                                    <Run Text="{Binding Path = CenterX,StringFormat={}{0:F0}}"/>
                                                    <Run Text=","/>
                                                    <Run Text="{Binding Path = CenterY,StringFormat={}{0:F0}}"/>
                                                    <Run Text=")"/>
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn Width="50">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate >
                                            <TextBlock Text="{Binding Path=Radius}">
                                            </TextBlock>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>

                </DockPanel>


            </Grid>

            <Grid x:Name="ZoomGrid"  Grid.Column="1" Background="#c3c3c3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Height="0.5" Background="#F0F0F0"/>


                <local:ZoomboxSub x:Name="Zoombox1"  MaxZoom="10" MinZoom="0.01" Grid.Row="1" Grid.ColumnSpan="2">
                    <local:DrawCanvas x:Name="ImageShow" MouseLeave="ImageShow_MouseLeave"  MouseEnter="ImageShow_MouseEnter"  MouseDown="ImageShow_MouseDown"  PreviewMouseUp="ImageShow_MouseUp"  MouseWheel="ImageShow_MouseWheel" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.ClearTypeHint="Enabled" MouseMove="ImageShow_MouseMove"/>
                </local:ZoomboxSub>
                <StackPanel Grid.Row="0" Grid.RowSpan="2" >
                    <ToolBar x:Name="ToolBar1" DataContext="{Binding ToolBarTop}"   Loaded="ToolBar1_Loaded" Margin="0,2,4,0" Background="#77FFFFFF" HorizontalAlignment="Center">
                        <ToolBar.Resources>
                            <ResourceDictionary Source="/Theme/Icons.xaml"/>
                        </ToolBar.Resources>
                        <ToggleButton x:Name="ToggleButtonMouse" Height="30" Width="30" IsChecked="{Binding Path=Move}">
                            <Image Height="22" Width="22" Stretch="Uniform" Source="{StaticResource DrawingImageMouse}"/>
                        </ToggleButton>
                        <ToggleButton x:Name="ToggleButtonDrag" IsChecked="{Binding Activate}" ToolTip="可以按下Ctrl缩放和移动图像">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagedrag}"/>
                        </ToggleButton>
                        <Button Command="{Binding ZoomUniform}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImage1_1}" />
                        </Button>
                        <Button Command="{Binding ZoomIncrease}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagezoom_in}" />
                        </Button>
                        <Button  Command="{Binding ZoomDecrease}">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagezoom_out}" />
                        </Button>
                        <Button Command="{Binding ZoomNone}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageexpend}" />
                        </Button>
                        <TextBox  Background="Transparent"  Width="50" Text="{Binding ElementName=Zoombox1,Path=ContentMatrix.M11,StringFormat={}{0:F2}}" />
                        <ToggleButton  Click="Button5_Click">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagegridlines}"/>
                        </ToggleButton>
                        <ToggleButton  Click="Button6_Click">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageruler}"/>
                        </ToggleButton>
                        <ToggleButton Height="30" Width="30" Click="Button7_Click">
                            <Image Height="22" Width="22" Stretch="Uniform" Source="{StaticResource DrawingImageeraser}"/>
                        </ToggleButton>
                        <ToggleButton Click="Button8_Click">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageMax}"/>
                        </ToggleButton>
                    </ToolBar>
                </StackPanel>
                <local:InkVisual  Grid.Row="1"  Visibility="Hidden" x:Name="InkVisual"/>
            </Grid>



            <GridSplitter Grid.Column="0"  Width="1" />
            <GridSplitter  Grid.Column="2"  Width="1" HorizontalAlignment="Left" />
        </Grid>
        <Grid Grid.Row="2">
            <StatusBar  DataContext="{Binding performanceSetting}">
                <StatusBarItem   x:Name="StatusBarItem1"  >
                    <TextBlock>
                        <Run Text="{Binding Path=MemoryThis}"/>
                    </TextBlock>
                </StatusBarItem>


                <StatusBarItem x:Name="MQTTStatusBarItem" DataContext="{Binding MQTTControl}">
                    <TextBlock >
                        <Run Text="MQTT："/>
                        <Run Text="{Binding Path=IsConnect,Mode=OneWay}"/>
                    </TextBlock>
                </StatusBarItem>
                <StatusBarItem  x:Name="StatusBarItem2" DataContext="{Binding performanceSetting}" HorizontalAlignment="Right" Content="{Binding Path=Time}">
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
