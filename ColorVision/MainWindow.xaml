<Window x:Class="ColorVision.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorVision"
        xmlns:effects="http://gu.se/Geometry"
        xmlns:winf="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="视彩配置窗口" Height="720" Width="1280">
    <Grid x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Menu x:Name="Menu1">
                <MenuItem Header="文件(_F)">
                </MenuItem>
                <MenuItem Header="MQTTDemo(_M)" Click="MenuItem_Click">
                </MenuItem>
                <MenuItem Header="帮助(_H)">
                    <MenuItem Header="关于(_A)" Click="MenuItem_Click_1"/>
                </MenuItem>
            </Menu>
        </Grid>
        <Grid x:Name="ContentGrid" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" >
                <StackPanel Orientation="Horizontal">
                    <Button Content="打开测试图像"  Click="Button_Click"/>
                    <Button Content="绘制测试图形"  Click="Button1_Click"/>
                    <Button Content="读取CFG测试"  Click="Button3_Click"/>
                    <Button Content="保存CFG测试"  Click="Button4_Click"/>
                    <Button Content="打开相机"  Click="Button4_Click"/>
                    <Button Content="拍摄图片"  Click="Button4_Click"/>
                </StackPanel>
                <ScrollViewer  Height="200" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ListView  x:Name="ListView1"  ScrollViewer.HorizontalScrollBarVisibility="Disabled"  ScrollViewer.VerticalScrollBarVisibility="Auto" ManipulationBoundaryFeedback="SCManipulationBoundaryFeedback" >
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Width="{Binding ElementName=ListView1,Path=ActualWidth}">
                                    <GridViewColumn.CellTemplate  >
                                        <DataTemplate >
                                            <Border x:Name="border1"  Height="20" Margin="0,2,0,2" BorderThickness="0" Width="{Binding ElementName=ListView1,Path=ActualWidth}"   >
                                                <Grid Margin="0,2,30,2" >
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="30"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <StackPanel Grid.Column="1" Margin="2" Orientation="Horizontal">
                                                        <CheckBox IsChecked="True" Click="CheckBox_Click" Tag="{Binding .}"/>
                                                        <TextBlock Text="{Binding Path = CenterX }" />
                                                        <TextBlock Text="{Binding Path = CenterY }" />
                                                    </StackPanel>
                                                </Grid>

                                            </Border>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </ScrollViewer>

                <WindowsFormsHost x:Name="WindowsFormsHost1" Height="200" >
                    <winf:PropertyGrid x:Name="PropertyGrid2">
                    </winf:PropertyGrid>
                </WindowsFormsHost>

                <WpfPlot Height="155"/>
            </StackPanel>
            <Grid x:Name="ZoomGrid"  Grid.Column="1" Background="#c3c3c3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0" Height="0.5" Background="#F0F0F0">
                   
                </Grid>
                <local:ZoomboxSub x:Name="Zoombox1"  MaxZoom="10" MinZoom="0.01" Grid.Row="1" Grid.ColumnSpan="2">
                    <local:DrawCanvas x:Name="ImageShow" MouseLeave="ImageShow_MouseLeave"  MouseEnter="ImageShow_MouseEnter"  MouseDown="ImageShow_MouseDown"  PreviewMouseUp="ImageShow_MouseUp"  MouseWheel="ImageShow_MouseWheel" RenderOptions.BitmapScalingMode="NearestNeighbor" RenderOptions.ClearTypeHint="Enabled" MouseMove="ImageShow_MouseMove"/>
                </local:ZoomboxSub>
                <StackPanel Grid.Row="0" Grid.RowSpan="2" >
                    <ToolBar x:Name="ToolBar1" DataContext="{Binding ToolBarTop}" Loaded="ToolBar1_Loaded" Margin="0,2,4,0" Background="#77FFFFFF" HorizontalAlignment="Center">
                        <ToolBar.Resources>
                            <ResourceDictionary Source="/Theme/Icons.xaml"/>
                        </ToolBar.Resources>
                        <ToggleButton x:Name="ToggleButtonMouse" Height="30" Width="30" Click="ToggleButton_Click"  ToolTip="可以按下Ctrl缩放和移动图像">
                            <Image Height="22" Width="22" Stretch="Uniform" Source="{StaticResource DrawingImageMouse}"/>
                        </ToggleButton>
                        <ToggleButton x:Name="ToggleButtonDrag" Click="ToggleButton_Click"  ToolTip="可以按下Ctrl缩放和移动图像">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagedrag}"/>
                        </ToggleButton>
                        <Button Command="{Binding ZoomUniform}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImage1_1}" />
                        </Button>
                        <Button Command="{Binding ZoomIncrease}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagezoom_in}" />
                        </Button>
                        <Button  Command="{Binding ZoomDecrease}">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagezoom_out}" />
                        </Button>
                        <Button Command="{Binding ZoomNone}">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageexpend}" />
                        </Button>
                        <TextBox  Background="Transparent"  Width="50" Text="{Binding ElementName=Zoombox1,Path=ContentMatrix.M11,StringFormat={}{0:F2}}" />
                        <ToggleButton  Click="Button5_Click">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImagegridlines}"/>
                        </ToggleButton>
                        <ToggleButton  Click="Button6_Click">
                            <Image  Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageruler}"/>
                        </ToggleButton>
                        <ToggleButton Height="30" Width="30" Click="Button7_Click">
                            <Image Height="22" Width="22" Stretch="Uniform" Source="{StaticResource DrawingImageeraser}"/>
                        </ToggleButton>
                        <ToggleButton Click="Button8_Click">
                            <Image Height="25" Width="25" Stretch="Uniform" Source="{StaticResource DrawingImageMax}"/>
                        </ToggleButton>
                    </ToolBar>
                </StackPanel>


                <local:InkVisual  Grid.Row="1"  Visibility="Hidden" x:Name="InkVisual"/>
            </Grid>


        </Grid>
        <Grid Grid.Row="2">
            <StatusBar x:Name="StatusBar1" DataContext="{Binding Path=performanceSetting}" >
                <StatusBarItem >
                    <TextBlock>
                        <Run Text="{Binding Path=MemoryThis}"/>
                    </TextBlock>
                </StatusBarItem>
                <StatusBarItem HorizontalAlignment="Right" Content="{Binding Path=Time}">
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</Window>
