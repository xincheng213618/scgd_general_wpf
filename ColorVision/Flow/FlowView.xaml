<UserControl x:Class="ColorVision.Flow.FlowView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorVision.Flow"
             xmlns:st ="clr-namespace:ST.Library.UI.NodeEditor;assembly=ST.Library.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Initialized="UserControl_Initialized" SizeChanged="UserControl_SizeChanged" >
    <Grid>
        <Grid.ColumnDefinitions >
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Grid.ColumnSpan="2">
            
            <WindowsFormsHost x:Name="winf1" >
                <st:STNodeEditor  x:Name="STNodeEditorMain" EnableEdit="False"/>
            </WindowsFormsHost>
        </Grid>
        <Grid Grid.Column="1">
            <WindowsFormsHost x:Name="winf2" Visibility="Collapsed" Width="200">
                <st:STNodePropertyGrid x:Name="STNodePropertyGrid1" />
            </WindowsFormsHost>
        </Grid>
    </Grid>
</UserControl>
