<UserControl x:Class="ColorVision.MQTT.Service.MQTTServiceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ColorVision.MQTT.Service"
             xmlns:mqtt="clr-namespace:ColorVision.MQTT"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:MQTTService}"
             d:DesignHeight="500" d:DesignWidth="600" Initialized="UserControl_Initialized">
    <UserControl.Resources>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="ToolTip" Value="{ Binding Text,RelativeSource={RelativeSource Self}}"/>
        </Style>
    </UserControl.Resources>
    <Grid Height="500">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,5">
            <Grid  x:Name="MQTTShowContent" Grid.Row="0" Margin="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <Border Margin="2" Background ="{DynamicResource GlobalBackground}" CornerRadius="5">
                    <StackPanel Margin="5">
                        <DockPanel Margin="5,2">
                            <TextBlock Text="{Binding ServiceConfig.Name}" FontWeight="Bold" FontSize="20"/>
                            <ToggleButton  Style="{StaticResource ButtonMQTTConnect}"  Height="20" Width="20"  HorizontalAlignment="Right" IsChecked="{Binding  ServiceConfig.IsAlive,Mode=OneWay}" IsEnabled="False" />
                        </DockPanel>
                        <GroupBox Header="基本信息" Margin="0,5">
                            <StackPanel Margin="5">
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="服务代码:" Width="120"/>
                                    <TextBlock Text="{Binding SysResourceModel.Code}"/>
                                </DockPanel>
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="发送信道:" Width="120"/>
                                    <TextBlock Text="{Binding ServiceConfig.SendTopic}"/>
                                </DockPanel>
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="接收信道:" Width="120"/>
                                    <TextBlock Text="{Binding ServiceConfig.SubscribeTopic}"/>
                                </DockPanel>
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="上次连接:" Width="120"/>
                                    <TextBlock Text="{Binding ServiceConfig.LastAliveTime}"/>
                                </DockPanel>
                                <DockPanel Margin="0,2">
                                    <TextBlock Text="心跳:" Width="120"/>
                                    <TextBlock Text="{Binding ServiceConfig.HeartbeatTime}"/>
                                </DockPanel>
                            </StackPanel>
                        </GroupBox>

                    </StackPanel>

                </Border>
                <Button  Grid.Column="1" Click="Button_Click_1" Margin="2">
                    <TextBlock Text="编辑服务" TextWrapping="Wrap" />
                </Button>
                <Button  Grid.Column="2" Click="Button_Click_2"  VerticalAlignment="Stretch" Margin="2">
                    <TextBlock Text="添加资源" TextWrapping="Wrap" />
                </Button>
            </Grid>
            <Grid  x:Name="MQTTEditContent" Visibility="Collapsed" Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <StackPanel >
                    <DockPanel Margin="5" >
                        <TextBlock Text="服务名称" Width="120"/>
                        <TextBox Text="{Binding Name}"/>
                    </DockPanel>
                    <DockPanel Margin="5" >
                        <TextBlock Text="服务Code" Width="120"/>
                        <TextBox Text="{Binding SysResourceModel.Code}"/>
                    </DockPanel>
                    <DockPanel Margin="5">
                        <TextBlock Text="上次连接:" Width="120"/>
                        <TextBox Text="{Binding ServiceConfig.HeartbeatTime}"/>
                    </DockPanel>
                </StackPanel>
                <Button Grid.Column="1"  Content="提交" Click="Button_Click" Command="{Binding SaveCommand}" />
            </Grid>

        </Grid>
        <GroupBox x:Name="MQTTCreate" Header="创建资源" Grid.Row="2" Visibility="Collapsed">
            <StackPanel x:Name="MQTTCreateStackPanel"  Grid.Column="1" Grid.Row="1" Margin="5">
                <DockPanel Margin="5" >
                    <TextBlock Text="类型" Width="60"/>
                    <ComboBox x:Name="TextBox_Type" SelectedValuePath="Name"  DisplayMemberPath="Name" />
                </DockPanel>
                <DockPanel Margin="5" >
                    <TextBlock Text="标识" Width="60"/>
                    <TextBox x:Name="TextBox_Code"/>
                </DockPanel>
                <DockPanel Margin="5" >
                    <TextBlock Text="名称" Width="60"/>
                    <TextBox x:Name="TextBox_Name" />
                </DockPanel>
                <Button   Click="Button_New_Click" Content="添加资源" />
            </StackPanel>
        </GroupBox>

        <ListView  x:Name="ListViewService" Grid.Row="1">
            <ListView.Resources >
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource ListViewItemBaseStyle}">
                    <Setter Property="ContextMenu" Value="{Binding ContextMenu}"/>
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.View >
                <GridView>
                    <GridViewColumn  Header="设备名称">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate  DataType="{x:Type mqtt:BaseDevice}">
                                <TextBlock Text="{ Binding Name}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn  Header="是否存活">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate  DataType="{x:Type mqtt:BaseDevice}">
                                <TextBlock Text="{ Binding  IsAlive}"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
