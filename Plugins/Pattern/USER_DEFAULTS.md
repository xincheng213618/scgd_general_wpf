# Pattern 用户默认值

用户默认文件的路径、按钮行为、损坏 JSON 处理，以及它与当前参数和模板的区别，统一维护在[用户默认值与当前参数](../../docs/04-api-reference/plugins/standard-plugins/pattern.md#用户默认值与当前参数)。

源码入口为 `PatternUserDefaultManager.cs` 和 `PatternWindow.xaml.cs`。本页是源码仓库定位入口，不随当前项目的输出元数据复制；包内必需说明见 [README](README.md)，完整契约需读取匹配版本的仓库文档。
