<UserControl x:Class="Conoscope.ConoscopeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Conoscope="clr-namespace:Conoscope.Core"
    xmlns:local="clr-namespace:Conoscope"
        xmlns:properties="clr-namespace:Conoscope.Properties"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Conoscope:ConoscopeManager}"
        Background="{DynamicResource GlobalBackground}" Initialized="Window_Initialized">
    <UserControl.Resources>
        <Style x:Key="PanelHeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{DynamicResource GlobalTextBrush}"/>
        </Style>
        <Style x:Key="PanelMutedTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="Gray"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="PanelCompactButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="9,3"/>
            <Setter Property="MinHeight" Value="26"/>
        </Style>
        <Style x:Key="PanelPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource PanelCompactButtonStyle}">
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="PanelCompactToggleStyle" TargetType="ToggleButton">
            <Setter Property="Padding" Value="9,3"/>
            <Setter Property="MinHeight" Value="26"/>
            <Setter Property="MinWidth" Value="68"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
        <Style x:Key="OverlayToolButtonStyle" TargetType="Button" BasedOn="{StaticResource PanelCompactButtonStyle}">
            <Setter Property="Width" Value="34"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
        <Style x:Key="OverlayToolToggleStyle" TargetType="ToggleButton">
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="MinWidth" Value="46"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Margin" Value="0"/>
        </Style>
        <DrawingBrush x:Key="TransparentGridBrush" Opacity="0.1" TileMode="Tile" Viewport="0 0 20 20" ViewportUnits="Absolute">
            <DrawingBrush.Drawing>
                <DrawingGroup>
                    <!--  Gray square  -->
                    <GeometryDrawing Brush="Gray" Geometry="M0,0 L0,10 10,10 10,0 Z" />
                    <!--  Transparent square  -->
                    <GeometryDrawing Brush="Transparent" Geometry="M10,0 L10,10 20,10 20,0 Z" />
                    <!--  Another row  -->
                    <GeometryDrawing Brush="Transparent" Geometry="M0,10 L0,20 10,20 10,10 Z" />
                    <GeometryDrawing Brush="Gray" Geometry="M10,10 L10,20 20,20 20,10 Z" />
                </DrawingGroup>
            </DrawingBrush.Drawing>
        </DrawingBrush>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="6"/>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid x:Name="ImageViewHost" Grid.Column="0" Background="{StaticResource TransparentGridBrush}">
            <local:ConoscopeImageHost x:Name="ImageView" />
            <Border HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,8,0,0" Padding="3" Background="#F2F4F4F4" BorderBrush="#CCB8B8B8" BorderThickness="1" CornerRadius="6">
                <ToolBar ToolBarTray.IsLocked="True" Background="Transparent" BorderThickness="0" Padding="0">
                    <ToggleButton x:Name="tglPanMode" Style="{StaticResource OverlayToolToggleStyle}" Content="平移" ToolTip="开启后左键拖拽可平移图像；关闭后按住 Ctrl 再平移。" IsChecked="True" Checked="tglPanMode_Checked" Unchecked="tglPanMode_Unchecked" />
                    <ToggleButton x:Name="tglReferenceInteraction" Style="{StaticResource OverlayToolToggleStyle}" Content="参考拖拽" ToolTip="允许在图上直接拖动参考方位线和极角圆；关闭后可直接选择和拖动关注点圆。" IsChecked="True" Checked="tglReferenceInteraction_Checked" Unchecked="tglReferenceInteraction_Unchecked" />
                    <Separator/>
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="-" FontSize="16" FontWeight="Bold" ToolTip="缩小" Click="ToolbarZoomOut_Click" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="+" FontSize="16" FontWeight="Bold" ToolTip="放大" Click="ToolbarZoomIn_Click" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="1:1" ToolTip="原始尺寸" Click="ToolbarZoomNone_Click" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="适" ToolTip="适应窗口" Click="ToolbarZoomUniform_Click" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="填" ToolTip="铺满窗口" Click="ToolbarZoomUniformToFill_Click" />
                    <TextBox x:Name="txtToolbarZoomRatio" Width="54" Margin="4,0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" Text="1.00" ToolTip="当前缩放倍率" PreviewKeyDown="txtToolbarZoomRatio_PreviewKeyDown" LostFocus="txtToolbarZoomRatio_LostFocus" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="全" ToolTip="切换图像全屏" Click="ToolbarFullScreen_Click" />
                    <Separator/>
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="3D" ToolTip="打开当前图像的 3D 视图" Click="ToolbarOpen3D_Click" />
                    <Button Style="{StaticResource OverlayToolButtonStyle}" Content="CIE" ToolTip="打开 CIE 色度图" Click="ToolbarOpenCie_Click" />
                </ToolBar>
            </Border>
            <Border x:Name="PseudoColorLegendPanel" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,12,0" Padding="8" Background="#DD1E1E1E" BorderBrush="#66FFFFFF" BorderThickness="1" CornerRadius="6" Visibility="Collapsed" IsHitTestVisible="False">
                <StackPanel>
                    <TextBlock x:Name="tbPseudoColorLegendTitle" Foreground="White" FontWeight="SemiBold" Text="Y" HorizontalAlignment="Center" />
                    <TextBlock x:Name="tbPseudoColorLegendMax" Margin="0,6,0,4" Foreground="White" FontSize="11" HorizontalAlignment="Center" Text="0" />
                    <Border Width="18" Height="124" BorderBrush="#55FFFFFF" BorderThickness="1">
                        <Image x:Name="imgPseudoColorLegend" Stretch="Fill" />
                    </Border>
                    <TextBlock x:Name="tbPseudoColorLegendMin" Margin="0,4,0,0" Foreground="White" FontSize="11" HorizontalAlignment="Center" Text="0" />
                </StackPanel>
            </Border>
            <Border x:Name="CoordinateDragOverlay" HorizontalAlignment="Left" VerticalAlignment="Top" Visibility="Collapsed" IsHitTestVisible="False" Background="#CC202020" BorderBrush="#FFFFD400" BorderThickness="1" CornerRadius="4" Padding="8,4">
                <TextBlock x:Name="CoordinateDragOverlayText" Foreground="White" FontWeight="SemiBold" />
            </Border>
            <Border HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,8" Padding="3" Background="#F2F4F4F4" BorderBrush="#CCB8B8B8" BorderThickness="1" CornerRadius="6">
                <ToolBar ToolBarTray.IsLocked="True" Background="Transparent" BorderThickness="0" Padding="0">
                    <ToggleButton x:Name="tglFocusCircleMode" Style="{StaticResource OverlayToolToggleStyle}" Content="编辑关注点" ToolTip="开启后使用底部工具绘制或擦除关注点；关闭后在关闭参考拖拽时可直接选择和拖动关注点。" Checked="tglFocusCircleMode_Checked" Unchecked="tglFocusCircleMode_Unchecked" />
                    <ToggleButton x:Name="tglFocusCircleDrawTool" Style="{StaticResource OverlayToolToggleStyle}" Content="圆" ToolTip="绘制关注点圆" IsChecked="True" Checked="tglFocusCircleDrawTool_Checked" Unchecked="tglFocusCircleDrawTool_Unchecked" />
                    <ToggleButton x:Name="tglFocusCircleEraseTool" Style="{StaticResource OverlayToolToggleStyle}" Content="橡皮" ToolTip="框选擦除关注点圆" Checked="tglFocusCircleEraseTool_Checked" Unchecked="tglFocusCircleEraseTool_Unchecked" />
                    <Separator/>
                    <Button x:Name="btnCalculateFocusCircles" Style="{StaticResource OverlayToolButtonStyle}" Content="算" ToolTip="计算全部关注点" Click="btnCalculateFocusCircles_Click" />
                    <Button x:Name="btnClearFocusCircles" Style="{StaticResource OverlayToolButtonStyle}" Content="清" ToolTip="清空全部关注点" Click="btnClearFocusCircles_Click" />
                </ToolBar>
            </Border>
        </Grid>

        <GridSplitter Grid.Column="1" Width="6" HorizontalAlignment="Stretch" Background="{DynamicResource BorderBrush}"/>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TabControl Grid.Row="0" MaxHeight="220" MinHeight="112">
                <TabItem Header="显示">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="5">
                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,0,6">
                                <WrapPanel Margin="0,0,0,2">
                                    <Button Content="应用当前预设" Click="btnApplyFilter_Click" Style="{StaticResource PanelPrimaryButtonStyle}" Margin="0,0,8,0"/>
                                    <Button Content="预处理设置" Click="btnOpenPreprocessSettings_Click" Style="{StaticResource PanelCompactButtonStyle}"/>
                                </WrapPanel>
                            </Border>

                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="显示与参考" Style="{StaticResource PanelHeaderTextStyle}" Margin="0,0,0,6"/>
                                    <WrapPanel Margin="0,4,0,2">
                                        <TextBlock Text="显示通道:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                        <ComboBox x:Name="cbDisplayChannel" MinWidth="120" SelectedIndex="1" SelectionChanged="DisplayChannel_SelectionChanged" Margin="0,0,14,0">
                                            <ComboBoxItem Content="X 通道" Tag="X"/>
                                            <ComboBoxItem Content="Y 通道" Tag="Y"/>
                                            <ComboBoxItem Content="Z 通道" Tag="Z"/>
                                            <ComboBoxItem Content="x 色度" Tag="CieX"/>
                                            <ComboBoxItem Content="y 色度" Tag="CieY"/>
                                            <ComboBoxItem Content="u 色度" Tag="CieU"/>
                                            <ComboBoxItem Content="v 色度" Tag="CieV"/>
                                            <ComboBoxItem Content="色差 Δuv" Tag="ColorDifference"/>
                                        </ComboBox>
                                        <TextBlock Text="参考模式:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                        <ComboBox x:Name="cbQuickReferenceMode" MinWidth="120" SelectionChanged="QuickReferenceMode_SelectionChanged">
                                            <ComboBoxItem Content="方位角直线" Tag="AzimuthLine"/>
                                            <ComboBoxItem Content="极角圆" Tag="PolarCircle"/>
                                        </ComboBox>
                                    </WrapPanel>

                                    <Grid x:Name="rowReferenceAngle" Margin="5,4,5,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="58"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="参考方位" VerticalAlignment="Center"/>
                                        <TextBox x:Name="txtQuickReferenceAngle" Grid.Column="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" PreviewKeyDown="QuickReferenceTextBox_PreviewKeyDown" LostFocus="txtQuickReferenceAngle_LostFocus"/>
                                        <Slider x:Name="sliderQuickReferenceAngle" Grid.Column="3" Minimum="0" Maximum="180" TickFrequency="1" IsSnapToTickEnabled="False" ValueChanged="QuickReferenceAngle_ValueChanged"/>
                                    </Grid>

                                    <Grid x:Name="rowReferenceRadius" Margin="5,4,5,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="58"/>
                                            <ColumnDefinition Width="8"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="参考极角" VerticalAlignment="Center"/>
                                        <TextBox x:Name="txtQuickReferenceRadius" Grid.Column="1" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" PreviewKeyDown="QuickReferenceTextBox_PreviewKeyDown" LostFocus="txtQuickReferenceRadius_LostFocus"/>
                                        <Slider x:Name="sliderQuickReferenceRadius" Grid.Column="3" Minimum="0" Maximum="80" TickFrequency="1" IsSnapToTickEnabled="False" ValueChanged="QuickReferenceRadius_ValueChanged"/>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="色差">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="5">
                            <Border x:Name="gbColorDifference" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,0,6">
                                <StackPanel>
                                    <TextBlock Text="色差计算" Style="{StaticResource PanelHeaderTextStyle}" Margin="0,0,0,6"/>
                                    <WrapPanel Margin="0,4,0,4">
                                        <TextBlock Text="基准:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                        <ComboBox x:Name="cbColorDifferenceReference" MinWidth="170" SelectionChanged="ColorDifferenceReference_SelectionChanged">
                                            <ComboBoxItem Content="D65 光源" Tag="D65"/>
                                            <ComboBoxItem Content="D50 光源" Tag="D50"/>
                                            <ComboBoxItem Content="A 光源" Tag="A"/>
                                            <ComboBoxItem Content="D75 光源" Tag="D75"/>
                                            <ComboBoxItem Content="图像中心 50px" Tag="ImageCenter"/>
                                            <ComboBoxItem Content="自定义坐标" Tag="Custom"/>
                                            <ComboBoxItem Content="实测基准图" Tag="ReferenceImage"/>
                                        </ComboBox>
                                        <Button x:Name="btnSaveColorDifferenceReference" Content="保存色差基准图" Click="btnSaveColorDifferenceReference_Click" Padding="10,3" Margin="10,0,6,0"/>
                                        <Button Content="计算色差" Click="btnCalculateColorDifference_Click" Padding="10,3"/>
                                    </WrapPanel>
                                    <WrapPanel x:Name="panelColorDifferenceCustomUv" Margin="0,2,0,4">
                                        <TextBlock Text="u:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                        <TextBox x:Name="txtColorDifferenceCustomU" Width="72" Text="0.1978" LostFocus="ColorDifferenceCustom_LostFocus"/>
                                        <TextBlock Text="v:" VerticalAlignment="Center" Margin="12,0,5,0"/>
                                        <TextBox x:Name="txtColorDifferenceCustomV" Width="72" Text="0.4684" LostFocus="ColorDifferenceCustom_LostFocus"/>
                                    </WrapPanel>
                                    <TextBlock x:Name="tbColorDifferenceReferenceStatus" Margin="5,0,5,2" Foreground="Gray" TextWrapping="Wrap"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="导出">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="5">
                            <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3" Padding="8,6" Margin="0,0,0,5">
                                <StackPanel>
                                    <TextBlock Text="导出设置" Style="{StaticResource PanelHeaderTextStyle}" Margin="0,0,0,6"/>
                                    <WrapPanel Margin="0,0,0,0">
                                    <TextBlock Text="导出模式:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                    <ComboBox x:Name="cbExportMode" MinWidth="120" SelectedIndex="0" Margin="0,0,14,0">
                                        <ComboBoxItem Content="按方位角导出" Tag="Angle"/>
                                        <ComboBoxItem Content="按极角导出" Tag="Circle"/>
                                    </ComboBox>
                                    <TextBlock Text="导出通道:" VerticalAlignment="Center" Margin="5,0,5,0"/>
                                    <ComboBox x:Name="cbExportChannel" MinWidth="120" SelectedIndex="1" Margin="0,0,14,0" SelectionChanged="ExportChannel_SelectionChanged">
                                        <ComboBoxItem Content="X通道" Tag="X"/>
                                        <ComboBoxItem Content="Y通道" Tag="Y"/>
                                        <ComboBoxItem Content="Z通道" Tag="Z"/>
                                        <ComboBoxItem Content="x色度" Tag="CieX"/>
                                        <ComboBoxItem Content="y色度" Tag="CieY"/>
                                        <ComboBoxItem Content="u色度" Tag="CieU"/>
                                        <ComboBoxItem Content="v色度" Tag="CieV"/>
                                        <ComboBoxItem Content="色差Δuv" Tag="ColorDifference"/>
                                    </ComboBox>
                                    <Button Content="方位角导出" Click="btnExportAngleMode_Click" Padding="10,4" Margin="0,0,6,0"/>
                                    <Button Content="极角导出" Click="btnExportCircleMode_Click" Padding="10,4"/>
                                    </WrapPanel>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="设置">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="5">
                            <Button Content="保存配置" Click="btnSaveConoscopeConfig_Click" HorizontalAlignment="Left" Padding="12,4" Margin="0,0,0,8"/>
                            <Grid x:Name="GridSetting" Margin="0,0,0,10" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <Border Grid.Row="1" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="3" Margin="0,6,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="8,5">
                        <DockPanel LastChildFill="False">
                            <TextBlock DockPanel.Dock="Left" Text="参考曲线" Style="{StaticResource PanelHeaderTextStyle}"/>
                            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal">
                                <TextBlock Text="模式" Style="{StaticResource PanelMutedTextStyle}" Margin="0,0,5,0"/>
                                <TextBlock x:Name="tbReferenceMode" MinWidth="72" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock Text="当前值" Style="{StaticResource PanelMutedTextStyle}" Margin="0,0,5,0"/>
                                <TextBlock x:Name="tbReferenceValue" MinWidth="78" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock Text="图形" Style="{StaticResource PanelMutedTextStyle}" Margin="0,0,5,0"/>
                                <ToggleButton x:Name="tglReferencePolarMode" Content="极坐标" Style="{StaticResource PanelCompactToggleStyle}" Checked="tglReferencePolarMode_Checked" Unchecked="tglReferencePolarMode_Unchecked" Margin="0,0,12,0"/>
                                <Button Content="导出当前" Click="btnExportCurrentReference_Click" Style="{StaticResource PanelCompactButtonStyle}"/>
                            </StackPanel>
                        </DockPanel>
                    </Border>
                    <Grid Grid.Row="1">
                        <ScottPlot:WpfPlot x:Name="wpfPlotReference" Margin="5" />
                        <local:ConoscopePolarPlot x:Name="polarPlotReference" Margin="5" Visibility="Collapsed" IsHitTestVisible="False"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
