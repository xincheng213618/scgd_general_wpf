<Window x:Class="Spectrum.Calibration.GenerateAmplitudeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Spectrum"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance local:SpectrometerManager}"
        Title="生成幅值标定文件" Width="750" Height="600"
        Background="{DynamicResource GlobalBackground}"
        Initialized="Window_Initialized">
    <Window.InputBindings>
        <KeyBinding Key="G" Command="{Binding GenerateAmplitudeFromExistingCommand}" Modifiers="Ctrl" />
    </Window.InputBindings>
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  Top: File Configuration  -->
        <StackPanel Grid.Row="0">
            <DockPanel Margin="0,0,0,5">
                <Button Margin="5,0,0,0" Command="{Binding SetCSFileCommand}" Content="选择" DockPanel.Dock="Right" />
                <TextBlock MinWidth="140" VerticalAlignment="Center" Text="标准光源绝对光谱文件" />
                <TextBox IsReadOnly="True" Text="{Binding CSFile}" ToolTip="{Binding CSFile}" />
            </DockPanel>

            <DockPanel Margin="0,0,0,5">
                <Button Margin="5,0,0,0" Command="{Binding SetMaguideOutputFileCommand}" Content="选择" DockPanel.Dock="Right" />
                <TextBlock MinWidth="140" VerticalAlignment="Center" Text="幅值标定文件保存路径" />
                <TextBox Text="{Binding MaguideFileOutput}" ToolTip="{Binding MaguideFileOutput}" />
            </DockPanel>

            <!--  Manual Data Acquisition  -->
            <DockPanel Margin="0,5,0,5">
                <Button Padding="10,4" Background="#8E44AD" Command="{Binding GenerateAmplitudeCommand}" Content="自动采集并生成" DockPanel.Dock="Right" Foreground="White" />
                <Button Margin="0,0,8,0" Padding="10,4" Background="#3498DB" Command="{Binding GetDarkDataCommand}" Content="获取暗数据" Foreground="White" />
                <Button Margin="0,0,8,0" Padding="10,4" Background="#E67E22" Command="{Binding GetLightDataCommand}" Content="获取亮数据" Foreground="White" />
                <Separator Margin="8,0" Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
            </DockPanel>
        </StackPanel>

        <!--  Middle: Chart  -->
        <ScottPlot:WpfPlot x:Name="AmplitudePlot" Grid.Row="1" Margin="0,5,0,5" />

        <!--  Bottom: Status  -->
        <TextBlock x:Name="StatusText" Grid.Row="2" Margin="0,5,0,0" Foreground="Gray" Text="就绪" />
    </Grid>
</Window>
