<Window x:Class="Spectrum.Update.SpectrumUpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="620" Height="470" MinWidth="520" MinHeight="400"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        Background="{DynamicResource GlobalBackground}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeadingText" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource GlobalTextBrush}" />

        <Grid Grid.Row="1" Margin="0,16,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock x:Name="CurrentVersionLabel" Foreground="Gray" />
                <TextBlock x:Name="CurrentVersionText" Margin="0,3,0,0" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource GlobalTextBrush}" />
            </StackPanel>
            <StackPanel Grid.Column="1">
                <TextBlock x:Name="LatestVersionLabel" Foreground="Gray" />
                <TextBlock x:Name="LatestVersionText" Margin="0,3,0,0" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource GlobalTextBrush}" />
            </StackPanel>
        </Grid>

        <GroupBox x:Name="ReleaseNotesGroup" Grid.Row="2" Foreground="{DynamicResource GlobalTextBrush}">
            <TextBox x:Name="ReleaseNotesText" Padding="8" IsReadOnly="True" TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" BorderThickness="0" Background="Transparent"
                     Foreground="{DynamicResource GlobalTextBrush}" />
        </GroupBox>

        <StackPanel Grid.Row="3" Margin="0,14,0,0">
            <ProgressBar x:Name="DownloadProgress" Height="8" Minimum="0" Maximum="100" Visibility="Collapsed" />
            <DockPanel Margin="0,7,0,0">
                <TextBlock x:Name="ProgressText" DockPanel.Dock="Right" Margin="12,0,0,0" Foreground="Gray" />
                <TextBlock x:Name="StatusText" TextWrapping="Wrap" Foreground="{DynamicResource GlobalTextBrush}" />
            </DockPanel>
        </StackPanel>

        <StackPanel Grid.Row="4" Margin="0,20,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button x:Name="CancelDownloadButton" MinWidth="90" Margin="0,0,8,0" Padding="14,6"
                    Visibility="Collapsed" Click="CancelDownloadButton_Click" />
            <Button x:Name="CloseButton" MinWidth="90" Margin="0,0,8,0" Padding="14,6" Click="CloseButton_Click" />
            <Button x:Name="PrimaryButton" MinWidth="130" Padding="14,6" IsDefault="True" Click="PrimaryButton_Click" />
        </StackPanel>
    </Grid>
</Window>
