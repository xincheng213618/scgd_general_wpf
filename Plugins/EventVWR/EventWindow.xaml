<Window x:Class="EventVWR.EventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EventVWR"
        mc:Ignorable="d"
        xmlns:event="clr-namespace:System.Diagnostics;assembly=System.Diagnostics.EventLog"
        Title="事件查看器" Height="550" Width="1000" 
        WindowStartupLocation="CenterScreen"
        Initialized="Window_Initialized">

    <Window.Resources>
        <!-- 统一样式 -->
        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>

        <!-- ListView 样式 -->
        <Style TargetType="ListView">
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- GridViewColumnHeader 样式 -->
        <Style TargetType="GridViewColumnHeader">
            <Setter Property="Background" Value="#F5F5F5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
        </Style>

        <!-- ListViewItem 样式 -->
        <Style TargetType="ListViewItem">
            <Setter Property="Padding" Value="4"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#F0F0F0"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E3F2FD"/>
                    <Setter Property="Foreground" Value="#1565C0"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F5F5F5"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Background="#FAFAFA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 标题栏 -->
            <Border Grid.Row="0" Background="#1976D2" Padding="16,12">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="📋" FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="Windows 事件查看器" 
                           FontSize="18" 
                           FontWeight="SemiBold" 
                           Foreground="White" 
                           VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- 主内容区 -->
            <Grid Grid.Row="1" Margin="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="400" MinWidth="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" MinWidth="200"/>
                </Grid.ColumnDefinitions>

                <!-- 左侧事件列表 -->
                <Border Grid.Column="0" 
                    Background="White" 
                    CornerRadius="8" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" Padding="12,10">
                            <TextBlock Text="事件列表" Style="{StaticResource HeaderStyle}" Margin="0"/>
                        </Border>

                        <ListView x:Name="ListViewEvent" 
                              Grid.Row="1" 
                              SelectionChanged="ListViewEvent_SelectionChanged"
                              BorderThickness="0"
                              Background="Transparent"
                              ScrollViewer.HorizontalScrollBarVisibility="Auto">
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="时间" Width="160">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate DataType="{x:Type event:EventLogEntry}">
                                                <TextBlock Text="{Binding TimeGenerated, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" 
                                                       Padding="4,2"
                                                       FontFamily="Consolas"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                    <GridViewColumn Header="来源" Width="180">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate DataType="{x:Type event:EventLogEntry}">
                                                <TextBlock Text="{Binding Source}" 
                                                       Padding="4,2"
                                                       TextTrimming="CharacterEllipsis"/>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </Grid>
                </Border>

                <!-- 分隔线 (可拖拽) -->
                <GridSplitter Grid.Column="1" 
                          Width="6" 
                          Background="Transparent"
                          VerticalAlignment="Stretch" 
                          HorizontalAlignment="Center"
                          Cursor="SizeWE"/>

                <!-- 右侧详情面板 -->
                <Border Grid.Column="2" 
                    Background="White" 
                    CornerRadius="8" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1"
                    Margin="10,0,0,0">
                    <Border.Effect>
                        <DropShadowEffect BlurRadius="8" ShadowDepth="1" Opacity="0.1"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Background="#F8F9FA" CornerRadius="8,8,0,0" Padding="12,10">
                            <TextBlock Text="事件详情" Style="{StaticResource HeaderStyle}" Margin="0"/>
                        </Border>

                        <ScrollViewer Grid.Row="1" 
                                  VerticalScrollBarVisibility="Auto" 
                                  Padding="16">
                            <TextBlock x:Name="MessageText" 
                                   Text="选择一个事件以查看详情..." 
                                   TextWrapping="Wrap" 
                                   Foreground="#666666"
                                   FontSize="13"
                                   LineHeight="22"/>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!-- 状态栏 -->
            <Border Grid.Row="2" Background="#F5F5F5" Padding="16,8" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
                <TextBlock x:Name="StatusText" Text="就绪" Foreground="#666666" FontSize="12"/>
            </Border>
    </Grid>
</Window>