# Web architecture

Current module responsibilities, compatibility boundaries, architecture checks and explicitly planned evolution are maintained in [Web architecture](../docs/03-architecture/components/web.md).

Use the matching source checkout when following this link. The guide distinguishes implemented ports and build gates from target structures and proposed interfaces.
